# Redis and Application Caching

## 1. Caching fundamentals and fit

- [ ] CACHE-FUND-001. What problem does caching solve in backend applications?
- [ ] CACHE-FUND-002. What are the main trade-offs of adding a cache?
- [ ] CACHE-FUND-003. What is the difference between local cache and distributed cache?
- [ ] CACHE-FUND-004. When is an in-process cache such as Caffeine better than Redis?
- [ ] CACHE-FUND-005. When is Redis a better fit than an in-process cache?
- [ ] CACHE-FUND-006. When should you avoid caching?
- [ ] CACHE-FUND-007. What data is safe to cache, and what data is risky to cache?
- [ ] CACHE-FUND-008. What is a cache hit rate, and why can it be misleading?
- [ ] CACHE-FUND-009. How do read/write ratio and data volatility affect caching decisions?
- [ ] 🧠 CACHE-FUND-010. How can caching make a system less reliable instead of more reliable?
- [ ] CACHE-FUND-011. What is multi-level caching, such as Caffeine plus Redis, and what trade-offs does it introduce?

## 2. Cache patterns and lifecycle

- [ ] CACHE-PAT-001. What is cache-aside?
- [ ] CACHE-PAT-002. What is read-through caching?
- [ ] CACHE-PAT-003. What is write-through caching?
- [ ] CACHE-PAT-004. What is cache warming, and when is it useful?
- [ ] CACHE-PAT-005. What is negative caching, and when should it be used?
- [ ] CACHE-PAT-006. How do you choose TTL values?
- [ ] 🧠 CACHE-PAT-007. How does write-behind caching affect durability and ordering?

## 3. Cache keys, TTLs and invalidation

- [ ] CACHE-INV-001. How do you design cache keys?
- [ ] CACHE-INV-002. Why should cache keys include tenant, locale, version or permission context when relevant?
- [ ] CACHE-INV-003. What is cache invalidation, and why is it hard?
- [ ] CACHE-INV-004. What is the difference between eviction and invalidation?
- [ ] CACHE-INV-005. How can stale cache data break business correctness?
- [ ] CACHE-INV-006. When should you invalidate a single key versus a whole cache region?
- [ ] CACHE-INV-007. How should cache invalidation interact with database transactions?
- [ ] CACHE-INV-008. How do you handle cache invalidation across multiple services that read the same data?
- [ ] 🧠 CACHE-INV-009. How can event-driven invalidation fail in production?

## 4. Cache failure modes and correctness traps

- [ ] CACHE-FAIL-001. What is cache stampede, and how can it be mitigated?
- [ ] CACHE-FAIL-002. What is cache penetration?
- [ ] CACHE-FAIL-003. What is cache avalanche?
- [ ] CACHE-FAIL-004. What is a hot key, and why is it dangerous?
- [ ] CACHE-FAIL-005. How can TTL jitter reduce synchronized cache expiration?
- [ ] CACHE-FAIL-006. How do locks or single-flight techniques reduce duplicate cache rebuilds?
- [ ] CACHE-FAIL-007. How should a service behave when cached data is stale but the database is slow?
- [ ] CACHE-FAIL-008. What can go wrong if user-specific data is cached under a shared key?
- [ ] CACHE-FAIL-009. What can go wrong if authorization-sensitive data is cached incorrectly?
- [ ] CACHE-FAIL-010. What is stale-while-revalidate, and when can it be acceptable?
- [ ] CACHE-FAIL-011. How can cache recovery after an outage overload the database?

## 5. Redis fundamentals and data structures

- [ ] REDIS-FUND-001. What is Redis commonly used for in Java backend systems?
- [ ] REDIS-FUND-002. Why is Redis fast for many cache workloads?
- [ ] REDIS-FUND-003. What Redis data structures are useful for backend services?
- [ ] REDIS-FUND-004. When would you use a Redis string?
- [ ] REDIS-FUND-005. When would you use a Redis hash?
- [ ] REDIS-FUND-006. When would you use a Redis set?
- [ ] REDIS-FUND-007. When would you use a Redis sorted set?
- [ ] REDIS-FUND-008. Why should Redis usually not be treated as the primary source of truth for critical data?
- [ ] 🧠 REDIS-FUND-009. What are Redis probabilistic data structures such as Bloom filters useful for?

## 6. Redis commands, performance and memory behavior

- [ ] REDIS-PERF-001. Why should Redis commands be chosen with time complexity in mind?
- [ ] REDIS-PERF-002. Why can large keys or large values hurt Redis performance?
- [ ] REDIS-PERF-003. Why is `KEYS` dangerous in production, and when should `SCAN` be used?
- [ ] REDIS-PERF-004. What is pipelining, and when does it help?
- [ ] REDIS-PERF-005. What is an atomic Redis command, and why does it matter?
- [ ] REDIS-PERF-006. How do Redis eviction policies affect application behavior?
- [ ] REDIS-PERF-007. How do serialization choices affect latency, memory and compatibility?
- [ ] 🧠 REDIS-PERF-008. How can slow commands or big-key problems be diagnosed?
- [ ] 🧠 REDIS-PERF-009. What are hot-key mitigation strategies in Redis?
- [ ] REDIS-PERF-010. What are Redis transactions with `MULTI` and `EXEC`, and what do they not guarantee?
- [ ] REDIS-PERF-011. When would you use `WATCH` for optimistic locking in Redis?
- [ ] REDIS-PERF-012. When should Lua scripts be used in Redis?
- [ ] REDIS-PERF-013. Why can multi-step commands such as `INCR` plus `EXPIRE` create correctness bugs?

## 7. Spring Cache, Spring Data Redis and Java clients

- [ ] REDIS-SPRING-001. How does Spring Cache use Redis?
- [ ] REDIS-SPRING-002. What do `@Cacheable`, `@CachePut` and `@CacheEvict` do?
- [ ] REDIS-SPRING-003. How does Spring generate default cache keys?
- [ ] REDIS-SPRING-004. When should you provide a custom cache key?
- [ ] REDIS-SPRING-005. What are common mistakes with `@Cacheable` on methods inside the same class?
- [ ] REDIS-SPRING-006. How do you configure per-cache TTLs with Redis Cache Manager?
- [ ] REDIS-SPRING-007. How do you choose serializers for Redis values in a Spring Boot application?
- [ ] REDIS-SPRING-008. How does transaction-aware cache behavior affect consistency?
- [ ] 🧠 REDIS-SPRING-009. What are the trade-offs of lock-free versus locking Redis cache writers?
- [ ] REDIS-SPRING-010. What are the practical differences between Lettuce and Jedis for Java services?
- [ ] REDIS-SPRING-011. How should Redis connections, pools and thread usage be configured in Java clients?
- [ ] REDIS-SPRING-012. What are the risks of using blocking Redis calls in reactive or event-loop based code?
- [ ] REDIS-SPRING-013. How should Java Redis clients handle retries, reconnects and topology refresh after failover?

## 8. Redis use cases beyond simple caching

- [ ] REDIS-USE-001. How can Redis be used for rate limiting?
- [ ] REDIS-USE-002. What are fixed window, sliding window and token bucket rate limiters?
- [ ] REDIS-USE-003. How can Redis be used for short-lived idempotency keys?
- [ ] REDIS-USE-004. When is a Redis distributed lock appropriate?
- [ ] REDIS-USE-005. What can go wrong with Redis distributed locks?
- [ ] REDIS-USE-006. Why should Redis locks use a `SET NX PX`-style acquire, a unique value and an expiration?
- [ ] REDIS-USE-007. When should a database constraint or transaction be preferred over a Redis lock?
- [ ] REDIS-USE-008. Why should Redis rate limiters use atomic commands or Lua scripts?
- [ ] REDIS-USE-009. Why should releasing a Redis lock verify the unique value atomically?
- [ ] 🧠 REDIS-USE-010. What is the Redlock algorithm, and why is it controversial?

## 9. Availability, persistence and scaling

- [ ] REDIS-OPS-001. What should a Java service do if Redis is unavailable?
- [ ] REDIS-OPS-002. How should timeouts be configured for Redis clients?
- [ ] REDIS-OPS-003. Why should Redis calls usually have bounded latency budgets?
- [ ] REDIS-OPS-004. When is persistence unnecessary for a pure cache?
- [ ] REDIS-OPS-005. What is Redis replication used for?
- [ ] REDIS-OPS-006. What consistency and data-loss risks remain with Redis replication or failover?
- [ ] REDIS-OPS-007. What is Redis Sentinel, and what problem does it solve?
- [ ] REDIS-OPS-008. What is Redis Cluster, and how do hash slots affect key placement?
- [ ] REDIS-OPS-009. Why do Redis Cluster multi-key operations need keys in the same hash slot?
- [ ] 🧠 REDIS-OPS-010. What are key tags in Redis Cluster, and when should they be used?
- [ ] REDIS-OPS-011. How should Java clients behave during Redis failover or topology changes?
- [ ] REDIS-OPS-012. What is the difference between RDB and AOF persistence?
- [ ] REDIS-OPS-013. How do Redis restart, cache warmup and cold-cache behavior affect backend services?
- [ ] REDIS-OPS-014. How should Redis be secured in a production Java backend environment?
- [ ] 🧠 REDIS-OPS-015. When can Redis persistence make a cache failure mode worse?

## 10. Testing, observability and production scenarios

- [ ] REDIS-PROD-001. How would you test code that uses Spring Cache?
- [ ] REDIS-PROD-002. When should Redis integration tests use Testcontainers?
- [ ] REDIS-PROD-003. How would you verify that caching actually reduces database load?
- [ ] REDIS-PROD-004. What Redis server metrics should be monitored?
- [ ] REDIS-PROD-005. What application cache metrics should be monitored?
- [ ] REDIS-PROD-006. How would you debug a cache that returns stale data?
- [ ] REDIS-PROD-007. How would you debug Redis memory growth?
- [ ] REDIS-PROD-008. How would you handle a Redis outage during peak traffic?
- [ ] REDIS-PROD-009. How would you investigate a Redis latency spike?
- [ ] REDIS-PROD-010. How would you diagnose Redis client connection pool exhaustion or command timeouts?
- [ ] REDIS-PROD-011. How would you distinguish Redis server latency from network or client-side latency?

## Counts

- Total questions: 109
- Priority 1 questions: 99
- Priority 2 questions: 10
