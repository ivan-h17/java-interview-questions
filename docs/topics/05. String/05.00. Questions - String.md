# String

## 1. String fundamentals and mental model

- [ ] STR-FUND-001. What is `String` in Java, and what does it represent?
- [ ] STR-FUND-002. Why is `String` immutable and declared `final`?
- [ ] STR-FUND-003. What are the main consequences of `String` immutability for sharing, hashing and the String Pool?
- [ ] STR-FUND-004. What would break if `String` were mutable?
- [ ] STR-FUND-005. What is the difference between an empty string, a blank string and `null`?
- [ ] STR-FUND-006. How should nullable strings be handled at Java API boundaries?
- [ ] STR-FUND-007. What is the difference between character data, binary data and textual representation?
- [ ] STR-FUND-008. What production bugs happen when strings are treated as "simple text" only?

## 2. Equality, hashing and map keys

- [ ] STR-EQ-001. What is the difference between `==` and `equals()` for strings?
- [ ] STR-EQ-002. Why can comparing strings with `==` appear to work sometimes and fail later?
- [ ] STR-EQ-003. What does `equalsIgnoreCase()` do, and what are its limitations?
- [ ] STR-EQ-004. How does `String.hashCode()` work conceptually, and is it cached?
- [ ] STR-EQ-005. Can two different strings have the same hash code, and why does that matter?
- [ ] STR-EQ-006. Why is `String` commonly used as a `HashMap` key?
- [ ] STR-EQ-007. How should you compare strings safely when either value can be `null`?

## 3. String literals, String Pool and interning

- [ ] STR-POOL-001. How does the String Pool work?
- [ ] STR-POOL-002. What is the difference between a string literal and `new String(...)`?
- [ ] STR-POOL-003. What is the difference between compile-time string constants and runtime concatenation?
- [ ] STR-POOL-004. What does `String.intern()` return, and when is manual interning useful?
- [ ] STR-POOL-005. How can excessive or unbounded interning cause memory problems?
- [ ] STR-POOL-006. What is the difference between string deduplication and interning?
- [ ] STR-POOL-007. Why should you not synchronize on string literals or interned strings?
- [ ] 🧠 STR-POOL-008. Where is the String Pool stored in modern JVMs, and how did this change historically?
- [ ] 🧠 STR-POOL-009. How does the String Pool interact with class loading?
- [ ] 🧠 STR-POOL-010. How would you design a safe custom string canonicalization cache?

## 4. String construction and conversion

- [ ] STR-CREATE-001. What happens when a `String` is created from a `char[]`, and why does copying matter?
- [ ] STR-CREATE-002. What happens when a `String` is created from a `byte[]`, and why should you specify a `Charset`?
- [ ] STR-CREATE-003. What is the difference between `String.valueOf(obj)`, `obj.toString()` and `Objects.toString(obj)`?
- [ ] STR-CREATE-004. What are `String.join()` and `StringJoiner` used for?
- [ ] STR-CREATE-005. When should you use `String.format()` or `formatted()`?
- [ ] STR-CREATE-006. When should you avoid creating many temporary strings?

## 5. Unicode, charsets and normalization

- [ ] STR-UNICODE-001. What is the difference between Unicode, a character set and a character encoding?
- [ ] STR-UNICODE-002. What is the difference between UTF-8 and UTF-16, and how does Java represent text conceptually?
- [ ] STR-UNICODE-003. What is a Java `char`, and how is it related to code units, code points and surrogate pairs?
- [ ] STR-UNICODE-004. What is a grapheme cluster?
- [ ] STR-UNICODE-005. Why is `String.length()` not the number of user-perceived characters?
- [ ] STR-UNICODE-006. How should you iterate over Unicode code points in a `String`?
- [ ] STR-UNICODE-007. How do you convert `String` to `byte[]` and back safely?
- [ ] STR-UNICODE-008. Why should backend code avoid the platform default charset?
- [ ] STR-UNICODE-009. What can go wrong when text is decoded with the wrong charset?
- [ ] STR-UNICODE-010. What is Unicode normalization, and why can two visually identical strings be unequal?
- [ ] 🧠 STR-UNICODE-011. What is the difference between NFC and NFD normalization, and where can it matter?
- [ ] STR-UNICODE-012. How can Unicode normalization affect login, search and uniqueness checks?
- [ ] 🧠 STR-UNICODE-013. What are homoglyph or confusable-character attacks?

## 6. Comparison, ordering and locale

- [ ] STR-I18N-001. How does `String.compareTo()` order strings, and is it locale-aware?
- [ ] STR-I18N-002. What is the difference between binary or code-unit ordering and human-language ordering?
- [ ] STR-I18N-003. When should you use `Collator` instead of `String.compareTo()`?
- [ ] STR-I18N-004. What is the difference between case-sensitive and case-insensitive comparison?
- [ ] STR-I18N-005. Why can `toLowerCase()` and `toUpperCase()` be locale-sensitive?
- [ ] STR-I18N-006. When should you use `Locale.ROOT`, and when should you use a user-specific `Locale`?
- [ ] 🧠 STR-I18N-007. How would you design case-insensitive search or identifier normalization without locale bugs?

## 7. Concatenation, formatting and builders

- [ ] STR-CONCAT-001. What happens when strings are concatenated using the `+` operator?
- [ ] STR-CONCAT-002. What is the difference between compile-time and runtime string concatenation?
- [ ] 🧠 STR-CONCAT-003. What changed about string concatenation in Java 9 with `invokedynamic`?
- [ ] STR-CONCAT-004. When should you use `StringBuilder` instead of repeated concatenation?
- [ ] STR-CONCAT-005. What is the difference between `StringBuilder` and `StringBuffer`?
- [ ] 🧠 STR-CONCAT-006. How do `StringBuilder` capacity and growth affect performance?
- [ ] STR-CONCAT-007. Why should you avoid `+` concatenation in large loops?
- [ ] STR-CONCAT-008. When is `+` concatenation perfectly fine?
- [ ] STR-CONCAT-009. What is the difference between `String.concat()` and `+`?
- [ ] STR-CONCAT-010. Why can `String.format()` be slower than simple concatenation?
- [ ] STR-CONCAT-011. Why should backend logging prefer parameterized logging over eager string concatenation?
- [ ] STR-CONCAT-012. How would you build a large CSV or JSON-like string safely?

## 8. Core String API, regex and parsing

- [ ] STR-API-001. What is the difference between `isEmpty()`, `isBlank()`, `trim()` and `strip()`?
- [ ] STR-API-002. What are text blocks, and what are their traps for SQL or JSON?
- [ ] STR-API-003. What is the difference between `chars()` and `codePoints()`?
- [ ] STR-API-004. What do `toCharArray()` and `getBytes(Charset)` return, and why does copying matter?
- [ ] 🧠 STR-OPS-001. How did old `substring()` sharing cause memory leaks, and how does modern `substring()` differ?
- [ ] STR-OPS-002. Why can `split(".")` or `split("|")` be wrong?
- [ ] STR-OPS-003. What is the difference between splitting by a regex and splitting by a literal delimiter?
- [ ] STR-OPS-004. What is the difference between `replace()`, `replaceAll()` and `replaceFirst()`?
- [ ] STR-OPS-005. Why can regex replacement strings require escaping, and what does `Matcher.quoteReplacement()` do?
- [ ] STR-OPS-006. What is the difference between `contains()`, `indexOf()`, `startsWith()`, `endsWith()` and `matches()`?
- [ ] STR-OPS-007. When should you precompile a `Pattern`?
- [ ] 🧠 STR-OPS-008. What is catastrophic backtracking, and how can regex cause denial-of-service risk?
- [ ] 🧠 STR-OPS-009. How would you parse a large text file without creating too many intermediate strings?

## 9. CharSequence, API design and domain modeling

- [ ] STR-TYPE-001. What is `CharSequence`, and which common classes implement it?
- [ ] STR-TYPE-002. When should an API accept `CharSequence` instead of `String`?
- [ ] STR-TYPE-003. Why can mutable `CharSequence` implementations cause bugs, and how should you defensively copy them?
- [ ] STR-COLL-001. Why should string keys often be normalized before storing them in a map?
- [ ] STR-COLL-002. What are the risks of using raw user input strings as map or cache keys?
- [ ] STR-COLL-003. When would you use an enum or value object instead of a plain string?
- [ ] STR-COLL-004. Why is a stringly typed domain model dangerous?
- [ ] STR-COLL-005. What are the risks of canonicalizing unbounded user input?

## 10. Strings in backend APIs, JSON, SQL and databases

- [ ] STR-BE-001. How should HTTP API boundaries define charset and media type for textual payloads?
- [ ] STR-BE-002. How should backend code read and write request, response and file bytes as text safely?
- [ ] STR-BE-003. How do JSON libraries handle Java strings, and what can go wrong with manual JSON concatenation?
- [ ] STR-BE-004. Why should SQL be built with prepared statements instead of string concatenation?
- [ ] STR-BE-005. How should string length validation be designed for API inputs?
- [ ] STR-BE-006. Why can database column length checks differ from Java `String.length()`?
- [ ] STR-BE-007. How can string truncation cause production bugs?
- [ ] STR-BE-008. When should you stream textual data instead of loading it into one `String`?
- [ ] 🧠 STR-BE-009. How can database encoding, collation or normalization differ from Java string behavior?

## 11. Security, secrets and validation

- [ ] STR-SEC-001. Why is string validation security-sensitive?
- [ ] STR-SEC-002. What is canonicalization before validation, and why does the order matter?
- [ ] STR-SEC-003. How can path traversal attacks involve string processing?
- [ ] STR-SEC-004. Why should you not build shell commands, HTML, JSON or XML by concatenating untrusted strings?
- [ ] STR-SEC-005. What is output encoding, and how does it relate to strings?
- [ ] STR-SEC-006. How can log injection or CRLF injection involve strings?
- [ ] STR-SEC-007. Why is `String` not ideal for passwords, tokens or long-lived cryptographic secrets?
- [ ] STR-SEC-008. Is `char[]` always safer than `String` for secrets, and what are the limitations?

## 12. Memory, GC and diagnostics

- [ ] 🧠 STR-MEM-001. How is a `String` represented in memory conceptually?
- [ ] 🧠 STR-MEM-002. What are Compact Strings in Java 9+, and do they mean Java stores strings as UTF-8 internally?
- [ ] 🧠 STR-MEM-003. Why is measuring or estimating `String` memory footprint non-trivial?
- [ ] STR-MEM-004. How do many short or duplicate strings affect heap usage?
- [ ] STR-MEM-005. How can large strings or temporary string creation affect allocation rate, GC pressure and latency?
- [ ] STR-MEM-006. How would you reduce memory usage in a string-heavy service?
- [ ] 🧠 STR-GC-001. What is G1 String deduplication, and does it deduplicate `String` objects or backing storage?
- [ ] 🧠 STR-GC-002. When can String deduplication help, and what are its costs?
- [ ] STR-GC-003. How can caches, interning, `ThreadLocal` values or log buffers keep strings alive?
- [ ] 🧠 STR-DIAG-001. How would you inspect string-heavy memory usage in a heap dump?
- [ ] 🧠 STR-DIAG-002. How can Java Flight Recorder or allocation profiling reveal string hot spots?
- [ ] STR-DIAG-003. How do you find who keeps a string alive?
- [ ] STR-DIAG-004. How should heap dumps that may contain sensitive strings be handled?

## 13. Concurrency and production scenarios

- [ ] STR-CONC-001. Is `String` thread-safe, and why is it safely shareable between threads?
- [ ] STR-CONC-002. Does using `StringBuffer` make a whole multithreaded workflow thread-safe?
- [ ] STR-SCENARIO-001. A service returns corrupted characters for non-English users. How would you debug it?
- [ ] STR-SCENARIO-002. A service has high allocation rate and many short-lived strings. How would you debug it?
- [ ] STR-SCENARIO-003. A heap dump shows millions of duplicate strings. What would you check?
- [ ] STR-SCENARIO-004. A login check fails for visually identical usernames. What could be wrong?
- [ ] STR-SCENARIO-005. An endpoint accepts emoji, but validation rejects valid input. What could be wrong?
- [ ] STR-SCENARIO-006. A regex used on user input causes latency spikes. How would you investigate it?
- [ ] STR-SCENARIO-007. A batch job builds a huge string and crashes with `OutOfMemoryError`. How would you redesign it?
- [ ] STR-SCENARIO-008. A cache keyed by raw request strings grows without bound. How would you fix it?
- [ ] STR-SCENARIO-009. A service compares strings with `==` and behaves inconsistently. How would you explain the bug?
- [ ] 🧠 STR-SCENARIO-010. A service migrated from Java 8 to Java 17/21/25. What String-related behavior would you review?

## Counts

- Total questions: 126
- Priority 1 questions: 106
- Priority 2 questions: 20
