# String

## 1. String fundamentals and mental model

- [ ] STR-FUND-001. What is `String` in Java?
- [ ] STR-FUND-002. Why is `String` one of the most important classes in Java backend development?
- [ ] STR-FUND-003. Why is `String` immutable?
- [ ] STR-FUND-004. What are the main consequences of `String` immutability?
- [ ] STR-FUND-005. Why are immutable strings useful for thread-safety?
- [ ] STR-FUND-006. Why are immutable strings useful for hashing and `HashMap` keys?
- [ ] STR-FUND-007. What is the difference between an empty string and `null`?
- [ ] STR-FUND-008. How should you handle nullable strings in Java APIs?
- [ ] STR-FUND-009. What is the difference between `String`, `StringBuilder`, `StringBuffer`, and `CharSequence`?
- [ ] STR-FUND-010. Why is `String` declared `final`?
- [ ] STR-FUND-011. What would break if `String` were mutable?
- [ ] STR-FUND-012. What are common production bugs caused by treating strings as simple text only?
- [ ] STR-FUND-013. What is the difference between character data, binary data, and textual representation?
- [ ] STR-FUND-014. Why is `String` not suitable for all text-processing workloads?
- [ ] STR-FUND-015. What is the difference between value equality and object identity for strings?
- [ ] STR-FUND-016. What are the most common anti-patterns when working with strings in Java?

## 2. String literals, String Pool and interning

- [ ] STR-POOL-001. How does the String Pool work?
- [ ] STR-POOL-002. What is the difference between creating a `String` using a literal and using `new String(...)`?
- [ ] STR-POOL-003. When should you use `intern()`?
- [ ] STR-POOL-004. What does `String.intern()` actually return?
- [ ] STR-POOL-005. Are string literals automatically interned?
- [ ] STR-POOL-006. Are compile-time string constants interned?
- [ ] STR-POOL-007. What is the difference between compile-time concatenation and runtime concatenation for string literals?
- [ ] STR-POOL-008. Can you use `==` to compare strings from the String Pool?
- [ ] STR-POOL-009. Where is the String Pool stored in modern JVMs?
- [ ] STR-POOL-010. Was the String Pool always stored in the same memory area?
- [ ] STR-POOL-011. Can the String Pool cause `OutOfMemoryError`?
- [ ] STR-POOL-012. How can excessive interning cause memory problems?
- [ ] STR-POOL-013. How does the String Pool interact with class loading?
- [ ] STR-POOL-014. Can two class loaders have different references to the same interned string?
- [ ] STR-POOL-015. What is the difference between deduplication and interning?
- [ ] STR-POOL-016. When is manual interning a good optimization?
- [ ] STR-POOL-017. When is manual interning a bad idea?
- [ ] STR-POOL-018. How would you design a safe custom string canonicalization cache?
- [ ] STR-POOL-019. How can you test whether two strings point to the same interned object?
- [ ] STR-POOL-020. What happens when you call `intern()` on a dynamically created string?
- [ ] STR-POOL-021. How does `intern()` behave with equal strings created at runtime?
- [ ] STR-POOL-022. Why should you not use interned strings as locks?
- [ ] STR-POOL-023. What are the risks of synchronizing on string literals?
- [ ] STR-POOL-024. How would you explain the String Pool in one minute during an interview?

## 3. String construction, copying and object creation

- [ ] STR-CREATE-001. What objects are created by `String s = "abc";`?
- [ ] STR-CREATE-002. What objects are created by `String s = new String("abc");`?
- [ ] STR-CREATE-003. Why is `new String("abc")` usually unnecessary?
- [ ] STR-CREATE-004. What happens when a `String` is created from a `char[]`?
- [ ] STR-CREATE-005. Does `new String(char[])` copy the array?
- [ ] STR-CREATE-006. What happens when a `String` is created from a `byte[]`?
- [ ] STR-CREATE-007. Why should you specify a `Charset` when creating a `String` from bytes?
- [ ] STR-CREATE-008. What is the difference between `String.valueOf()` and `toString()`?
- [ ] STR-CREATE-009. What does `String.copyValueOf()` do?
- [ ] STR-CREATE-010. How does `String.join()` work conceptually?
- [ ] STR-CREATE-011. How does `String.format()` create strings?
- [ ] STR-CREATE-012. When should you avoid creating many temporary strings?

## 4. Equality, comparison, ordering and hashing

- [ ] STR-EQ-001. Can you use `==` to compare `String` values?
- [ ] STR-EQ-002. What is the difference between `==` and `equals()` for `String`?
- [ ] STR-EQ-003. Why should business string comparison usually use `equals()`?
- [ ] STR-EQ-004. What does `equalsIgnoreCase()` do?
- [ ] STR-EQ-005. What are the limitations of `equalsIgnoreCase()`?
- [ ] STR-EQ-006. How does `String.hashCode()` work conceptually?
- [ ] STR-EQ-007. Is the `String` hash code cached?
- [ ] STR-EQ-008. Can two different strings have the same hash code?
- [ ] STR-EQ-009. Why is `String` often used as a key in `HashMap`?
- [ ] STR-EQ-010. What are the risks of using user-controlled strings as map keys?
- [ ] STR-EQ-011. Why does `String` implement `Comparable`?
- [ ] STR-EQ-012. How does `String.compareTo()` order strings?
- [ ] STR-EQ-013. Is `String.compareTo()` locale-aware?
- [ ] STR-EQ-014. When should you use `Collator` instead of `String.compareTo()`?
- [ ] STR-EQ-015. What is the difference between binary/code-unit ordering and human language ordering?
- [ ] STR-EQ-016. Why can sorting strings be tricky in internationalized applications?
- [ ] STR-EQ-017. What is the difference between case-sensitive and case-insensitive comparison?
- [ ] STR-EQ-018. How would you compare strings safely when either value can be `null`?

## 5. Immutability, security and reflection

- [ ] STR-IMM-001. Why is `String` immutable?
- [ ] STR-IMM-002. How does immutability make `String` safe for sharing between threads?
- [ ] STR-IMM-003. How does immutability help the String Pool?
- [ ] STR-IMM-004. How does immutability help `String` work as a `HashMap` key?
- [ ] STR-IMM-005. How does immutability help security-sensitive APIs such as class loading, file paths, URLs and database connections?
- [ ] 🧠 STR-IMM-006. Can the contents of a `String` be changed through reflection?
- [ ] STR-IMM-007. Can the contents of a `String` be changed through reflection in modern Java with strong encapsulation?
- [ ] 🧠 STR-IMM-008. Why is relying on reflection to modify `String` internals unsafe and non-portable?
- [ ] STR-IMM-009. What changed for reflective access after the Java module system?
- [ ] STR-IMM-010. Why should passwords and secrets usually not be stored in `String`?
- [ ] STR-IMM-011. Is `char[]` always safer than `String` for secrets?
- [ ] STR-IMM-012. What are the limitations of clearing a `char[]` after use?
- [ ] STR-IMM-013. How can strings leak secrets through logs, heap dumps or error messages?
- [ ] STR-IMM-014. Why should you avoid putting secrets into exception messages?
- [ ] STR-IMM-015. Why should you avoid logging raw request payloads that contain strings?
- [ ] STR-IMM-016. What are the risks of string-based configuration values in production?
- [ ] STR-IMM-017. What is string canonicalization and why can it be security-sensitive?
- [ ] STR-IMM-018. What is a path traversal issue and how can string processing contribute to it?

## 6. Memory layout, Compact Strings and memory footprint

- [ ] 🧠 STR-MEM-001. How is a `String` represented in memory conceptually?
- [ ] 🧠 STR-MEM-002. How was `String` represented before Java 9?
- [ ] 🧠 STR-MEM-003. What are Compact Strings in Java 9+?
- [ ] 🧠 STR-MEM-004. Why were Compact Strings introduced?
- [ ] 🧠 STR-MEM-005. Does Compact Strings mean Java stores strings as UTF-8 internally?
- [ ] 🧠 STR-MEM-006. What are LATIN1 and UTF16 coders in Compact Strings?
- [ ] 🧠 STR-MEM-007. How does Compact Strings affect memory footprint?
- [ ] 🧠 STR-MEM-008. How can Compact Strings affect performance?
- [ ] 🧠 STR-MEM-009. What is the `-XX:-CompactStrings` option?
- [ ] 🧠 STR-MEM-010. How can you estimate how much memory a `String` uses?
- [ ] STR-MEM-011. Why is measuring `String` memory footprint non-trivial?
- [ ] 🧠 STR-MEM-012. What is the role of object header, reference fields and array header in String memory usage?
- [ ] STR-MEM-013. How do compressed ordinary object pointers affect String memory footprint?
- [ ] STR-MEM-014. What is the difference between character length and memory size of a `String`?
- [ ] STR-MEM-015. How do many short strings affect heap usage?
- [ ] STR-MEM-016. How do many duplicate strings affect heap usage?
- [ ] STR-MEM-017. How can large strings affect GC pressure?
- [ ] STR-MEM-018. How can temporary string creation affect allocation rate?
- [ ] STR-MEM-019. How do string-heavy applications show up in heap dumps?
- [ ] STR-MEM-020. What does it mean when `byte[]` or `String` dominates a heap dump?
- [ ] STR-MEM-021. How would you reduce memory usage in a string-heavy service?
- [ ] STR-MEM-022. How would you decide whether string memory optimization is worth doing?

## 7. Unicode, char, code points and encodings

- [ ] STR-UNICODE-001. What is string encoding?
- [ ] STR-UNICODE-002. What is the difference between a character set and a character encoding?
- [ ] STR-UNICODE-003. What is Unicode?
- [ ] STR-UNICODE-004. What is UTF-8?
- [ ] STR-UNICODE-005. What is UTF-16?
- [ ] STR-UNICODE-006. How does Java represent text conceptually?
- [ ] STR-UNICODE-007. What is a Java `char`?
- [ ] STR-UNICODE-008. Why can one Unicode character require two Java `char` values?
- [ ] STR-UNICODE-009. What are surrogate pairs?
- [ ] STR-UNICODE-010. What is a code point?
- [ ] STR-UNICODE-011. What is a grapheme cluster?
- [ ] STR-UNICODE-012. What is the difference between `length()` and the number of user-perceived characters?
- [ ] STR-UNICODE-013. How should you iterate over Unicode code points in a `String`?
- [ ] STR-UNICODE-014. How do you correctly convert `String` to `byte[]` and back?
- [ ] STR-UNICODE-015. Why should you avoid using the platform default charset?
- [ ] STR-UNICODE-016. What is `StandardCharsets.UTF_8` and why is it useful?
- [ ] STR-UNICODE-017. What can go wrong when decoding bytes with the wrong charset?
- [ ] STR-UNICODE-018. What is mojibake?
- [ ] STR-UNICODE-019. What is Unicode normalization?
- [ ] STR-UNICODE-020. Why can two visually identical strings be unequal?
- [ ] STR-UNICODE-021. When should you normalize Unicode text?
- [ ] STR-UNICODE-022. What is the difference between NFC and NFD normalization?
- [ ] STR-UNICODE-023. How can Unicode normalization affect login, search and uniqueness checks?
- [ ] STR-UNICODE-024. What are homoglyph attacks?
- [ ] STR-UNICODE-025. How can Unicode make input validation harder?
- [ ] STR-UNICODE-026. How would you safely handle text in HTTP APIs?
- [ ] STR-UNICODE-027. How would you safely handle text in database columns?
- [ ] STR-UNICODE-028. How does PostgreSQL database encoding interact with Java strings?

## 8. Concatenation, formatting and builders

- [ ] STR-CONCAT-001. What happens when strings are concatenated using the `+` operator?
- [ ] STR-CONCAT-002. How does the Java compiler optimize string concatenation?
- [ ] STR-CONCAT-003. What is the difference between compile-time and runtime concatenation?
- [ ] STR-CONCAT-004. What changed about string concatenation in Java 9?
- [ ] STR-CONCAT-005. What is `invokedynamic`-based string concatenation?
- [ ] STR-CONCAT-006. When should you use `StringBuilder`?
- [ ] STR-CONCAT-007. When should you use `StringBuffer`?
- [ ] STR-CONCAT-008. Why is `StringBuffer` usually slower than `StringBuilder`?
- [ ] STR-CONCAT-009. Is `StringBuilder` thread-safe?
- [ ] STR-CONCAT-010. Is `StringBuffer` always the best choice in multithreaded code?
- [ ] STR-CONCAT-011. How does initial capacity affect `StringBuilder` performance?
- [ ] 🧠 STR-CONCAT-012. What happens when `StringBuilder` needs to grow its internal buffer?
- [ ] STR-CONCAT-013. Why should you avoid `+` concatenation in large loops?
- [ ] STR-CONCAT-014. When is `+` concatenation perfectly fine?
- [ ] STR-CONCAT-015. What is the difference between `String.concat()` and `+`?
- [ ] STR-CONCAT-016. What is `String.join()` used for?
- [ ] STR-CONCAT-017. What is `StringJoiner` used for?
- [ ] STR-CONCAT-018. When should you use `String.format()`?
- [ ] STR-CONCAT-019. Why can `String.format()` be slower than simple concatenation?
- [ ] STR-CONCAT-020. What is `formatted()` and how does it relate to `String.format()`?
- [ ] STR-CONCAT-021. How would you build a large CSV or JSON-like string safely?
- [ ] STR-CONCAT-022. Why should you usually use a real JSON library instead of manual string concatenation?

## 9. Substring, search, split, replace and regex behavior

- [ ] STR-OPS-001. What does `substring()` do?
- [ ] STR-OPS-002. How did `substring()` work before Java 7?
- [ ] STR-OPS-003. Why was the old `substring()` implementation changed in Java 7?
- [ ] STR-OPS-004. Does modern `substring()` usually copy the underlying data?
- [ ] STR-OPS-005. What is the memory-leak risk of old substring sharing?
- [ ] STR-OPS-006. How does `split()` work?
- [ ] STR-OPS-007. Why can `split(".")` be wrong?
- [ ] STR-OPS-008. Why can `split("|")` be wrong?
- [ ] STR-OPS-009. What is the difference between `split(regex)` and splitting by a literal delimiter?
- [ ] STR-OPS-010. When should you precompile a `Pattern` instead of repeatedly calling `split()` or `matches()`?
- [ ] STR-OPS-011. What is the difference between `replace()` and `replaceAll()`?
- [ ] STR-OPS-012. What is the difference between `replaceFirst()` and `replaceAll()`?
- [ ] STR-OPS-013. Why can regex replacement strings require escaping?
- [ ] STR-OPS-014. What does `Matcher.quoteReplacement()` do?
- [ ] STR-OPS-015. What does `contains()` do?
- [ ] STR-OPS-016. What is the difference between `indexOf()` and `contains()`?
- [ ] STR-OPS-017. What does `startsWith()` do?
- [ ] STR-OPS-018. What does `endsWith()` do?
- [ ] STR-OPS-019. What does `matches()` do?
- [ ] STR-OPS-020. Why does `matches()` try to match the whole string?
- [ ] STR-OPS-021. How can regex cause performance problems?
- [ ] STR-OPS-022. What is catastrophic backtracking?
- [ ] STR-OPS-023. How would you avoid regex denial-of-service risks?
- [ ] STR-OPS-024. How would you parse a large text file without creating too many intermediate strings?

## 10. Core String API and modern methods

- [ ] STR-API-001. What is the difference between `isEmpty()` and `isBlank()`?
- [ ] STR-API-002. What is the difference between `trim()` and `strip()`?
- [ ] STR-API-003. What are `stripLeading()` and `stripTrailing()`?
- [ ] STR-API-004. What does `lines()` do?
- [ ] STR-API-005. What does `repeat()` do?
- [ ] STR-API-006. What does `indent()` do?
- [ ] STR-API-007. What does `stripIndent()` do?
- [ ] STR-API-008. What does `translateEscapes()` do?
- [ ] STR-API-009. What is `String.formatted()`?
- [ ] STR-API-010. What are text blocks in Java?
- [ ] STR-API-011. When are text blocks useful in Java backend code?
- [ ] STR-API-012. What are the traps of using text blocks for SQL or JSON?
- [ ] STR-API-013. What does `chars()` return?
- [ ] STR-API-014. What does `codePoints()` return?
- [ ] STR-API-015. When should you prefer `codePoints()` over `chars()`?
- [ ] STR-API-016. What does `toCharArray()` do?
- [ ] STR-API-017. What does `getBytes(Charset)` do?
- [ ] STR-API-018. What are `String.CASE_INSENSITIVE_ORDER` and its limitations?
- [ ] STR-API-019. What is the difference between `contentEquals()` and `equals()`?
- [ ] STR-API-020. When is `regionMatches()` useful?
- [ ] STR-API-021. How does `String.valueOf(null)` behave?
- [ ] STR-API-022. What is the difference between `String.valueOf(obj)` and `Objects.toString(obj)`?
- [ ] STR-API-023. What is `StringBuilder::append` doing when appending `null`?
- [ ] STR-API-024. Which modern String methods were added after Java 8 and why are they useful?

## 11. String, CharSequence and related interfaces/classes

- [ ] STR-TYPE-001. Why does `String` implement `CharSequence`?
- [ ] STR-TYPE-002. Why does `String` implement `Comparable<String>`?
- [ ] STR-TYPE-003. What is `CharSequence`?
- [ ] STR-TYPE-004. What classes commonly implement `CharSequence`?
- [ ] STR-TYPE-005. When should an API accept `CharSequence` instead of `String`?
- [ ] STR-TYPE-006. What are the risks of accepting `CharSequence`?
- [ ] STR-TYPE-007. Is every `CharSequence` immutable?
- [ ] STR-TYPE-008. Why can `CharSequence` be a source of mutability bugs?
- [ ] STR-TYPE-009. What is `StringBuilder` as a `CharSequence`?
- [ ] STR-TYPE-010. What is `StringBuffer` as a `CharSequence`?
- [ ] STR-TYPE-011. What is the difference between `CharSequence.length()` and Unicode code point count?
- [ ] STR-TYPE-012. How should you defensively copy a `CharSequence` input?

## 12. String and collections, maps and domain modeling

- [ ] STR-COLL-001. Why is `String` a good key for `HashMap`?
- [ ] STR-COLL-002. What are the risks of using raw user input strings as keys?
- [ ] STR-COLL-003. Why should string keys often be normalized before storing in a map?
- [ ] STR-COLL-004. How can case sensitivity affect string keys?
- [ ] STR-COLL-005. How can Unicode normalization affect string keys?
- [ ] STR-COLL-006. How can leading/trailing spaces affect string keys?
- [ ] STR-COLL-007. When would you use an enum instead of a string?
- [ ] STR-COLL-008. Why is a stringly typed domain model dangerous?
- [ ] STR-COLL-009. What is the difference between using `String` and a value object for identifiers?
- [ ] STR-COLL-010. How would you model email, phone number or currency code instead of plain strings?
- [ ] STR-COLL-011. How can a large number of duplicate string keys affect memory?
- [ ] STR-COLL-012. How can interning or canonicalization help map keys?
- [ ] STR-COLL-013. What are the risks of canonicalizing unbounded user input?
- [ ] STR-COLL-014. How would you design cache keys safely with strings?

## 13. String in backend APIs, HTTP, JSON and databases

- [ ] STR-BE-001. How should Java services handle string encodings in HTTP requests and responses?
- [ ] STR-BE-002. Why should API boundaries explicitly define charset and media type?
- [ ] STR-BE-003. How can string encoding bugs appear in REST APIs?
- [ ] STR-BE-004. What is the role of `Content-Type` charset for textual payloads?
- [ ] STR-BE-005. What is the default charset trap when reading files or streams?
- [ ] STR-BE-006. How should you convert request bytes to text safely?
- [ ] STR-BE-007. How should you write text files safely?
- [ ] STR-BE-008. How do JSON libraries handle Java strings?
- [ ] STR-BE-009. What can go wrong when building JSON manually with string concatenation?
- [ ] STR-BE-010. What can go wrong when building SQL manually with string concatenation?
- [ ] STR-BE-011. Why are prepared statements better than string-concatenated SQL?
- [ ] STR-BE-012. How should string length validation be designed for API inputs?
- [ ] STR-BE-013. What is the difference between byte length, char length, code point count and user-perceived length?
- [ ] STR-BE-014. Why can database column length checks differ from Java `String.length()`?
- [ ] STR-BE-015. How can string truncation cause production bugs?
- [ ] STR-BE-016. How should you handle large strings in request bodies?
- [ ] STR-BE-017. When should you stream textual data instead of loading it into one `String`?
- [ ] STR-BE-018. How can string-heavy logging create memory and cost problems?

## 14. String performance and allocation patterns

- [ ] STR-PERF-001. Why can excessive string allocation hurt performance?
- [ ] STR-PERF-002. How can temporary strings increase GC pressure?
- [ ] STR-PERF-003. Why can repeated string concatenation in a loop be inefficient?
- [ ] STR-PERF-004. How can `StringBuilder` reduce temporary allocations?
- [ ] STR-PERF-005. How should you choose initial `StringBuilder` capacity?
- [ ] STR-PERF-006. When is manual optimization worse than simple readable string code?
- [ ] STR-PERF-007. How does escape analysis affect some string-related allocations?
- [ ] 🧠 STR-PERF-008. How can JIT optimizations make microbenchmarks misleading?
- [ ] STR-PERF-009. How would you benchmark string operations correctly?
- [ ] 🧠 STR-PERF-010. Why should you use JMH for string microbenchmarks?
- [ ] STR-PERF-011. How can regex-heavy string processing become a bottleneck?
- [ ] STR-PERF-012. How can `String.format()` become a performance problem?
- [ ] STR-PERF-013. How can logging string concatenation become a performance problem?
- [ ] STR-PERF-014. Why should you prefer parameterized logging over eager string concatenation?
- [ ] STR-PERF-015. How can large strings affect latency?
- [ ] STR-PERF-016. How can string parsing affect CPU usage?
- [ ] STR-PERF-017. How would you profile a service that spends too much CPU in string processing?
- [ ] STR-PERF-018. How would you reduce allocations in a CSV import pipeline?
- [ ] STR-PERF-019. How would you process a huge text file without loading it fully into memory?
- [ ] STR-PERF-020. What are the trade-offs of caching computed strings?

## 15. GC, String deduplication and memory leaks

- [ ] 🧠 STR-GC-001. What is String deduplication in G1 GC?
- [ ] 🧠 STR-GC-002. What problem does String deduplication try to solve?
- [ ] 🧠 STR-GC-003. Does String deduplication deduplicate `String` objects or their backing storage?
- [ ] 🧠 STR-GC-004. How is String deduplication different from `intern()`?
- [ ] 🧠 STR-GC-005. How do you enable String deduplication?
- [ ] 🧠 STR-GC-006. What are the costs of String deduplication?
- [ ] 🧠 STR-GC-007. When can String deduplication help a backend service?
- [ ] 🧠 STR-GC-008. When can String deduplication be useless or harmful?
- [ ] STR-GC-009. How can strings cause memory leaks in Java?
- [ ] STR-GC-010. How can caches of strings create memory leaks?
- [ ] STR-GC-011. How can interning user input create memory leaks?
- [ ] STR-GC-012. How can old substring behavior cause memory leaks?
- [ ] 🧠 STR-GC-013. How can classloader leaks keep strings alive?
- [ ] STR-GC-014. How can log buffers or queues keep strings alive?
- [ ] STR-GC-015. How can ThreadLocal values keep strings alive?
- [ ] STR-GC-016. How would you detect duplicate strings in a heap dump?
- [ ] 🧠 STR-GC-017. How would you decide whether to enable string deduplication in production?
- [ ] STR-GC-018. Which metrics would you inspect for string-related memory pressure?

## 16. Diagnostics, heap dumps and memory measurement

- [ ] 🧠 STR-DIAG-001. How can you find how much memory strings use in a running application?
- [ ] 🧠 STR-DIAG-002. How can you inspect strings in a heap dump?
- [ ] 🧠 STR-DIAG-003. What tools help analyze string-heavy memory usage?
- [ ] STR-DIAG-004. How can Java Flight Recorder help diagnose string allocation pressure?
- [ ] STR-DIAG-005. How can allocation profiling reveal string hot spots?
- [ ] STR-DIAG-006. What is a dominator tree and how can it help with string leaks?
- [ ] STR-DIAG-007. What does retained size mean for strings?
- [ ] STR-DIAG-008. How do you distinguish many duplicate strings from one huge string?
- [ ] STR-DIAG-009. How do you find who keeps a string alive?
- [ ] STR-DIAG-010. How do GC logs help diagnose string-related memory pressure?
- [ ] STR-DIAG-011. How can logging accidentally create too many temporary strings?
- [ ] STR-DIAG-012. How would you investigate `OutOfMemoryError` caused by strings?
- [ ] STR-DIAG-013. How would you create a heap dump on `OutOfMemoryError`?
- [ ] STR-DIAG-014. How would you safely handle heap dumps that may contain sensitive strings?

## 17. Internationalization, locale and text correctness

- [ ] STR-I18N-001. Why can `toLowerCase()` and `toUpperCase()` be locale-sensitive?
- [ ] STR-I18N-002. Why is Turkish locale a classic string bug example?
- [ ] STR-I18N-003. When should you use `Locale.ROOT`?
- [ ] STR-I18N-004. When should you use a user-specific `Locale`?
- [ ] STR-I18N-005. Why should case normalization for identifiers usually use `Locale.ROOT`?
- [ ] STR-I18N-006. Why is human-language sorting different from `String.compareTo()`?
- [ ] STR-I18N-007. When should you use `Collator`?
- [ ] STR-I18N-008. How can Unicode normalization affect search?
- [ ] STR-I18N-009. How can accents and diacritics affect string comparison?
- [ ] STR-I18N-010. How would you design case-insensitive search correctly?
- [ ] STR-I18N-011. How would you design email normalization carefully?
- [ ] STR-I18N-012. Why is URL normalization security-sensitive?
- [ ] STR-I18N-013. How can filename normalization differ across operating systems?
- [ ] STR-I18N-014. Why is string length validation hard for emoji and composed characters?

## 18. String, concurrency and thread-safety

- [ ] STR-CONC-001. Is `String` thread-safe?
- [ ] STR-CONC-002. Why is immutable `String` safely shareable between threads?
- [ ] STR-CONC-003. Is `StringBuilder` thread-safe?
- [ ] STR-CONC-004. Is `StringBuffer` thread-safe?
- [ ] STR-CONC-005. Does synchronized `StringBuffer` make a whole workflow thread-safe?
- [ ] STR-CONC-006. Why should you avoid synchronizing on string literals?
- [ ] STR-CONC-007. Why can interned strings cause accidental lock sharing?
- [ ] STR-CONC-008. How can shared mutable `CharSequence` implementations cause concurrency bugs?
- [ ] STR-CONC-009. How can logging context strings leak across threads through `ThreadLocal`?
- [ ] STR-CONC-010. How would you safely build strings in concurrent code?

## 19. Security, validation and correctness

- [ ] STR-SEC-001. Why is string validation security-sensitive?
- [ ] STR-SEC-002. Why should you not build SQL queries by concatenating strings?
- [ ] STR-SEC-003. Why should you not build shell commands by concatenating untrusted strings?
- [ ] STR-SEC-004. Why should you not build HTML/JSON/XML manually without escaping?
- [ ] STR-SEC-005. What is output encoding and how does it relate to strings?
- [ ] STR-SEC-006. What is canonicalization before validation?
- [ ] STR-SEC-007. Why can validating before decoding/canonicalization be dangerous?
- [ ] STR-SEC-008. How can path traversal attacks involve strings?
- [ ] STR-SEC-009. How can log injection involve strings?
- [ ] STR-SEC-010. How can CRLF injection involve strings?
- [ ] STR-SEC-011. How can Unicode confusables affect security?
- [ ] STR-SEC-012. How can secrets leak through string interning, heap dumps or logs?
- [ ] STR-SEC-013. Why should token values be redacted in logs?
- [ ] STR-SEC-014. How would you design safe string validation for an API field?
- [ ] STR-SEC-015. How would you avoid leaking PII stored as strings?
- [ ] STR-SEC-016. Why is `String` not ideal for long-lived cryptographic secrets?

## 20. Testing, debugging and senior scenario questions

- [ ] STR-SCENARIO-001. How would you test string encoding behavior in an API?
- [ ] STR-SCENARIO-002. How would you test Unicode edge cases such as emoji and composed characters?
- [ ] STR-SCENARIO-003. How would you test case-insensitive matching correctly?
- [ ] STR-SCENARIO-004. How would you test regex-based parsing safely?
- [ ] STR-SCENARIO-005. A service returns corrupted characters for non-English users. How would you debug it?
- [ ] STR-SCENARIO-006. A service has high allocation rate and many short-lived strings. How would you debug it?
- [ ] STR-SCENARIO-007. A heap dump shows millions of duplicate strings. What would you check?
- [ ] STR-SCENARIO-008. A login check fails for visually identical usernames. What could be wrong?
- [ ] STR-SCENARIO-009. An endpoint accepts emoji but validation rejects valid input. What could be wrong?
- [ ] STR-SCENARIO-010. A log statement causes high CPU and allocation under debug-disabled logging. What could be wrong?
- [ ] STR-SCENARIO-011. A batch job builds a huge string and crashes with `OutOfMemoryError`. How would you redesign it?
- [ ] STR-SCENARIO-012. A cache keyed by raw request strings grows without bound. How would you fix it?
- [ ] STR-SCENARIO-013. A regex used on user input causes latency spikes. How would you investigate it?
- [ ] STR-SCENARIO-014. A production service interns request strings and memory keeps growing. What is the problem?
- [ ] STR-SCENARIO-015. A service compares strings with `==` and behaves inconsistently. How would you explain the bug?
- [ ] STR-SCENARIO-016. A database uniqueness check differs from Java string equality. What could be the cause?
- [ ] STR-SCENARIO-017. A service migrated from Java 8 to Java 17/21/25. What String-related behavior would you review?
- [ ] STR-SCENARIO-018. How would you decide whether to enable `-XX:+UseStringDeduplication`?
- [ ] STR-SCENARIO-019. How would you design a string-heavy import pipeline?
- [ ] STR-SCENARIO-020. How would you reduce memory usage in a service that processes large JSON payloads?
- [ ] STR-SCENARIO-021. How would you safely parse and validate user-controlled identifiers?
- [ ] STR-SCENARIO-022. How would you review code that uses `String` for every domain concept?
- [ ] STR-SCENARIO-023. How would you explain to a teammate why `String.length()` is not the number of characters a user sees?
- [ ] STR-SCENARIO-024. How would you investigate slow search caused by string comparison or normalization?
- [ ] STR-SCENARIO-025. How would you design logging to avoid excessive string allocation and PII leaks?

---

## Useful resources

- Java `String` API: https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/String.html
- Java `StringBuilder` API: https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/StringBuilder.html
- JLS string literals: https://docs.oracle.com/javase/specs/jls/se25/html/jls-3.html#jls-3.10.5
- JLS constant expressions: https://docs.oracle.com/javase/specs/jls/se25/html/jls-15.html#jls-15.29
- JEP 254, Compact Strings: https://openjdk.org/jeps/254
- JEP 192, String Deduplication in G1: https://openjdk.org/jeps/192
- Unicode Standard: https://www.unicode.org/versions/latest/

---

## Counts

- Total questions: 369
- Priority 1 questions: 340
- Priority 2 questions: 29
