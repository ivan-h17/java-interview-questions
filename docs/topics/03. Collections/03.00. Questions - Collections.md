# Collections

## 1. Collections Framework fundamentals and selection

- [ ] COL-FUND-001. What is the Java Collections Framework?
- [ ] COL-FUND-002. What are the main interfaces in the Java Collections Framework?
- [ ] COL-FUND-003. What is the difference between `Collection`, `Collections` and `Collector`?
- [ ] COL-FUND-004. Why does `Map` not extend `Collection`?
- [ ] COL-FUND-005. How are `Iterable`, `Collection`, `List`, `Set`, `Queue` and `Map` related?
- [ ] 🧠 COL-FUND-006. What are optional operations in collection interfaces, and why can they throw `UnsupportedOperationException`?
- [ ] COL-FUND-007. What is the difference between a collection interface and a collection implementation?
- [ ] 🧠 COL-FUND-008. Why are `Vector`, `Stack`, `Hashtable` and `Enumeration` considered legacy?
- [ ] COL-FUND-009. What is the difference between ordered, sorted and encounter-ordered collections?
- [ ] 🧠 COL-FUND-010. What are `SequencedCollection`, `SequencedSet` and `SequencedMap`?
- [ ] COL-FUND-011. How do collection implementations define equality and `hashCode`?
- [ ] COL-FUND-012. What does it mean that most collection implementations are not synchronized by default?
- [ ] COL-FUND-013. How would you choose the right collection for a use case?

## 2. Lists, arrays and deque-backed stacks

- [ ] COL-LIST-001. What is a `List`?
- [ ] COL-LIST-002. What is the difference between `List`, `ArrayList` and `LinkedList`?
- [ ] COL-LIST-003. How does `ArrayList` work under the hood?
- [ ] 🧠 COL-LIST-004. How does `LinkedList` work under the hood?
- [ ] COL-LIST-005. When should you use `ArrayList`, and when should you avoid it?
- [ ] COL-LIST-006. Why is `LinkedList` often a bad default choice in modern Java applications?
- [ ] COL-LIST-007. What are the time complexity trade-offs of `ArrayList` and `LinkedList`?
- [ ] 🧠 COL-LIST-008. How do `ArrayList` capacity, growth, pre-sizing and trimming work?
- [ ] COL-LIST-009. What is the difference between `Arrays.asList`, `List.of`, `List.copyOf` and `new ArrayList<>()`?
- [ ] COL-LIST-010. Why are `Vector` and `Stack` poor defaults, and what should be used instead?
- [ ] COL-LIST-011. When should you use `ArrayDeque` for stack-like or queue-like behavior?
- [ ] COL-LIST-012. What are the traps of `List.subList()`?
- [ ] 🧠 COL-LIST-013. How do memory locality and CPU cache behavior affect `ArrayList` and `LinkedList` performance?
- [ ] COL-LIST-014. What are common production mistakes with large `List`s?

## 3. Set implementations and uniqueness

- [ ] COL-SET-001. What is a `Set`?
- [ ] COL-SET-002. What is the difference between `HashSet`, `LinkedHashSet` and `TreeSet`?
- [ ] COL-SET-003. How does `HashSet` work internally at a high level?
- [ ] COL-SET-004. Why does `HashSet` rely on `equals()` and `hashCode()`?
- [ ] COL-SET-005. What is `LinkedHashSet`, and when would you use it?
- [ ] 🧠 COL-SET-006. What are `SortedSet`, `NavigableSet` and `TreeSet`?
- [ ] 🧠 COL-SET-007. How does `TreeSet` use `Comparable` or `Comparator`?
- [ ] COL-SET-008. What happens if a `TreeSet` comparator is inconsistent with `equals()`?
- [ ] 🧠 COL-SET-009. What is `EnumSet`, and why is it efficient?
- [ ] 🧠 COL-SET-010. What is `CopyOnWriteArraySet`, and when is it useful?
- [ ] COL-SET-011. What is the difference between Java `Set` uniqueness and database uniqueness?
- [ ] COL-SET-012. How would you remove duplicates while preserving insertion order?
- [ ] COL-SET-013. What are common mistakes when storing mutable objects in a `Set`?
- [ ] COL-SET-014. How would you choose between `HashSet`, `LinkedHashSet` and `TreeSet`?

## 4. Map implementations and key-value modeling

- [ ] COL-MAP-001. What is a `Map`?
- [ ] COL-MAP-002. What is the difference between `HashMap`, `LinkedHashMap` and `TreeMap`?
- [ ] 🧠 COL-MAP-003. When should you use `TreeMap` or `NavigableMap`?
- [ ] COL-MAP-004. What is `LinkedHashMap`, and how can it implement simple LRU behavior?
- [ ] 🧠 COL-MAP-005. What is `IdentityHashMap`, and when is identity-based lookup useful?
- [ ] 🧠 COL-MAP-006. What is `EnumMap`, and why is it efficient?
- [ ] 🧠 COL-MAP-007. What is `WeakHashMap`, and why is it not a general-purpose cache?
- [ ] 🧠 COL-MAP-008. What is the difference between `keySet`, `values` and `entrySet` views?
- [ ] COL-MAP-009. What are `computeIfAbsent`, `merge` and `putIfAbsent` used for?
- [ ] COL-MAP-010. What are common pitfalls when modifying a `Map` while iterating over it?
- [ ] COL-MAP-011. When should a `Map` be replaced by a proper domain object?
- [ ] COL-MAP-012. How can incorrect `equals()` or `hashCode()` break map behavior?
- [ ] COL-MAP-013. How would you choose a `Map` implementation for a backend use case?

## 5. Queue, Deque and producer-consumer structures

- [ ] COL-QUEUE-001. What is a `Queue`?
- [ ] 🧠 COL-QUEUE-002. What is the difference between `add`, `offer`, `remove`, `poll`, `element` and `peek`?
- [ ] COL-QUEUE-003. What is a `Deque`, and how is it different from `Queue`?
- [ ] COL-QUEUE-004. When would you use `ArrayDeque`?
- [ ] COL-QUEUE-005. What is `PriorityQueue`, and what are its ordering caveats?
- [ ] 🧠 COL-QUEUE-006. How does `PriorityQueue` work under the hood?
- [ ] COL-QUEUE-007. What is `BlockingQueue`, and when is it useful?
- [ ] 🧠 COL-QUEUE-008. What is the difference between `ArrayBlockingQueue` and `LinkedBlockingQueue`?
- [ ] 🧠 COL-QUEUE-009. What are `DelayQueue`, `SynchronousQueue` and `PriorityBlockingQueue` used for?
- [ ] COL-QUEUE-010. What is the difference between queue-based buffering and backpressure?
- [ ] COL-QUEUE-011. How would you choose a queue for a producer-consumer problem?
- [ ] COL-QUEUE-012. What can go wrong if an application uses an unbounded queue?

## 6. Iteration, structural modification and sorting

- [ ] COL-ITER-001. What is `Iterator`?
- [ ] 🧠 COL-ITER-002. How does the enhanced `for` loop use `Iterable`?
- [ ] COL-ITER-003. What is structural modification?
- [ ] COL-ITER-004. What is fail-fast behavior, and is it guaranteed?
- [ ] COL-ITER-005. What is `ConcurrentModificationException`, and does it always mean multiple threads are involved?
- [ ] COL-ITER-006. How do you correctly remove elements during iteration?
- [ ] COL-ITER-007. What is the difference between `collection.remove`, `iterator.remove` and `removeIf`?
- [ ] COL-ITER-008. How do concurrent collections behave during iteration?
- [ ] 🧠 COL-ITER-009. What are weakly consistent iterators and snapshot iterators?
- [ ] COL-ITER-010. What can go wrong if you expose a mutable collection from an object?
- [ ] COL-ITER-011. How would you safely iterate and update a collection in a multi-threaded service?
- [ ] COL-ORDER-001. What is the difference between `Comparable` and `Comparator`?
- [ ] COL-ORDER-002. What does it mean for a comparator to be consistent with `equals()`?
- [ ] COL-ORDER-003. What can happen if a comparator violates transitivity?
- [ ] 🧠 COL-ORDER-004. How do `List.sort` and `Collections.sort` work?
- [ ] COL-ORDER-005. How do you handle nulls and multiple fields in comparators?
- [ ] COL-ORDER-006. Why can `TreeSet` or `TreeMap` keep only one of two unequal objects if comparison returns zero?
- [ ] 🧠 COL-ORDER-007. What is stable sorting, and when does it matter?
- [ ] COL-ORDER-008. How would you sort without mutating the original collection?
- [ ] COL-ORDER-009. What are common comparator bugs in Java backend code?

## 7. Mutability, wrappers and API design

- [ ] COL-MUT-001. What is the difference between mutable, unmodifiable and immutable collections?
- [ ] COL-MUT-002. What does `Collections.unmodifiableList` do, and why is it not the same as an immutable copy?
- [ ] COL-MUT-003. What is the difference between `Collections.unmodifiableList`, `List.copyOf`, `List.of` and `Arrays.asList`?
- [ ] COL-MUT-004. How can you protect a collection field from external modification?
- [ ] COL-MUT-005. What is defensive copying?
- [ ] COL-MUT-006. When do you need shallow copy vs deep copy for collections?
- [ ] COL-MUT-007. How can mutable elements break an immutable collection design?
- [ ] COL-MUT-008. How should DTOs and records handle collection fields?
- [ ] 🧠 COL-MUT-009. What are synchronized collection wrappers?
- [ ] 🧠 COL-MUT-010. What are checked collection wrappers, and when are they useful?
- [ ] COL-API-001. What collection type should a method return: `List`, `Set`, `Collection`, `Iterable` or `Stream`?
- [ ] COL-API-002. When should method parameters use `Collection` instead of `List`?
- [ ] COL-API-003. How should APIs express ordering, uniqueness and mutability guarantees?
- [ ] COL-API-004. Should APIs return `null` or an empty collection?
- [ ] COL-API-005. How would you design an API that returns a large number of elements?
- [ ] COL-API-006. How would you represent a cache of values by key?
- [ ] COL-API-007. How would you choose between `List` and `Set` for DTO fields?
- [ ] COL-API-008. How would you safely expose collection data from a Spring service?
- [ ] COL-API-009. What are the most common collection anti-patterns in API design?

## 8. Concurrent collections and synchronization

- [ ] COL-CONC-001. What is the difference between synchronized collections and concurrent collections?
- [ ] 🧠 COL-CONC-002. What is the correct way to iterate over a synchronized collection?
- [ ] COL-CONC-003. What is `ConcurrentHashMap`?
- [ ] COL-CONC-004. How does `ConcurrentHashMap` provide thread-safety at a high level?
- [ ] COL-CONC-005. How is `ConcurrentHashMap` different from `Hashtable` and `Collections.synchronizedMap`?
- [ ] 🧠 COL-CONC-006. Why does `ConcurrentHashMap` not allow null keys or null values?
- [ ] COL-CONC-007. What are atomic compound operations in `ConcurrentHashMap`?
- [ ] COL-CONC-008. Why is get-then-put not always thread-safe even with `ConcurrentHashMap`?
- [ ] 🧠 COL-CONC-009. What is `CopyOnWriteArrayList`, and when should you use or avoid it?
- [ ] 🧠 COL-CONC-010. What are `ConcurrentSkipListMap` and `ConcurrentLinkedQueue`?
- [ ] COL-CONC-011. How is `BlockingQueue` used in producer-consumer designs?
- [ ] COL-CONC-012. Can a thread-safe collection make the whole class thread-safe?
- [ ] COL-CONC-013. How would you choose between locking, `ConcurrentHashMap` and immutable snapshots?
- [ ] COL-CONC-014. How can collection usage cause memory leaks in concurrent services?
- [ ] COL-CONC-015. What are common race conditions involving collections?

## 9. Performance, memory and Streams integration

- [ ] COL-PERF-001. What are the typical time complexities of common `List`, `Set` and `Map` operations?
- [ ] COL-PERF-002. Why is Big-O not enough to choose a collection?
- [ ] 🧠 COL-PERF-003. How do memory overhead and CPU cache locality affect collection performance?
- [ ] COL-PERF-004. How do boxing and unboxing affect collection memory usage?
- [ ] COL-PERF-005. What is the cost of storing millions of objects in standard collections?
- [ ] COL-PERF-006. How would you reduce memory usage of large collections?
- [ ] COL-PERF-007. How would you avoid loading an entire dataset into memory?
- [ ] COL-PERF-008. When would you prefer streaming or pagination over a collection?
- [ ] COL-PERF-009. How can collection choice affect GC pressure?
- [ ] COL-PERF-010. How can collection resizing cause latency spikes?
- [ ] 🧠 COL-PERF-011. How would you benchmark collection performance correctly?
- [ ] 🧠 COL-PERF-012. What can go wrong with microbenchmarks for collections?
- [ ] COL-PERF-013. What production metrics can reveal collection-related memory problems?
- [ ] COL-STREAM-001. How are collections related to Streams?
- [ ] COL-STREAM-002. When should you use a Stream instead of explicit iteration?
- [ ] COL-STREAM-003. What is the difference between `stream().toList()`, `Collectors.toList()` and collecting to a specific collection?
- [ ] COL-STREAM-004. What can go wrong with `Collectors.toMap`, and how do you handle duplicate keys?
- [ ] 🧠 COL-STREAM-005. How do you preserve ordering when collecting stream results?
- [ ] 🧠 COL-STREAM-006. How do parallel streams interact with collection implementations?
- [ ] COL-STREAM-007. Why is mutating shared collections inside streams dangerous?

## 10. Senior backend scenarios and review questions

- [ ] COL-SCENARIO-001. How would you debug `ConcurrentModificationException` in production?
- [ ] COL-SCENARIO-002. How would you debug memory growth caused by a collection?
- [ ] COL-SCENARIO-003. How would you handle duplicate entities from a database query?
- [ ] COL-SCENARIO-004. What collection-related issues commonly appear in Hibernate/JPA entities?
- [ ] COL-SCENARIO-005. What collection choices can cause latency spikes in request processing?
- [ ] COL-SCENARIO-006. How would you review code that uses collections heavily?
- [ ] COL-SCENARIO-007. A service uses a `List` for repeated lookups by ID. What would you check?
- [ ] COL-SCENARIO-008. A cache is implemented with a plain `HashMap`. What questions would you ask?
- [ ] COL-SCENARIO-009. A response endpoint returns thousands of items in memory. How would you redesign it?
- [ ] COL-SCENARIO-010. What are the most common collection anti-patterns in Java backend code?

## Counts

- Total questions: 150
- Priority 1 questions: 116
- Priority 2 questions: 34
