# Immutability

## 1. Fundamentals and immutability mental model

- [ ] IMM-FUND-001. What is an immutable object?
- [ ] IMM-FUND-002. What is the difference between immutable, read-only and unmodifiable?
- [ ] IMM-FUND-003. What is the difference between shallow, deep and transitive immutability?
- [ ] IMM-FUND-004. What benefits and trade-offs do immutable objects provide?
- [ ] IMM-FUND-005. When is a mutable design better than an immutable one?
- [ ] IMM-FUND-006. Why are immutable value objects easier to reason about than mutable entities?
- [ ] IMM-FUND-007. Can an object be logically immutable but physically mutable?
- [ ] IMM-FUND-008. What is representation exposure, and how can it break immutability?

## 2. Designing immutable classes in Java

- [ ] IMM-DESIGN-001. How do you create an immutable class in Java?
- [ ] IMM-DESIGN-002. Why should immutable classes usually use private final fields and no setters?
- [ ] IMM-DESIGN-003. Is it enough to make all fields final to make a class immutable?
- [ ] IMM-DESIGN-004. How should an immutable class handle mutable constructor arguments?
- [ ] IMM-DESIGN-005. How should getters expose mutable fields safely?
- [ ] IMM-DESIGN-006. How do you handle arrays, collections and nested mutable objects inside immutable classes?
- [ ] IMM-DESIGN-007. How do you preserve invariants and validation in an immutable class?
- [ ] IMM-DESIGN-008. What are withers and modified-copy methods in immutable models?
- [ ] IMM-DESIGN-009. Why should immutable classes avoid leaking `this` from constructors?
- [ ] IMM-DESIGN-010. Why are final classes and composition often safer than inheritance for immutable models?

## 3. `final`, safe publication and concurrency

- [ ] IMM-JMM-001. What does the `final` keyword mean for variables, fields, methods and classes?
- [ ] IMM-JMM-002. How do final-field semantics help safe publication?
- [ ] IMM-JMM-003. Can immutable objects be shared between threads without synchronization?
- [ ] IMM-JMM-004. What is safe publication, and when is it still needed with immutable objects?
- [ ] IMM-JMM-005. When are `volatile` or `AtomicReference` useful with immutable state?
- [ ] IMM-JMM-006. How can compare-and-set update immutable state safely?
- [ ] IMM-JMM-007. How can lazy cached values be implemented safely in immutable objects?
- [ ] IMM-JMM-008. Are `static final` fields always immutable?
- [ ] 🧠 IMM-JMM-009. Can reflection, `Unsafe` or deserialization break immutability guarantees?
- [ ] IMM-JMM-010. What is the difference between immutable data and a thread-safe service?

## 4. Defensive copying and immutable collections

- [ ] IMM-COPY-001. What is defensive copying, and when do you need it?
- [ ] IMM-COPY-002. What is the difference between shallow copy and deep copy?
- [ ] IMM-COPY-003. How do you defensively copy arrays?
- [ ] IMM-COPY-004. How do you defensively copy lists, sets and maps?
- [ ] IMM-COPY-005. How do you protect a collection that contains mutable elements?
- [ ] IMM-COPY-006. What is the difference between an unmodifiable view and an immutable copy?
- [ ] IMM-COPY-007. What are `List.of()`, `Set.of()`, `Map.of()` and `copyOf()` used for?
- [ ] IMM-COPY-008. What does `UnsupportedOperationException` mean for unmodifiable collections?
- [ ] IMM-COPY-009. When should you prefer copying over wrapping, and when is a live view acceptable?
- [ ] IMM-COPY-010. What performance costs can defensive copying and snapshots introduce?

## 5. Records, Java time API and value-based classes

- [ ] IMM-REC-001. What is a record, and how does it help create immutable models?
- [ ] IMM-REC-002. Are Java records deeply immutable?
- [ ] IMM-REC-003. How do you make defensive copies in a record constructor and accessor?
- [ ] IMM-REC-004. What traps appear when record components are arrays or collections?
- [ ] IMM-REC-005. Can records have behavior and validation logic?
- [ ] IMM-REC-006. When should you use a record instead of a classic immutable class?
- [ ] IMM-REC-007. Why are records usually poor JPA entities but useful DTOs and projections?
- [ ] IMM-REC-008. How do records interact with JSON serialization and API compatibility?
- [ ] IMM-REC-009. Why are `LocalDate`, `Instant` and other `java.time` types immutable?
- [ ] 🧠 IMM-REC-010. What are value-based classes, and why should they not be used for identity-sensitive operations?

## 6. Equality, `HashMap` keys and caching

- [ ] IMM-KEY-001. Why are immutable objects good candidates for map keys and cache keys?
- [ ] IMM-KEY-002. What happens if a key is mutated after insertion into a `HashMap`?
- [ ] IMM-KEY-003. What are the requirements for a reliable `HashMap` key?
- [ ] IMM-KEY-004. How do immutable value objects simplify `equals()` and `hashCode()` correctness?
- [ ] IMM-KEY-005. Why is `String` a common `HashMap` key?
- [ ] IMM-KEY-006. Can arrays or lists be reliable map keys?
- [ ] IMM-KEY-007. Should JPA entities be used as `HashMap` keys?
- [ ] IMM-KEY-008. How would you debug a failed lookup caused by a mutated key?

## 7. Framework integration and domain modeling

- [ ] IMM-FW-001. How do immutable classes work with Jackson JSON deserialization?
- [ ] IMM-FW-002. What should you consider when using immutable DTOs in Spring MVC?
- [ ] IMM-FW-003. How do mapping libraries such as MapStruct work with immutable targets?
- [ ] IMM-FW-004. Should JPA entities be immutable?
- [ ] IMM-FW-005. When are immutable JPA embeddables useful?
- [ ] IMM-FW-006. How does Hibernate dirty checking interact with immutable objects?
- [ ] IMM-FW-007. How can Spring Boot immutable `@ConfigurationProperties` be modeled and reloaded?
- [ ] IMM-DOMAIN-001. Why are value objects usually immutable in DDD?
- [ ] IMM-DOMAIN-002. How would you model `Money` as an immutable value object?
- [ ] IMM-DOMAIN-003. How do immutable command and event objects help backend systems?
- [ ] IMM-DOMAIN-004. How can immutable snapshots support audit, history and event sourcing?
- [ ] IMM-DOMAIN-005. How can overusing immutable DTOs lead to an anemic domain model?

## 8. Performance, security and testing

- [ ] IMM-PERF-001. How does immutability affect performance?
- [ ] IMM-PERF-002. How can immutable objects increase allocation rate and GC pressure?
- [ ] IMM-PERF-003. How can immutable snapshots reduce lock contention?
- [ ] 🧠 IMM-PERF-004. When are persistent data structures and structural sharing useful?
- [ ] IMM-PERF-005. When is object pooling useful or harmful for immutable objects?
- [ ] IMM-SEC-001. How can immutability improve security and prevent time-of-check to time-of-use bugs?
- [ ] IMM-SEC-002. Why are `String` objects problematic for passwords and secrets?
- [ ] IMM-SEC-003. What sensitive data should not appear in generated `toString()` methods?
- [ ] IMM-TEST-001. How would you test that a class is immutable?
- [ ] IMM-TEST-002. How can code review and static analysis catch mutability leaks?

## 9. Senior scenario-based immutability questions

- [ ] IMM-SCENARIO-001. Design an immutable `Money` class for a payment service.
- [ ] IMM-SCENARIO-002. Design an immutable `OrderSnapshot` that contains order items.
- [ ] IMM-SCENARIO-003. How would you refactor a mutable DTO used across multiple threads?
- [ ] IMM-SCENARIO-004. How would you design a cache key for a complex search request?
- [ ] IMM-SCENARIO-005. How would you protect a public API from caller-side mutation of request data?
- [ ] IMM-SCENARIO-006. How would you safely reload application configuration using immutable objects?
- [ ] IMM-SCENARIO-007. How would you design immutable events for a Kafka-based system?
- [ ] IMM-SCENARIO-008. How would you handle a nested collection of mutable objects in an immutable aggregate?
- [ ] IMM-SCENARIO-009. A code review shows `Collections.unmodifiableList(input)` in a constructor. What questions would you ask?
- [ ] IMM-SCENARIO-010. How would you use immutable objects to reduce locking in a read-heavy service?
- [ ] IMM-SCENARIO-011. How would you balance immutability and performance for large response objects?
- [ ] IMM-SCENARIO-012. A framework requires a no-arg constructor and setters. How would you keep your domain model immutable?

---

## Counts

- Total questions: 90
- Priority 1 questions: 87
- Priority 2 questions: 3
