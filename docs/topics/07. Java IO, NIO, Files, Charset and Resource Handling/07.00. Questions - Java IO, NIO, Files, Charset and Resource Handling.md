# Java I/O, NIO, Files, Charset and Resource Handling

## 1. I/O fundamentals and mental model

- [ ] JIO-FUND-001. What is the difference between byte-oriented I/O and character-oriented I/O?
- [ ] JIO-FUND-002. What is the difference between `InputStream`/`OutputStream` and `Reader`/`Writer`?
- [ ] JIO-FUND-003. What is blocking I/O, and what does it mean for a Java thread?
- [ ] JIO-FUND-004. How should backend code choose between reading all data at once and processing data incrementally?

## 2. Streams, readers, writers and buffering

- [ ] JIO-STREAM-001. How does the Java I/O decorator model work with wrappers like `BufferedInputStream`, `InputStreamReader` and `GZIPInputStream`?
- [ ] JIO-STREAM-002. Why should binary data not be processed through `Reader` or `Writer`?
- [ ] JIO-STREAM-003. What does buffering solve in Java I/O?
- [ ] JIO-STREAM-004. What is the difference between `flush()` and `close()`?
- [ ] JIO-STREAM-005. Why should code not assume that one `read()` call fills the entire buffer?
- [ ] JIO-STREAM-006. Why can `readAllBytes()`, `readAllLines()` or `readString()` be dangerous for large inputs?
- [ ] JIO-STREAM-007. How should backend code copy data between request bodies, files, object storage and HTTP responses?

## 3. Charsets, Unicode and text boundaries

- [ ] JIO-CHAR-001. What is a charset, and why does it matter when converting bytes to text?
- [ ] JIO-CHAR-002. What problems can relying on the platform default charset create, and why should backend code specify charsets explicitly?
- [ ] JIO-CHAR-003. Why is UTF-8 commonly used for APIs, configuration files, logs and data exchange?
- [ ] JIO-CHAR-004. What is mojibake, and how would you debug text decoded with the wrong charset?
- [ ] 🧠 JIO-CHAR-005. How can BOM markers, line endings or invisible Unicode characters break file parsing?

## 4. `File`, `Path` and the `Files` API

- [ ] JIO-FILES-001. What is the difference between `java.io.File` and `java.nio.file.Path`?
- [ ] JIO-FILES-002. What does the `Files` utility class provide?
- [ ] JIO-FILES-003. What is the difference between relative, absolute, normalized and real paths?
- [ ] JIO-FILES-004. Why can checking `Files.exists()` before opening a file still be unsafe?
- [ ] JIO-FILES-005. How do symbolic links affect file access, deletion and directory traversal?
- [ ] JIO-FILES-006. Why must streams returned by `Files.lines()`, `Files.list()`, `Files.walk()` and `Files.find()` usually be closed?
- [ ] JIO-FILES-007. How can backend code create a file safely without accidentally overwriting an existing file?
- [ ] JIO-FILES-008. How should a service write or replace a file when partial writes or crashes matter?
- [ ] 🧠 JIO-FILES-009. What are temporary files and directories used for, and what cleanup or permission risks do they create?

## 5. Classpath and application resource loading

- [ ] JIO-CP-001. What is the difference between a filesystem path and a classpath resource?
- [ ] JIO-CP-002. How do `Class.getResource()` and `ClassLoader.getResource()` differ?
- [ ] JIO-CP-003. Why can `new File("src/main/resources/...")` fail after a Java application is packaged?
- [ ] JIO-CP-004. How should code read configuration templates, certificates or seed data packaged inside a JAR?
- [ ] 🧠 JIO-CP-005. Why should code not assume that a classpath resource can always be converted to `Path` or `File`?

## 6. Resource lifecycle, ownership and leaks

- [ ] JIO-RES-001. What kinds of Java objects represent external resources that must be closed?
- [ ] JIO-RES-002. How does try-with-resources work?
- [ ] 🧠 JIO-RES-003. What are suppressed exceptions, and why do they matter in resource cleanup?
- [ ] JIO-RES-004. What is a file descriptor leak, and how can it appear in a Java service?
- [ ] JIO-RES-005. Should a method close a stream it received from its caller?
- [ ] JIO-RES-006. Why is relying on finalizers, cleaners or garbage collection unsafe for resource cleanup?

## 7. NIO channels and buffers

- [ ] JIO-NIO-001. What is Java NIO, and how does it differ from classic `java.io`?
- [ ] JIO-NIO-002. What is a `ByteBuffer`?
- [ ] 🧠 JIO-NIO-003. How do `position`, `limit` and `capacity` work in a buffer?
- [ ] 🧠 JIO-NIO-004. What do `flip()`, `clear()`, `rewind()` and `compact()` do?
- [ ] 🧠 JIO-NIO-005. What is the difference between heap buffers and direct buffers?

## 8. Blocking, non-blocking and asynchronous I/O

- [ ] JIO-ASYNC-001. How do blocking, non-blocking and asynchronous I/O differ?
- [ ] 🧠 JIO-ASYNC-002. What is a `Selector`, and what problem does it solve?
- [ ] JIO-ASYNC-003. How do virtual threads change the trade-off between blocking I/O and non-blocking I/O?
- [ ] JIO-ASYNC-004. When is simple blocking I/O a better engineering choice than NIO selectors?

## 9. Advanced file-system and NIO topics

- [ ] JIO-ADV-001. What are `FileChannel` and `SocketChannel` used for?
- [ ] 🧠 JIO-ADV-002. What are `transferTo()` and `transferFrom()`, and when can they be useful?
- [ ] 🧠 JIO-ADV-003. What is memory-mapped I/O, and what risks come with memory-mapped files?
- [ ] 🧠 JIO-ADV-004. What is `WatchService`, and what are its limitations?
- [ ] 🧠 JIO-ADV-005. How do file locks work in Java, and why are they easy to misuse?

## 10. Backend production traps and scenarios

- [ ] JIO-PROD-001. How can file uploads or downloads accidentally consume too much memory?
- [ ] JIO-PROD-002. What is path traversal, and how can backend code avoid it?
- [ ] JIO-PROD-003. What is Zip Slip, and why is archive extraction security-sensitive?
- [ ] JIO-PROD-004. A Java service fails with `Too many open files`. How would you investigate it?
- [ ] JIO-PROD-005. A CSV import causes a memory spike. What I/O design problems would you check?
- [ ] JIO-PROD-006. A service cannot find a resource file after deployment, although it worked in the IDE. What would you check?
- [ ] JIO-PROD-007. A service writes corrupted or half-written export files during restarts. What design problems would you check?

## Counts

- Total questions: 57
- Priority 1 questions: 45
- Priority 2 questions: 12
