# Linux and Networking Basics

## 1. Linux process and service runtime basics

- [ ] LNX-PROC-001. What is a Linux process, and how is it different from a thread?
- [ ] LNX-PROC-002. What are PID, PPID and process hierarchy?
- [ ] LNX-PROC-003. What happens when a process exits, and what is an exit code?
- [ ] LNX-PROC-004. What are zombie and orphan processes?
- [ ] LNX-PROC-005. What are environment variables, and how do backend services use them?
- [ ] LNX-PROC-006. What are standard input, standard output and standard error?
- [ ] LNX-PROC-007. What are Linux signals, and why does `SIGTERM` matter for Java services?
- [ ] LNX-PROC-008. What is the difference between `SIGTERM`, `SIGKILL`, `SIGINT` and `SIGHUP`?
- [ ] LNX-PROC-009. How should a Java service handle graceful shutdown on Linux?
- [ ] LNX-PROC-010. What are file descriptors, and why are sockets also file descriptors?
- [ ] LNX-PROC-011. What is `ulimit`, and which limits matter for backend services?
- [ ] 🧠 LNX-PROC-012. How do Linux namespaces and cgroups affect a service running in a container?

## 2. Filesystem, permissions and service artifacts

- [ ] LNX-FS-001. What is the difference between an absolute path and a relative path?
- [ ] LNX-FS-002. What are common Linux directories such as `/etc`, `/var`, `/tmp`, `/opt` and `/usr` used for?
- [ ] LNX-FS-003. What is the difference between a regular file, directory, symbolic link and socket file?
- [ ] LNX-FS-004. What is an inode, and why can disk space look available while file creation still fails?
- [ ] LNX-FS-005. How do Linux file permissions work for owner, group and others?
- [ ] LNX-FS-006. Why does execute permission on a directory matter?
- [ ] LNX-FS-007. What are `chmod`, `chown` and `umask` used for?
- [ ] LNX-FS-008. What is the difference between a hard link and a symbolic link?
- [ ] LNX-FS-009. How can log files, temp files and uploaded files fill a production filesystem?
- [ ] 🧠 LNX-FS-010. What are mount points, tmpfs and bind mounts at a high level?

## 3. Shell, logs and local diagnostics

- [ ] LNX-CMD-001. How do `ps`, `top` and `htop` help during production debugging?
- [ ] LNX-CMD-002. How do you find the process that owns a specific PID or port?
- [ ] LNX-CMD-003. How do `kill`, `pkill` and `kill -9` differ?
- [ ] LNX-CMD-004. How do `tail`, `less`, `grep` and `journalctl` help investigate service logs?
- [ ] LNX-CMD-005. How do `df` and `du` differ when debugging disk usage?
- [ ] LNX-CMD-006. How do `free`, `vmstat`, `iostat` or similar tools help identify resource pressure?
- [ ] LNX-CMD-007. How do you inspect environment variables and command-line arguments of a running process?
- [ ] LNX-CMD-008. How can `lsof` help debug open files, sockets and deleted-but-still-open files?
- [ ] 🧠 LNX-CMD-009. When is `strace` useful for debugging a stuck or failing service?
- [ ] 🧠 LNX-CMD-010. How can shell pipelines help during incident investigation, and what are their risks?

## 4. Resource limits, CPU, memory and disk pressure

- [ ] LNX-RES-001. What is load average, and how is it different from CPU usage?
- [ ] LNX-RES-002. How do you distinguish high CPU usage from high I/O wait?
- [ ] LNX-RES-003. What is swap, and why can heavy swapping hurt Java service latency?
- [ ] LNX-RES-004. What is the Linux OOM killer, and how can it affect a JVM process?
- [ ] LNX-RES-005. How can open file descriptor exhaustion affect a Java service?
- [ ] LNX-RES-006. How can process or thread limits affect a backend service?
- [ ] LNX-RES-007. What can happen when disk space is full on a host running a Java service?
- [ ] LNX-RES-008. What can happen when inode count is exhausted?
- [ ] LNX-RES-009. Why can a deleted log file still consume disk space?
- [ ] LNX-RES-010. How do Linux resource limits differ on a host and inside a container?
- [ ] 🧠 LNX-RES-011. How do CPU quotas and throttling show up in Java service latency?
- [ ] 🧠 LNX-RES-012. How can clock or time synchronization problems affect distributed backend services?

## 5. TCP/IP and socket fundamentals

- [ ] NET-FUND-001. What is the TCP/IP model, and how is it useful for troubleshooting?
- [ ] NET-FUND-002. What is an IP address, and how do IPv4 and IPv6 differ at a high level?
- [ ] NET-FUND-003. What is a subnet, and why should backend engineers understand it at a basic level?
- [ ] NET-FUND-004. What is a port, and how does it relate to a socket?
- [ ] NET-FUND-005. What uniquely identifies a TCP connection?
- [ ] NET-FUND-006. What is the difference between TCP and UDP?
- [ ] NET-FUND-007. What happens during the TCP three-way handshake?
- [ ] NET-FUND-008. What happens during TCP connection close?
- [ ] NET-FUND-009. What is the difference between `LISTEN`, `ESTABLISHED`, `TIME_WAIT` and `CLOSE_WAIT` states?
- [ ] NET-FUND-010. What is NAT, and why does it matter for outbound backend calls?
- [ ] NET-FUND-011. What is ICMP, and why is `ping` useful but incomplete?
- [ ] NET-FUND-012. What is MTU, and how can MTU problems affect service connectivity?
- [ ] 🧠 NET-FUND-013. What are TCP retransmissions, flow control and congestion control at a high level?
- [ ] 🧠 NET-FUND-014. What are SYN backlog and accept backlog, and when can they matter?

## 6. DNS and Linux name resolution

- [ ] NET-DNS-001. What is DNS, and how can DNS failures affect backend services?
- [ ] NET-DNS-002. What are A, AAAA, CNAME, MX, TXT and SRV records used for?
- [ ] NET-DNS-003. What is DNS TTL, and how does it affect failover?
- [ ] NET-DNS-004. What is the difference between recursive and authoritative DNS servers?
- [ ] NET-DNS-005. What is the difference between `dig`, `nslookup` and `getent hosts`?
- [ ] NET-DNS-006. How do `/etc/resolv.conf` and `/etc/nsswitch.conf` affect name resolution?
- [ ] NET-DNS-007. How can DNS caching create stale connections or delayed failover?
- [ ] NET-DNS-008. How can Java DNS caching affect production behavior?
- [ ] NET-DNS-009. What is split-horizon DNS, and when can it surprise backend services?
- [ ] 🧠 NET-DNS-010. How do DNS round-robin and service discovery differ from real load balancing?

## 7. TLS and certificate basics

- [ ] NET-TLS-001. What is TLS, and what does it protect?
- [ ] NET-TLS-002. What happens during a TLS handshake at a high level?
- [ ] NET-TLS-003. What is a certificate chain, and why must clients trust it?
- [ ] NET-TLS-004. What are hostname verification and Subject Alternative Names?
- [ ] NET-TLS-005. What is SNI, and why can it matter behind proxies or load balancers?
- [ ] NET-TLS-006. What is the difference between a Java truststore and keystore?
- [ ] NET-TLS-007. What is mutual TLS, and when is it useful?
- [ ] NET-TLS-008. What common certificate problems can break backend connectivity?
- [ ] 🧠 NET-TLS-009. How can TLS version or cipher mismatch cause environment-specific failures?

## 8. HTTP transport, proxies and load balancers

- [ ] NET-HTTP-001. What are HTTP keep-alive connections, and why do they matter?
- [ ] NET-HTTP-002. How do HTTP/1.1 and HTTP/2 differ from a connection-management perspective?
- [ ] NET-HTTP-003. What is the difference between a forward proxy and a reverse proxy?
- [ ] NET-HTTP-004. What is the difference between L4 and L7 load balancing?
- [ ] NET-HTTP-005. How do reverse proxies and load balancers affect backend services?
- [ ] NET-HTTP-006. What are `X-Forwarded-For`, `X-Forwarded-Proto` and `Forwarded` headers used for?
- [ ] NET-HTTP-007. How do proxy and load balancer idle timeouts cause intermittent errors?
- [ ] NET-HTTP-008. What is the difference between connection refused, connection reset and timeout?
- [ ] NET-HTTP-009. What do `502 Bad Gateway`, `503 Service Unavailable` and `504 Gateway Timeout` usually indicate?
- [ ] NET-HTTP-010. What are health checks, and how can bad health checks cause production incidents?
- [ ] NET-HTTP-011. How can sticky sessions create scaling or failover problems?
- [ ] 🧠 NET-HTTP-012. How do WebSocket or long-polling connections change proxy and timeout requirements?

## 9. Linux networking diagnostics

- [ ] NET-DIAG-001. How do you inspect open ports and network connections on Linux?
- [ ] NET-DIAG-002. How do `ss` and `netstat` help debug socket state?
- [ ] NET-DIAG-003. How do `curl -v` and response timing output help debug HTTP connectivity?
- [ ] NET-DIAG-004. How do `dig` and `getent hosts` help debug DNS problems?
- [ ] NET-DIAG-005. How do `nc` or `telnet` help test basic TCP connectivity?
- [ ] NET-DIAG-006. How do `ping`, `traceroute` and `mtr` differ?
- [ ] NET-DIAG-007. How do `ip addr`, `ip route` and `ip link` help debug local networking?
- [ ] NET-DIAG-008. How do you check whether a service is bound to `localhost`, `0.0.0.0` or a specific interface?
- [ ] NET-DIAG-009. How can `openssl s_client` help debug TLS connectivity?
- [ ] NET-DIAG-010. How can `tcpdump` or Wireshark help when application logs are not enough?
- [ ] 🧠 NET-DIAG-011. How do firewall rules with `iptables` or `nftables` affect connectivity?
- [ ] 🧠 NET-DIAG-012. How can `strace -e trace=network` help debug network syscalls?

## 10. Network performance, latency and capacity

- [ ] NET-PERF-001. What is the difference between latency, throughput and bandwidth?
- [ ] NET-PERF-002. How do packet loss and retransmissions affect application latency?
- [ ] NET-PERF-003. How can connection pooling improve latency and reduce resource usage?
- [ ] NET-PERF-004. How can creating a new connection per request hurt a Java service?
- [ ] NET-PERF-005. How can ephemeral port exhaustion happen?
- [ ] NET-PERF-006. How can NAT gateways, proxies or load balancers become bottlenecks?
- [ ] NET-PERF-007. How can DNS lookup latency affect request latency?
- [ ] NET-PERF-008. How can TLS handshake latency affect low-timeout clients?
- [ ] NET-PERF-009. What is head-of-line blocking at a high level?
- [ ] 🧠 NET-PERF-010. How can MTU black holes or fragmentation show up as intermittent connectivity problems?

## 11. Java backend interactions with Linux and networking

- [ ] LNX-JAVA-001. How does a JVM process receive and handle Linux shutdown signals?
- [ ] LNX-JAVA-002. Why should Java services close files, sockets and response bodies?
- [ ] LNX-JAVA-003. How do Java thread pools interact with blocking network I/O?
- [ ] LNX-JAVA-004. How can slow network calls exhaust servlet threads or worker pools?
- [ ] LNX-JAVA-005. Why should Java HTTP clients use connection pools?
- [ ] LNX-JAVA-006. How can Java DNS cache settings affect service discovery and failover?
- [ ] LNX-JAVA-007. How can Java truststore configuration break outbound HTTPS calls?
- [ ] LNX-JAVA-008. What is the difference between binding a Java server to `localhost` and `0.0.0.0`?
- [ ] LNX-JAVA-009. How can JVM memory settings conflict with Linux or container memory limits?
- [ ] 🧠 LNX-JAVA-010. How can many short-lived outbound connections from a Java service exhaust ports or file descriptors?

## 12. Linux and network security basics

- [ ] LNX-SEC-001. Why should backend services avoid running as `root`?
- [ ] LNX-SEC-002. What are Linux users, groups and least privilege in service deployment?
- [ ] LNX-SEC-003. What are Linux capabilities at a high level?
- [ ] LNX-SEC-004. How can firewall rules affect a service that appears healthy locally?
- [ ] LNX-SEC-005. What is SSH used for, and what are safer alternatives for production access?
- [ ] LNX-SEC-006. Why should secrets not be passed carelessly through command-line arguments or environment variables?
- [ ] 🧠 LNX-SEC-007. What are common risks of debugging production with packet captures or verbose network logs?

## 13. Production troubleshooting scenarios

- [ ] NET-SCEN-001. A service cannot connect to another service by hostname. What do you check?
- [ ] NET-SCEN-002. A service can resolve a hostname but cannot connect to the port. What do you check next?
- [ ] NET-SCEN-003. A Java app handles traffic locally but fails in a container. What Linux and network differences matter?
- [ ] NET-SCEN-004. A service returns `Connection refused` after deployment. What are likely causes?
- [ ] NET-SCEN-005. A service gets intermittent `Connection reset by peer`. What would you investigate?
- [ ] NET-SCEN-006. A service times out when calling a dependency, but the dependency logs show no request. Where can the failure be?
- [ ] NET-SCEN-007. TLS handshake failures appear only in one environment. What do you check?
- [ ] NET-SCEN-008. DNS failover happened, but some Java instances still call the old IP. What could explain it?
- [ ] NET-SCEN-009. A Java service suddenly hits `Too many open files`. How do you debug and mitigate it?
- [ ] NET-SCEN-010. A host has disk space available, but the service cannot create files. What do you check?
- [ ] NET-SCEN-011. CPU usage is low, but request latency is high. Which Linux and network signals do you inspect?
- [ ] NET-SCEN-012. A load balancer returns `504` while backend instances look healthy. What do you investigate?
- [ ] NET-SCEN-013. Outbound calls fail only under high concurrency. What are likely bottlenecks?
- [ ] NET-SCEN-014. After a deploy, many connections stay in `CLOSE_WAIT`. What does that suggest?
- [ ] NET-SCEN-015. A service works with direct IP but fails by hostname. What does that tell you?
- [ ] 🧠 NET-SCEN-016. How do network partitions show up in distributed backend systems?

## Counts

- Total questions: 144
- Priority 1 questions: 127
- Priority 2 questions: 17
