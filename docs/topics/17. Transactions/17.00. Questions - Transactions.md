# Transactions

## 1. Transaction fundamentals and boundaries

- [ ] TX-FUND-001. What problem do transactions solve in backend applications?
- [ ] TX-FUND-002. How would you explain ACID without only listing the acronym?
- [ ] TX-FUND-003. What is a transaction boundary, and why is it usually placed at the service layer?
- [ ] TX-FUND-004. How do commit, rollback, and savepoints differ?
- [ ] TX-FUND-005. How does JDBC autocommit affect transaction behavior?
- [ ] TX-FUND-006. What is the difference between local, global, and distributed transactions?
- [ ] 🧠 TX-FUND-007. What is the Unit of Work pattern, and how is it related to transactions?
- [ ] TX-FUND-008. Why are long-running transactions dangerous?
- [ ] TX-FUND-009. What is the difference between database consistency and application-level business consistency?
- [ ] TX-FUND-010. Why do transactions not solve all distributed consistency problems?

## 2. Isolation, anomalies, and retry safety

- [ ] TX-ISO-001. What SQL transaction isolation levels exist, and what trade-offs do they represent?
- [ ] TX-ISO-002. How do dirty reads, non-repeatable reads, phantom reads, lost updates, and write skew differ?
- [ ] TX-ISO-003. Which anomalies can still happen at Read Committed?
- [ ] TX-ISO-004. Why does Repeatable Read not mean exactly the same thing in every database?
- [ ] TX-ISO-005. What does Serializable isolation guarantee, and what does it cost?
- [ ] 🧠 TX-ISO-006. How is Snapshot Isolation different from Serializable isolation?
- [ ] TX-ISO-007. What is a serialization failure, and why should applications usually retry it?
- [ ] TX-ISO-008. How is a deadlock different from a serialization failure?
- [ ] TX-ISO-009. How would you choose an isolation level for money transfer and reporting use cases?
- [ ] TX-ISO-010. How can replica reads violate read-your-writes expectations?
- [ ] TX-ISO-011. How can unique constraints prevent some concurrency anomalies?
- [ ] TX-ISO-012. How can `SELECT FOR UPDATE` help prevent lost updates?
- [ ] TX-ISO-013. How would you explain isolation anomalies with a concrete order or payment example?

## 3. Database behavior Java developers should know

- [ ] TX-DB-001. Why can the same isolation level behave differently in PostgreSQL and MySQL?
- [ ] TX-DB-002. What is MVCC, and how is it related to transaction isolation?
- [ ] TX-DB-003. What is `idle in transaction`, and why is it dangerous?
- [ ] TX-DB-004. How do transaction timeout, statement timeout, lock timeout, and connection pool timeout differ?
- [ ] 🧠 TX-DB-005. How should a Java service handle PostgreSQL SQLSTATE `40001` and `40P01`?
- [ ] TX-DB-006. How can application transactions block DDL or database migrations?
- [ ] TX-DB-007. What transaction behavior should a Java developer not assume when switching from MySQL to PostgreSQL?

## 4. Spring transaction model

- [ ] TX-SPR-001. What does `@Transactional` do at a high level?
- [ ] TX-SPR-002. Why does `@Transactional` usually rely on a Spring proxy?
- [ ] TX-SPR-003. Why do self-invocation, private methods, final methods, and constructor calls often bypass `@Transactional`?
- [ ] TX-SPR-004. Why is `@Transactional` usually placed on service methods instead of controllers or repositories only?
- [ ] TX-SPR-005. What is `PlatformTransactionManager`, and how does Spring choose one?
- [ ] TX-SPR-006. When would you use `TransactionTemplate` instead of `@Transactional`?
- [ ] TX-SPR-007. Why does `@Transactional` not automatically work across threads or async boundaries?
- [ ] TX-SPR-008. When would you use `@TransactionalEventListener` for after-commit work?
- [ ] 🧠 TX-SPR-009. How does reactive or R2DBC transaction management differ from thread-bound transaction management?

## 5. Propagation, rollback, and transaction attributes

- [ ] TX-PROP-001. What transaction propagation modes does Spring support, and what is the safest default?
- [ ] TX-PROP-002. How does `Propagation.REQUIRES_NEW` work, and why can it exhaust the connection pool?
- [ ] TX-PROP-003. Why do propagation settings not cross HTTP, microservice, or async boundaries?
- [ ] TX-PROP-004. How would you choose propagation for order creation, audit logging, and outbox writes?
- [ ] TX-PROP-005. Why can an inner `REQUIRED` method cause `UnexpectedRollbackException` in the outer method?
- [ ] TX-PROP-006. Which exceptions trigger rollback by default in Spring, and how do checked or caught exceptions change that?
- [ ] TX-PROP-007. When would you use `noRollbackFor` for business exceptions?
- [ ] TX-PROP-008. How is `@Transactional` timeout different from statement timeout, HTTP timeout, and connection pool timeout?
- [ ] TX-PROP-009. Why can isolation, timeout, and read-only attributes be ignored when a method joins an existing transaction?
- [ ] TX-PROP-010. What does `readOnly = true` mean in Spring, and what does it not guarantee?
- [ ] TX-PROP-011. What are the risks of routing read-only transactions to replicas?

## 6. JPA/Hibernate and transaction boundaries

- [ ] TX-JPA-001. How is the JPA persistence context scoped in a typical Spring transaction?
- [ ] TX-JPA-002. How do dirty checking, flush, and commit differ?
- [ ] TX-JPA-003. When can Hibernate flush before transaction commit?
- [ ] TX-JPA-004. How can `readOnly = true` interact with Hibernate flush mode and dirty checking?
- [ ] TX-JPA-005. How is `LazyInitializationException` related to transaction boundaries and Open Session in View?
- [ ] TX-JPA-006. Why is keeping a transaction open during JSON serialization dangerous?
- [ ] TX-JPA-007. How does optimistic locking with `@Version` work, and how should `OptimisticLockException` be handled?
- [ ] TX-JPA-008. Why should external service calls usually not happen inside a JPA transaction?
- [ ] TX-JPA-009. How can N+1 queries or a large persistence context increase transaction duration?
- [ ] TX-JPA-010. How should large batch imports manage transaction boundaries and persistence-context memory?
- [ ] TX-JPA-011. What is the difference between database constraints and JPA validation during a transaction?

## 7. Locking and transactional consistency patterns

- [ ] TX-LOCK-001. When is optimistic locking better than pessimistic locking, and vice versa?
- [ ] TX-LOCK-002. How can version columns prevent lost updates?
- [ ] TX-LOCK-003. When would you use `SELECT FOR UPDATE`?
- [ ] 🧠 TX-LOCK-004. How can `SKIP LOCKED` be used for worker queues?
- [ ] TX-LOCK-005. How do unique constraints and upserts prevent check-then-insert races?
- [ ] TX-LOCK-006. How would you prevent two workers from processing the same record?
- [ ] TX-LOCK-007. How would you implement a safe money transfer between two accounts?
- [ ] TX-LOCK-008. How do consistent lock ordering and short transactions reduce deadlocks?
- [ ] TX-LOCK-009. Why is database-level locking usually better than `synchronized` for shared database state?
- [ ] TX-LOCK-010. How would you choose between constraints, explicit locks, optimistic locking, and Serializable isolation?
- [ ] TX-LOCK-011. How can idempotency keys and unique constraints enforce transactional command safety?

## 8. Distributed workflows and microservice consistency

- [ ] TX-DIST-001. What is a distributed transaction, and why is it difficult?
- [ ] 🧠 TX-DIST-002. What is two-phase commit, and what are its main drawbacks?
- [ ] TX-DIST-003. Why are XA transactions rarely preferred in microservice architectures?
- [ ] TX-DIST-004. What is the Saga pattern?
- [ ] TX-DIST-005. How do choreography and orchestration differ in Saga workflows?
- [ ] TX-DIST-006. How is a compensating transaction different from rollback?
- [ ] TX-DIST-007. How would you design order creation across Order, Payment, and Inventory services?
- [ ] TX-DIST-008. How can timeout ambiguity affect distributed workflow outcomes?
- [ ] TX-DIST-009. Why should each service usually own its database?
- [ ] TX-DIST-010. How should a user-facing API represent an eventually consistent workflow state?
- [ ] TX-DIST-011. How do retries, duplicate messages, and out-of-order messages affect distributed workflows?
- [ ] TX-DIST-012. How would you reconcile inconsistent state between services?
- [ ] 🧠 TX-DIST-013. What is a process manager, and when is it useful?
- [ ] TX-DIST-014. When would you keep a workflow in a single monolith transaction instead of splitting it into microservices?

## 9. Outbox, messaging, and idempotency

- [ ] TX-OUTBOX-001. What is the Transactional Outbox pattern?
- [ ] TX-OUTBOX-002. Why is writing to a database and publishing to Kafka not atomic by default?
- [ ] TX-OUTBOX-003. How does an outbox table participate in the same local transaction as business data?
- [ ] 🧠 TX-OUTBOX-004. What are the trade-offs between polling publisher and CDC-based outbox publishing?
- [ ] TX-OUTBOX-005. Why does Transactional Outbox still require idempotent consumers?
- [ ] TX-OUTBOX-006. How would you implement an idempotent consumer with a `processed_messages` table?
- [ ] TX-OUTBOX-007. How would you implement idempotency keys for HTTP commands?
- [ ] 🧠 TX-OUTBOX-008. What is the Inbox pattern, and how does it differ from Outbox?
- [ ] TX-OUTBOX-009. What ordering guarantees can and cannot be achieved with Outbox?
- [ ] TX-OUTBOX-010. How would you handle failed outbox publishing and monitor outbox lag?
- [ ] TX-OUTBOX-011. How would you design an event so duplicate delivery is safe?
- [ ] TX-OUTBOX-012. Why does Kafka exactly-once processing not automatically make database writes exactly-once?
- [ ] TX-OUTBOX-013. Why is a Kafka producer transaction not the same as a database transaction?
- [ ] TX-OUTBOX-014. How should message listener offset commits relate to database transaction commits?

## 10. Performance and resource usage

- [ ] TX-PERF-001. How do transactions affect database connection usage?
- [ ] TX-PERF-002. Why can long transactions exhaust a connection pool?
- [ ] TX-PERF-003. Why should external HTTP calls or Kafka publishing usually not block inside a database transaction?
- [ ] TX-PERF-004. How can slow queries and N+1 queries increase transaction duration?
- [ ] TX-PERF-005. What are the trade-offs between one huge transaction and many tiny transactions?
- [ ] TX-PERF-006. How can locks, foreign keys, constraints, and triggers affect transaction performance?
- [ ] TX-PERF-007. How does connection pool max size interact with database `max_connections`?
- [ ] TX-PERF-008. How do virtual threads change, or not change, database transaction bottlenecks?
- [ ] TX-PERF-009. What metrics would you monitor for transaction duration, lock waits, and rollback rate?
- [ ] TX-PERF-010. How would you prevent one slow transaction from degrading the whole service?

## 11. Testing transaction behavior

- [ ] TX-TEST-001. How do you test transactional behavior in Spring?
- [ ] TX-TEST-002. Why can `@Transactional` tests hide real production behavior?
- [ ] TX-TEST-003. How would you test rollback behavior and `rollbackFor` checked exceptions?
- [ ] TX-TEST-004. How would you test `REQUIRES_NEW`, `NESTED`, or self-invocation behavior?
- [ ] TX-TEST-005. Why should transaction isolation tests use a real database, often with Testcontainers?
- [ ] TX-TEST-006. How would you test lost update prevention, optimistic locking, and deadlock retry?
- [ ] TX-TEST-007. How would you test outbox atomicity and idempotency under retries?
- [ ] TX-TEST-008. How can mocking repositories give false confidence about transactional code?
- [ ] TX-TEST-009. What should be unit-tested versus integration-tested in transactional code?

## 12. Production troubleshooting and anti-patterns

- [ ] TX-PROD-001. How would you investigate many `idle in transaction` sessions?
- [ ] TX-PROD-002. How would you investigate blocked transactions and find the blocking transaction?
- [ ] TX-PROD-003. How would you investigate deadlocks or frequent serialization failures?
- [ ] TX-PROD-004. How would you investigate connection pool exhaustion caused by transactions?
- [ ] TX-PROD-005. How would you investigate `UnexpectedRollbackException` or `@Transactional` not working?
- [ ] 🧠 TX-PROD-006. How would you investigate long locks, blocked migrations, or VACUUM not keeping up?
- [ ] 🧠 TX-PROD-007. How would you investigate slow commits or a high rollback rate?
- [ ] TX-PROD-008. How would you debug a lost update or Read Committed anomaly in production?
- [ ] TX-PROD-009. How would you debug a message published before rollback or a commit that succeeded before publishing failed?
- [ ] TX-PROD-010. How would you debug and drain an outbox backlog?
- [ ] TX-PROD-011. How would you debug replica lag affecting read-only transactions?
- [ ] TX-PROD-012. How would you debug transaction timeout, lock timeout, and statement timeout issues?
- [ ] TX-PROD-013. How would you debug a stuck batch job with a very large transaction?
- [ ] TX-PROD-014. How would you correlate application logs, database sessions, and metrics for transaction failures?
- [ ] TX-PROD-015. Why can transaction-per-controller and transaction-per-repository-method both be bad designs?
- [ ] TX-PROD-016. Why is treating `readOnly = true` as a security feature a trap?

## 13. Senior scenario-based transaction questions

- [ ] TX-SCENARIO-001. A payment transaction writes to the database and the service crashes before publishing an event. How would you fix the design?
- [ ] TX-SCENARIO-002. A service publishes a Kafka event and then the database transaction rolls back. What went wrong?
- [ ] TX-SCENARIO-003. Two users reserve the last available item at the same time. How would you prevent overselling?
- [ ] TX-SCENARIO-004. Two workers process the same job row. How would you fix it?
- [ ] TX-SCENARIO-005. A batch import holds locks for minutes and blocks the application. How would you redesign it?
- [ ] 🧠 TX-SCENARIO-006. A transaction stays idle for 20 minutes and autovacuum cannot clean old tuples. What is happening?
- [ ] TX-SCENARIO-007. An API retries after timeout and creates duplicate orders. How would you make it safe?
- [ ] TX-SCENARIO-008. A read-only endpoint sometimes does not see the object it just created. What could cause it?
- [ ] TX-SCENARIO-009. A migration waits for a long time because application transactions hold locks. What would you check?
- [ ] TX-SCENARIO-010. A supposedly atomic operation touches two databases. What are your options?
- [ ] 🧠 TX-SCENARIO-011. How would you explain the lifecycle of a Spring transactional service method from proxy call to commit?

## Counts

- Total questions: 146
- Priority 1 questions: 133
- Priority 2 questions: 13
