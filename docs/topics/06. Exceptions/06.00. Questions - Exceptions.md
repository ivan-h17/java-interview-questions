# Exceptions

## 1. Exception hierarchy, contracts and checked vs unchecked

- [ ] EXC-CORE-001. Explain the Java exception hierarchy and the practical difference between `Throwable`, `Exception`, `RuntimeException` and `Error`.
- [ ] EXC-CORE-002. What makes an exception checked or unchecked, and which exceptions must be handled or declared?
- [ ] EXC-CORE-003. How are exceptions propagated through the call stack?
- [ ] EXC-CORE-004. Why are exceptions part of a method or API contract?
- [ ] EXC-CORE-005. When is catching `Throwable` or `Error` acceptable, and why is it usually risky?
- [ ] EXC-CORE-006. How do exception type, message, cause and stack trace serve different purposes?
- [ ] EXC-CORE-007. How do common runtime exceptions like `NullPointerException`, `IllegalArgumentException`, `IllegalStateException`, `UnsupportedOperationException` and `ClassCastException` signal different bugs?
- [ ] EXC-CHECK-001. When should you use checked exceptions, and when should you use unchecked exceptions?
- [ ] EXC-CHECK-002. Why are checked exceptions controversial in backend Java applications?
- [ ] EXC-CHECK-003. Why should public APIs usually avoid `throws Exception` and generic `Exception`?
- [ ] 🧠 EXC-CHECK-004. What is "sneaky throws", and why should it be used carefully?
- [ ] 🧠 EXC-CORE-008. How are exceptions represented in bytecode and the JVM execution model?

## 2. `try`, `catch`, `finally` and resource cleanup

- [ ] EXC-FLOW-001. How should `catch` blocks be ordered, and what does multi-catch change?
- [ ] EXC-FLOW-002. What does `finally` guarantee, and when can it fail to run?
- [ ] EXC-FLOW-003. How do `return`, `throw` and `finally` interact?
- [ ] EXC-FLOW-004. Why should `finally` not return or throw without a deliberate reason?
- [ ] EXC-FLOW-005. Why should exceptions not be used for normal control flow?
- [ ] EXC-TWR-001. How does try-with-resources work?
- [ ] EXC-TWR-002. What requirements must a resource satisfy to be used in try-with-resources?
- [ ] EXC-TWR-003. In what order are resources closed in try-with-resources?
- [ ] EXC-TWR-004. What are suppressed exceptions, how do they differ from the cause, and what happens if both the body and `close()` throw?
- [ ] EXC-TWR-005. How should JDBC resources, HTTP responses and file streams be closed?
- [ ] EXC-TWR-006. Why is relying on GC, finalizers or `Cleaner` unsafe for external resource cleanup?
- [ ] 🧠 EXC-TWR-007. Should `AutoCloseable.close()` be idempotent or throw `InterruptedException`?

## 3. Custom exception design, propagation and chaining

- [ ] EXC-CUSTOM-001. When should you create a custom exception, and when should you avoid creating one?
- [ ] EXC-CUSTOM-002. Should a custom exception extend `Exception` or `RuntimeException`?
- [ ] EXC-CUSTOM-003. How should custom exceptions preserve the cause and expose useful structured details?
- [ ] EXC-CUSTOM-004. What is the difference between domain exceptions and technical exceptions?
- [ ] EXC-CUSTOM-005. How would you design an exception hierarchy for business rules or a Java SDK?
- [ ] EXC-CUSTOM-006. How should validation failures be represented: exceptions, result objects or error lists?
- [ ] EXC-CUSTOM-007. At which boundaries should technical exceptions be translated into application or domain exceptions?
- [ ] EXC-PROP-001. What is exception chaining, and how is it different from exception wrapping?
- [ ] EXC-PROP-002. When should you rethrow the same exception instead of throwing a new one?
- [ ] EXC-PROP-003. How can wrapping add useful context without hiding the root cause?
- [ ] EXC-PROP-004. How can stack trace information or the original cause be lost?
- [ ] EXC-PROP-005. When would you prefer `Optional`, `Result`, `Either` or error objects instead of exceptions?
- [ ] 🧠 EXC-CUSTOM-008. When would you disable stack trace generation in a custom exception?

## 4. Stack traces, logging, observability and security

- [ ] EXC-LOG-001. How do you read a Java stack trace and identify the real root cause?
- [ ] EXC-LOG-002. Why should production code usually avoid `printStackTrace()`?
- [ ] EXC-LOG-003. How should exceptions be logged correctly, including logger argument order?
- [ ] EXC-LOG-004. Why is logging the same exception multiple times harmful, and what does "log once, handle once" mean?
- [ ] EXC-LOG-005. What context should be included when logging an exception?
- [ ] EXC-LOG-006. How do correlation IDs, trace IDs and MDC / thread context help exception debugging?
- [ ] EXC-LOG-007. How should stack traces be handled in user-facing API responses?
- [ ] EXC-LOG-008. Which exception metrics are useful in production, and why can high-cardinality labels hurt metrics systems?
- [ ] EXC-LOG-009. When would you sample exception logs?
- [ ] EXC-SEC-001. What internal data can exception messages or logs accidentally leak?
- [ ] EXC-SEC-002. How do you convert internal exceptions into safe external errors?
- [ ] EXC-SEC-003. How can swallowed exceptions create security or correctness bugs?
- [ ] 🧠 EXC-LOG-010. How do you read `... N more` in Java stack traces?
- [ ] 🧠 EXC-LOG-011. Why can asynchronous code make stack traces and logging context harder to read?

## 5. Spring and backend exception handling

- [ ] EXC-SPRING-001. How does Spring MVC handle exceptions from controllers and global handlers?
- [ ] EXC-SPRING-002. How would you choose between `@ControllerAdvice`, `@ExceptionHandler`, `ResponseStatusException` and `ProblemDetail`?
- [ ] EXC-SPRING-003. How should backend exceptions be mapped to HTTP status codes and stable application error codes?
- [ ] EXC-SPRING-004. How are validation exceptions usually handled in Spring Boot?
- [ ] EXC-SPRING-005. Why should internal exception details not be exposed to API clients?
- [ ] EXC-SPRING-006. How does `@Transactional` decide whether to roll back for checked exceptions, unchecked exceptions and `Error`?
- [ ] EXC-SPRING-007. How can swallowing an exception break transaction rollback?
- [ ] EXC-SPRING-008. What is Spring persistence exception translation, and why is `DataAccessException` unchecked?
- [ ] EXC-SPRING-009. How should retryable and non-retryable backend exceptions be distinguished?
- [ ] EXC-SPRING-010. How do idempotency, timeouts and exception handling interact in REST APIs?
- [ ] EXC-SPRING-011. How should HTTP client exceptions from `RestClient`, `WebClient`, Feign or `RestTemplate` be handled?
- [ ] EXC-SPRING-012. How should exceptions in scheduled jobs, `@Async` methods and message listeners be handled?
- [ ] EXC-SPRING-013. What observability should exist around backend exceptions?

## 6. Concurrency, async execution and interruption

- [ ] EXC-ASYNC-001. What happens if an exception is thrown from `Thread.run()`, and what is `Thread.UncaughtExceptionHandler`?
- [ ] EXC-ASYNC-002. What is the difference between `ExecutorService.execute()` and `submit()` for exceptions?
- [ ] EXC-ASYNC-003. How does `Future.get()` report task failures?
- [ ] EXC-ASYNC-004. How does `CompletableFuture` wrap failures, and how do `get()` and `join()` differ?
- [ ] EXC-ASYNC-005. How should exceptions be handled in a `CompletableFuture` chain?
- [ ] EXC-ASYNC-006. How do checked exceptions fit into `CompletableFuture` pipelines?
- [ ] EXC-ASYNC-007. What makes `InterruptedException` special, and why should the interrupt flag often be restored?
- [ ] EXC-ASYNC-008. What is the difference between cancellation and failure?
- [ ] EXC-ASYNC-009. How can unhandled async exceptions silently disappear?
- [ ] EXC-ASYNC-010. Why should locks, semaphores, permits and other concurrency resources be released after exceptions?
- [ ] 🧠 EXC-ASYNC-011. How do exceptions behave in virtual threads?
- [ ] 🧠 EXC-ASYNC-012. How do structured concurrency APIs aggregate or propagate task failures?

## 7. Performance and testing

- [ ] EXC-PERF-001. Are exceptions expensive in Java, and what part is usually expensive?
- [ ] EXC-PERF-002. Why should exceptions not be used in hot-path control flow?
- [ ] EXC-PERF-003. How do exception allocation and full stack trace logging affect CPU, memory and GC pressure?
- [ ] EXC-PERF-004. What is an exception storm in production, and how would you reduce exception volume?
- [ ] 🧠 EXC-PERF-005. What do `Throwable.fillInStackTrace()` and HotSpot fast-throw optimization do?
- [ ] 🧠 EXC-PERF-006. Is a `try-catch` block expensive when no exception is thrown, and how can exceptions affect JIT optimization?
- [ ] 🧠 EXC-PERF-007. How would you benchmark exception-heavy code correctly?
- [ ] EXC-TEST-001. How do you test that code throws an exception and verify its message, error code or cause?
- [ ] EXC-TEST-002. How do you test cleanup, transaction rollback or retry behavior after an exception?
- [ ] EXC-TEST-003. How do you test interruption handling and exceptions in asynchronous code?
- [ ] EXC-TEST-004. Why are tests that assert full stack traces usually brittle?
- [ ] 🧠 EXC-TEST-005. How do you test suppressed exceptions or use failure injection testing?

## 8. Senior scenario-based exception questions

- [ ] EXC-SCENARIO-001. An external payment API times out. What should the service return, log and retry?
- [ ] EXC-SCENARIO-002. A database unique constraint violation happens during user registration. How should it be handled?
- [ ] EXC-SCENARIO-003. A request partially succeeds before an exception is thrown. How do you keep the system consistent?
- [ ] EXC-SCENARIO-004. A scheduled job fails repeatedly. How should failures be logged, retried and alerted?
- [ ] EXC-SCENARIO-005. A Kafka consumer fails to process a message. How should retries and DLQ behavior be designed?
- [ ] EXC-SCENARIO-006. A REST endpoint returns a stack trace to the client. What is wrong and how would you fix it?
- [ ] EXC-SCENARIO-007. A service slowly exhausts its database connection pool. How can exception handling cause this?
- [ ] EXC-SCENARIO-008. The application throws `OutOfMemoryError` in production. Should you catch it?
- [ ] EXC-SCENARIO-009. Logs show only `CompletionException`. How do you find the real root cause?
- [ ] EXC-SCENARIO-010. A service ignores `InterruptedException` and shutdown hangs. What is the bug?
- [ ] EXC-SCENARIO-011. A Spring `@Transactional` method throws a checked exception and data is committed. Why?
- [ ] EXC-SCENARIO-012. A client retries after a timeout and creates duplicate data. How should exceptions and idempotency be handled?

---

## Counts

- Total questions: 100
- Priority 1 questions: 88
- Priority 2 questions: 12
