# HashMap / equals / hashCode

## 1. Equality and hashing fundamentals

- [ ] HM-FUND-001. What is the difference between object identity, logical equality and `==`?
- [ ] HM-FUND-002. What is `hashCode()`, and how is hash-code equality different from object equality?
- [ ] HM-FUND-003. Why do hash-based collections need both `equals()` and `hashCode()`?
- [ ] HM-FUND-004. What are a hash table, a hash function and a bucket?
- [ ] HM-FUND-005. What is a hash collision, and why are collisions unavoidable?
- [ ] HM-FUND-006. What does it mean for a hash function to be well-distributed?
- [ ] HM-FUND-007. How does bad hash distribution affect `HashMap` performance?
- [ ] HM-FUND-008. How do records generate `equals()` and `hashCode()`?
- [ ] 🧠 HM-FUND-009. How can Lombok-generated `equals()` and `hashCode()` break domain objects or collections?

## 2. `equals()` and `hashCode()` contract

- [ ] HM-CONTRACT-001. What is the `equals()` and `hashCode()` contract?
- [ ] HM-CONTRACT-002. If two objects are equal by `equals()`, what must be true about `hashCode()`?
- [ ] HM-CONTRACT-003. If two objects have the same `hashCode()`, must they be equal by `equals()`?
- [ ] HM-CONTRACT-004. What happens if only one of `equals()` or `hashCode()` is overridden?
- [ ] HM-CONTRACT-005. What properties must `equals()` satisfy: reflexive, symmetric, transitive, consistent and non-null?
- [ ] HM-CONTRACT-006. What are common symmetry and transitivity violations in `equals()`?
- [ ] HM-CONTRACT-007. Why can inheritance make `equals()` difficult to implement correctly?
- [ ] 🧠 HM-CONTRACT-008. What is the `canEqual` pattern?
- [ ] HM-CONTRACT-009. Should `equals()` use `getClass()` or `instanceof`?
- [ ] HM-CONTRACT-010. How should `equals()` and `hashCode()` handle nullable fields and arrays?
- [ ] HM-CONTRACT-011. Why is `BigDecimal` tricky in equality and hash-based collections?
- [ ] HM-CONTRACT-012. What fields should be included in `equals()` and `hashCode()`?
- [ ] HM-CONTRACT-013. Why are mutable fields dangerous in `equals()` and `hashCode()`?
- [ ] 🧠 HM-CONTRACT-014. How can `Objects.equals()` and `Objects.hash()` help, and what are the performance trade-offs?
- [ ] HM-CONTRACT-015. How would you test an `equals()` and `hashCode()` implementation?

## 3. `HashMap` internals

- [ ] HM-INTERNAL-001. How is `HashMap` structured internally?
- [ ] HM-INTERNAL-002. How does `HashMap` turn a key into a bucket index?
- [ ] HM-INTERNAL-003. Why is `HashMap` capacity usually a power of two?
- [ ] 🧠 HM-INTERNAL-004. What hash spreading does `HashMap` perform?
- [ ] HM-INTERNAL-005. How does `put()` work for new and existing keys?
- [ ] HM-INTERNAL-006. How does `get()` work internally?
- [ ] 🧠 HM-INTERNAL-007. How does `remove()` work internally?
- [ ] HM-INTERNAL-008. What is the difference between table capacity and map size?
- [ ] 🧠 HM-INTERNAL-009. When is the internal table allocated?
- [ ] HM-INTERNAL-010. Does `HashMap` preserve insertion order, and why should you not rely on its iteration order?
- [ ] HM-INTERNAL-011. How do `keySet()`, `values()` and `entrySet()` views work, and are they backed by the map?
- [ ] HM-INTERNAL-012. Are `HashMap` iterators fail-fast?
- [ ] 🧠 HM-INTERNAL-013. What is `modCount`, and what structural modifications affect `HashMap` iterators?
- [ ] 🧠 HM-INTERNAL-014. What are common wrong explanations of `HashMap` internals?

## 4. Collisions and tree bins

- [ ] HM-COLL-001. How does `HashMap` handle collisions?
- [ ] HM-COLL-002. What is separate chaining?
- [ ] HM-COLL-003. What happens when many keys land in the same bucket?
- [ ] HM-COLL-004. What is treeification in `HashMap`?
- [ ] 🧠 HM-COLL-005. What are `TREEIFY_THRESHOLD`, `UNTREEIFY_THRESHOLD` and `MIN_TREEIFY_CAPACITY`?
- [ ] 🧠 HM-COLL-006. Why does `HashMap` resize instead of treeifying when the table is still small?
- [ ] 🧠 HM-COLL-007. How does `HashMap` compare keys inside a tree bin, and do keys need to implement `Comparable`?
- [ ] HM-COLL-008. How does treeification affect worst-case lookup time?
- [ ] HM-COLL-009. Can a poor `hashCode()` still hurt performance even with tree bins?
- [ ] HM-COLL-010. How does modern `HashMap` mitigate collision-heavy cases compared to older Java versions?

## 5. Capacity, load factor and resizing

- [ ] HM-CAP-001. What are capacity, size, load factor and resize threshold in `HashMap`?
- [ ] HM-CAP-002. What is the default load factor in `HashMap`, and why is it a trade-off?
- [ ] HM-CAP-003. When does resizing happen, and what happens during resize?
- [ ] HM-CAP-004. Why can resizing be expensive and cause latency spikes?
- [ ] HM-CAP-005. How would you choose initial capacity for a `HashMap`?
- [ ] 🧠 HM-CAP-006. Why is `new HashMap<>(expectedSize)` often not the exact final capacity you need?
- [ ] 🧠 HM-CAP-007. In modern Java, when is `HashMap.newHashMap(expectedMappings)` useful?
- [ ] 🧠 HM-CAP-008. How does Java 8+ `HashMap` redistribute nodes during resize?
- [ ] HM-CAP-009. How do capacity and load factor trade memory for speed?
- [ ] HM-CAP-010. Can very high or very low load factors be a problem?
- [ ] HM-CAP-011. Why can an oversized `HashMap` make iteration slower?
- [ ] HM-CAP-012. How would you optimize a `HashMap` used in a hot path?

## 6. Keys, mutability and domain modeling

- [ ] HM-KEY-001. Can you use a mutable object as a `HashMap` key?
- [ ] HM-KEY-002. What happens if a key is mutated after being added to `HashMap`?
- [ ] HM-KEY-003. What are the requirements for a good `HashMap` key?
- [ ] HM-KEY-004. Why are `String` and records often good `HashMap` keys?
- [ ] HM-KEY-005. What can go wrong when using arrays as `HashMap` keys?
- [ ] HM-KEY-006. What can go wrong when using `List` or `Set` as `HashMap` keys?
- [ ] HM-KEY-007. Can `BigDecimal` be tricky as a `HashMap` key?
- [ ] HM-KEY-008. Should JPA entities be used as `HashMap` keys?
- [ ] HM-KEY-009. What are `equals()` and `hashCode()` pitfalls for Hibernate entities?
- [ ] HM-KEY-010. How do generated IDs affect `equals()` and `hashCode()` in entities?
- [ ] HM-KEY-011. What key would you choose for a cache map?
- [ ] HM-KEY-012. How do you design a composite key class?
- [ ] 🧠 HM-KEY-013. Should `hashCode()` be cached, and when can cached hash codes be safe?
- [ ] HM-KEY-014. What can go wrong if `equals()` or `hashCode()` depend on external state?

## 7. Nulls, views, iteration and ordering

- [ ] HM-NULL-001. Can `HashMap` store null keys and null values?
- [ ] HM-NULL-002. How does `get()` behave when a key is absent versus mapped to null?
- [ ] HM-NULL-003. Why can null values make `containsKey()` necessary?
- [ ] HM-NULL-004. What is the difference between `get()` and `getOrDefault()`?
- [ ] HM-NULL-005. What is the difference between `containsKey()` and `containsValue()`?
- [ ] HM-NULL-006. Can `ConcurrentHashMap` store null keys or null values, and why?
- [ ] HM-ITER-001. How does `HashMap` iteration work?
- [ ] HM-ITER-002. What is `ConcurrentModificationException` in `HashMap` iteration?
- [ ] HM-ITER-003. How do you remove entries while iterating over a `HashMap`?
- [ ] HM-ITER-004. What is the difference between iterating over `keySet()` and `entrySet()`?
- [ ] HM-ITER-005. How can you preserve insertion order in a map?
- [ ] HM-ITER-006. How can you preserve sorted order in a map?

## 8. Comparison with related `Map` implementations

- [ ] HM-COMP-001. How do `HashMap`, `LinkedHashMap` and `TreeMap` differ?
- [ ] HM-COMP-002. When should you choose `LinkedHashMap` or `TreeMap` instead of `HashMap`?
- [ ] HM-COMP-003. What is the difference between `HashMap`, `Hashtable` and `Collections.synchronizedMap()`?
- [ ] HM-COMP-004. What is the difference between `HashMap` and `ConcurrentHashMap`?
- [ ] HM-COMP-005. What is the difference between `HashMap` and `Map.of()`?
- [ ] 🧠 HM-COMP-006. How do `Map.equals()` and `Map.hashCode()` work across different map implementations?
- [ ] HM-COMP-007. What is the difference between `HashMap` and an `ArrayList` of key-value pairs?
- [ ] 🧠 HM-COMP-008. When are `IdentityHashMap`, `WeakHashMap` or `EnumMap` better than a normal `HashMap`?

## 9. `ConcurrentHashMap` and multi-threaded behavior

- [ ] HM-CONC-001. Is `HashMap` thread-safe?
- [ ] HM-CONC-002. What can go wrong if multiple threads modify a `HashMap` concurrently?
- [ ] HM-CONC-003. How does `ConcurrentHashMap` provide thread-safety at a high level?
- [ ] 🧠 HM-CONC-004. What changed in `ConcurrentHashMap` after Java 8?
- [ ] HM-CONC-005. What are atomic operations in `ConcurrentHashMap`?
- [ ] HM-CONC-006. Why is check-then-act unsafe with maps?
- [ ] HM-CONC-007. How should you implement a frequency map with `ConcurrentHashMap`?
- [ ] HM-CONC-008. What are `computeIfAbsent()`, `compute()`, `merge()` and `putIfAbsent()` used for?
- [ ] 🧠 HM-CONC-009. What concurrency caveats exist with `ConcurrentHashMap` mapping functions?
- [ ] HM-CONC-010. Why should `ConcurrentHashMap` mapping functions be short and side-effect safe?
- [ ] 🧠 HM-CONC-011. What are weakly consistent iterators in `ConcurrentHashMap`?
- [ ] HM-CONC-012. When would you still use external locking around a map?
- [ ] HM-CONC-013. Can `ConcurrentHashMap` make a multi-step business invariant thread-safe?
- [ ] HM-CONC-014. How would you debug race conditions involving maps?

## 10. Performance, memory and security

- [ ] HM-PERF-001. What is the average time complexity of `get()` and `put()` in `HashMap`?
- [ ] HM-PERF-002. When can `HashMap` operations become `O(n)`?
- [ ] HM-PERF-003. Why is `HashMap` performance still not just `O(1)`?
- [ ] HM-PERF-004. How do `hashCode()` quality, allocation and `equals()` cost affect performance?
- [ ] HM-PERF-005. How can expensive `equals()` hurt `HashMap` performance?
- [ ] HM-PERF-006. How does key object allocation affect GC pressure?
- [ ] 🧠 HM-PERF-007. What is the memory overhead of `HashMap` entries?
- [ ] HM-PERF-008. How can pre-sizing a `HashMap` improve performance?
- [ ] HM-PERF-009. How can an unbounded or static `HashMap` cause a memory leak?
- [ ] HM-PERF-010. How would you profile `HashMap`-related memory growth?
- [ ] 🧠 HM-PERF-011. What are hash collision denial-of-service attacks?
- [ ] HM-PERF-012. How would you choose between `HashMap`, a cache library and a database lookup?

## 11. Java backend scenarios and traps

- [ ] HM-SCEN-001. How would you choose between `ArrayList` and `HashMap` for storing key-value pairs?
- [ ] HM-SCEN-002. How would you group objects by a field efficiently?
- [ ] HM-SCEN-003. How would you count occurrences in a collection?
- [ ] HM-SCEN-004. How do `computeIfAbsent()`, `merge()` and `putIfAbsent()` differ, especially around nulls?
- [ ] HM-SCEN-005. How would you build a cache key for a Spring service method?
- [ ] HM-SCEN-006. What can go wrong with map keys based on request objects?
- [ ] HM-SCEN-007. How can `equals()` and `hashCode()` bugs break `HashSet`, `HashMap` or `Stream.distinct()`?
- [ ] HM-SCEN-008. How can `equals()` and `hashCode()` bugs break Hibernate collections?
- [ ] HM-SCEN-009. How can `equals()` and `hashCode()` bugs break deduplication logic?
- [ ] HM-SCEN-010. How would you review a custom `equals()` and `hashCode()` implementation?
- [ ] HM-SCEN-011. How would you migrate from mutable keys to immutable keys?
- [ ] HM-SCEN-012. How would you test map behavior under concurrency?
- [ ] HM-SCEN-013. What are the most common `HashMap` interview traps?

## Counts

- Total questions: 133
- Priority 1 questions: 111
- Priority 2 questions: 22
