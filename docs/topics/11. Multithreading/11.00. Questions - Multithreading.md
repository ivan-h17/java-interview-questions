# Multithreading

## 1. Fundamentals and Java Memory Model

- [ ] MT-FUND-001. What is concurrency, and how is it different from parallelism?
- [ ] MT-FUND-002. What is thread-safety?
- [ ] MT-FUND-003. What is shared mutable state, and why is it dangerous?
- [ ] MT-FUND-004. What is a race condition, and how is it different from a data race?
- [ ] MT-FUND-005. What are atomicity, visibility and ordering in concurrent programs?
- [ ] MT-JMM-001. What is the Java Memory Model?
- [ ] MT-JMM-002. What is the happens-before relationship, and why does it matter?
- [ ] MT-JMM-003. What happens-before rules matter most in everyday Java backend code?
- [ ] MT-JMM-004. What guarantees does `volatile` provide, and what does it not provide?
- [ ] MT-JMM-005. Why is `volatile` not enough for compound actions like `count++`?
- [ ] MT-JMM-006. What is the difference between `synchronized` and `volatile`?
- [ ] MT-JMM-007. What is safe publication?
- [ ] MT-JMM-008. What can go wrong if `this` escapes during construction?
- [ ] 🧠 MT-JMM-009. What special visibility guarantees do `final` fields have?
- [ ] 🧠 MT-JMM-010. What is double-checked locking, and why does it require `volatile`?
- [ ] 🧠 MT-JMM-011. How would you explain the JMM without treating "flush to main memory" as a literal implementation detail?

## 2. Threads, lifecycle, interruption and cancellation

- [ ] MT-THREAD-001. What is the difference between a process and a thread?
- [ ] MT-THREAD-002. What is the difference between `Thread` and `Runnable`?
- [ ] MT-THREAD-003. What is the difference between `Runnable`, `Callable` and `Future`?
- [ ] MT-THREAD-004. What is the difference between `Thread.start()` and `Thread.run()`?
- [ ] MT-THREAD-005. What are the main thread states in Java?
- [ ] MT-THREAD-006. What is a daemon thread, and when would you use one?
- [ ] MT-THREAD-007. What do `Thread.sleep()`, `Thread.join()` and `Thread.yield()` do?
- [ ] MT-THREAD-008. What is thread interruption?
- [ ] MT-THREAD-009. What is the difference between `interrupt()`, `isInterrupted()` and `interrupted()`?
- [ ] MT-THREAD-010. How should `InterruptedException` be handled?
- [ ] MT-THREAD-011. Why is `Thread.stop()` deprecated and dangerous?
- [ ] MT-THREAD-012. What happens when an uncaught exception is thrown in a thread, and why do thread names matter?

## 3. Monitors, `synchronized` and explicit locks

- [ ] MT-LOCK-001. What are a monitor and an intrinsic lock in Java?
- [ ] MT-LOCK-002. What object is locked by `synchronized` instance methods, static methods and blocks?
- [ ] MT-LOCK-003. Why might a private lock object be better than synchronizing on `this`, a `String` literal or a public object?
- [ ] MT-LOCK-004. What does it mean that `synchronized` and `ReentrantLock` are reentrant?
- [ ] MT-LOCK-005. What visibility guarantees does `synchronized` provide?
- [ ] MT-LOCK-006. What are `wait()`, `notify()` and `notifyAll()` used for?
- [ ] MT-LOCK-007. Why must `wait()`, `notify()` and `notifyAll()` be called while holding the monitor?
- [ ] MT-LOCK-008. What is the difference between `wait()` and `sleep()`?
- [ ] MT-LOCK-009. What is the difference between `notify()` and `notifyAll()`?
- [ ] 🧠 MT-LOCK-010. Why should `wait()` normally be used inside a `while` loop?
- [ ] MT-LOCK-011. What is a guarded block, and how would you implement a bounded buffer with `wait()` and `notifyAll()`?
- [ ] MT-LOCK-012. How can locks cause deadlocks, and how can lock ordering prevent them?
- [ ] MT-LOCK-013. What is the difference between `synchronized` and `ReentrantLock`?
- [ ] MT-LOCK-014. When are `tryLock()`, `lockInterruptibly()` and fair locks useful?
- [ ] MT-LOCK-015. How is `Condition` different from `wait()`/`notify()`?
- [ ] MT-LOCK-016. When does `ReadWriteLock` help, and when can it make performance worse?
- [ ] 🧠 MT-LOCK-017. What is `StampedLock`, and what are its pitfalls?

## 4. Atomic classes and concurrent data structures

- [ ] MT-ATOM-001. What are Atomic classes, and how does CAS work conceptually?
- [ ] MT-ATOM-002. What is a CAS retry loop, and why is CAS called optimistic?
- [ ] MT-ATOM-003. What are the advantages and disadvantages of Atomic classes compared with locks?
- [ ] MT-ATOM-004. When would you use `LongAdder` instead of `AtomicLong`?
- [ ] MT-ATOM-005. How can `AtomicReference` help publish immutable snapshots safely?
- [ ] 🧠 MT-ATOM-006. What is the ABA problem, and how can it be mitigated?
- [ ] 🧠 MT-ATOM-007. What is `VarHandle`, and how is it related to Atomic classes?
- [ ] 🧠 MT-ATOM-008. What is the difference between lock-free and wait-free progress?
- [ ] MT-ATOM-009. What is the difference between atomicity of one variable and atomicity of an invariant involving multiple variables?
- [ ] MT-COLL-001. What is the difference between synchronized collections and concurrent collections?
- [ ] MT-COLL-002. What is `ConcurrentHashMap`, and are compound operations automatically atomic?
- [ ] MT-COLL-003. What pitfalls exist when using `computeIfAbsent()`?
- [ ] MT-COLL-004. How can `ConcurrentHashMap` and `LongAdder` implement a scalable frequency map?
- [ ] MT-COLL-005. What is `CopyOnWriteArrayList`, and when should you use or avoid it?
- [ ] MT-COLL-006. How is `BlockingQueue` used in producer-consumer designs?
- [ ] MT-COLL-007. What are weakly consistent iterators, and how would you choose between `ConcurrentHashMap`, synchronized maps and immutable maps?

## 5. Executors, thread pools and task execution

- [ ] MT-EXEC-001. What is a thread pool, and why do backend services use thread pools?
- [ ] MT-EXEC-002. What is the difference between `Executor`, `ExecutorService` and `ThreadPoolExecutor`?
- [ ] MT-EXEC-003. What is the difference between `execute()` and `submit()`, especially for exception handling?
- [ ] MT-EXEC-004. What are the risks of blocking on `Future.get()`, and what does `cancel(true)` mean?
- [ ] MT-EXEC-005. How do `corePoolSize`, `maximumPoolSize`, `keepAliveTime` and the work queue interact?
- [ ] MT-EXEC-006. What is the difference between bounded and unbounded executor queues?
- [ ] MT-EXEC-007. What happens when an executor queue is full, and what are common rejection policies?
- [ ] MT-EXEC-008. What are the risks of `Executors.newFixedThreadPool()` and `Executors.newCachedThreadPool()`?
- [ ] MT-EXEC-009. What is the difference between `scheduleAtFixedRate()` and `scheduleWithFixedDelay()`?
- [ ] MT-EXEC-010. How should thread pool sizing differ for CPU-bound and I/O-bound work?
- [ ] MT-EXEC-011. Why is "one pool for everything" often a bad design?
- [ ] MT-EXEC-012. How do you shut down an `ExecutorService` gracefully?
- [ ] MT-EXEC-013. Why should custom executors use named threads through a `ThreadFactory`?
- [ ] MT-EXEC-014. How can thread pools create deadlocks or starvation?
- [ ] MT-EXEC-015. How would you isolate slow external calls with a separate executor?
- [ ] MT-EXEC-016. How do timeouts and retries relate to thread pool exhaustion?
- [ ] MT-EXEC-017. What metrics should you monitor for a thread pool, and how would you configure an executor in Spring Boot?

## 6. ForkJoinPool, parallel streams and liveness failures

- [ ] 🧠 MT-FJ-001. What is `ForkJoinPool`, and what is work stealing?
- [ ] 🧠 MT-FJ-002. How is `ForkJoinPool` related to parallel streams and `CompletableFuture`?
- [ ] 🧠 MT-FJ-003. Why should blocking I/O be avoided in the common `ForkJoinPool`?
- [ ] MT-FJ-004. Should backend request handling use parallel streams by default?
- [ ] 🧠 MT-FJ-005. How do custom `ForkJoinPool`s interact with parallel streams?
- [ ] MT-LIVE-001. What conditions are necessary for deadlock, and how can deadlocks be prevented?
- [ ] MT-LIVE-002. What are livelock and starvation?
- [ ] MT-LIVE-003. What is lock contention, and how can it be reduced?
- [ ] MT-LIVE-004. What are lost updates and thread starvation deadlocks?
- [ ] MT-LIVE-005. How can unbounded task submission or blocking inside a small pool cause cascading failures?
- [ ] MT-LIVE-006. How do timeouts and cancellation help preserve liveness?
- [ ] MT-LIVE-007. How would you review code for concurrency risks?

## 7. ThreadLocal, context propagation and backend boundaries

- [ ] MT-CTX-001. What is `ThreadLocal`, and when is it useful?
- [ ] MT-CTX-002. Why can `ThreadLocal` cause memory leaks or user-context leaks in thread pools?
- [ ] 🧠 MT-CTX-003. What is `InheritableThreadLocal`, and why can it be dangerous?
- [ ] MT-CTX-004. How do MDC, tracing context and security context usually propagate across async boundaries?
- [ ] MT-CTX-005. Why can async execution lose `SecurityContext` or transaction context?
- [ ] 🧠 MT-CTX-006. What are Scoped Values?
- [ ] 🧠 MT-CTX-007. When would Scoped Values be preferable to `ThreadLocal`?
- [ ] MT-CTX-008. How would you propagate request context safely across async boundaries?
- [ ] MT-CTX-009. Why should transaction context not be blindly propagated to another thread?
- [ ] MT-BE-001. Is a Spring singleton bean thread-safe by default, and why should it usually be stateless?
- [ ] MT-BE-002. How does `@Async` work in Spring, and why should its executor be configured explicitly?
- [ ] MT-BE-003. What can go wrong if async code is started inside a transaction or uses JPA entities across threads?

## 8. Virtual threads and structured concurrency

- [ ] MT-VT-001. What are virtual threads in Java 21+, and how are they different from platform threads?
- [ ] MT-VT-002. What are the main benefits of virtual threads, and when should you use them?
- [ ] MT-VT-003. When should you not use virtual threads, and why do they not make CPU-bound work faster?
- [ ] MT-VT-004. How do you create virtual threads, and what is `Executors.newVirtualThreadPerTaskExecutor()`?
- [ ] MT-VT-005. Should virtual threads be pooled, and how should concurrency be limited instead?
- [ ] 🧠 MT-VT-006. What are carrier threads, mounting and unmounting?
- [ ] 🧠 MT-VT-007. What is virtual-thread pinning?
- [ ] 🧠 MT-VT-008. What did JEP 491 in JDK 24 improve about virtual threads and `synchronized` code?
- [ ] 🧠 MT-VT-009. How do virtual threads interact with native calls or foreign functions?
- [ ] MT-VT-010. Why do virtual threads not remove the need for JDBC connection pool limits?
- [ ] 🧠 MT-VT-011. How do virtual threads affect `ThreadLocal` usage, observability and thread dumps?
- [ ] 🧠 MT-VT-012. How do virtual threads compare with reactive programming?
- [ ] 🧠 MT-VT-013. What is structured concurrency, and what problem does `StructuredTaskScope` solve?
- [ ] 🧠 MT-VT-014. Why is structured concurrency still a preview API in JDK 26, and what should production teams consider?
- [ ] 🧠 MT-VT-015. How do structured concurrency, cancellation and timeouts help avoid task leaks?

## 9. Diagnostics, testing and performance tuning

- [ ] MT-DIAG-001. How do you get and read a Java thread dump?
- [ ] MT-DIAG-002. How can a thread dump reveal deadlocks or thread pool starvation?
- [ ] MT-DIAG-003. What thread states are most important in production diagnostics?
- [ ] MT-DIAG-004. How can `jstack`, `jcmd` and Java Flight Recorder help with concurrency issues?
- [ ] MT-DIAG-005. What metrics should be collected for platform-thread pools and virtual-thread workloads?
- [ ] MT-DIAG-006. How can you detect lock contention or blocking calls in a latency-sensitive path?
- [ ] MT-DIAG-007. How would you test code that uses `ExecutorService` without making tests flaky?
- [ ] MT-DIAG-008. How would you test timeout, cancellation and `ThreadLocal` cleanup behavior?
- [ ] MT-DIAG-009. Why are `Thread.sleep()`-based tests flaky, and when is Awaitility useful?
- [ ] 🧠 MT-DIAG-010. What is jcstress, and when would you use it?
- [ ] MT-DIAG-011. Why are concurrency tests hard to make deterministic?
- [ ] MT-DIAG-012. How would you debug a request that hangs forever or high CPU caused by a busy retry loop?
- [ ] MT-PERF-001. What is Little's Law, and how can load testing prove a concurrency change helped?

## 10. Senior backend scenarios and traps

- [ ] MT-SCEN-001. A service has high latency and many threads in `WAITING` state. How would you investigate?
- [ ] MT-SCEN-002. A service has high CPU and many `RUNNABLE` threads. How would you investigate?
- [ ] MT-SCEN-003. A thread pool queue grows without bound. What could be wrong?
- [ ] MT-SCEN-004. A small fixed thread pool processes tasks that submit more tasks to the same pool and wait. What can go wrong?
- [ ] MT-SCEN-005. An endpoint fans out to 20 downstream services. How would you limit concurrency and handle timeouts?
- [ ] MT-SCEN-006. A downstream service becomes slow and your application runs out of threads. What design changes would you make?
- [ ] MT-SCEN-007. A scheduled job runs on every application instance and causes duplicate work. How would you fix it?
- [ ] MT-SCEN-008. A Spring singleton service stores mutable state in a field and fails under load. How would you refactor it?
- [ ] MT-SCEN-009. A cache implementation sometimes returns stale or corrupted values. How would you review it?
- [ ] MT-SCEN-010. A code path uses `CompletableFuture` but still blocks request threads. How would you find and fix it?
- [ ] MT-SCEN-011. A virtual-thread migration increased database timeouts. What would you check?
- [ ] MT-SCEN-012. A service uses `ThreadLocal` for user context and sometimes leaks users between requests. What happened?
- [ ] MT-SCEN-013. A transaction starts in one thread and async work runs in another. What problems can happen?
- [ ] MT-SCEN-014. A race condition only appears in production. How would you approach debugging and mitigation?
- [ ] MT-SCEN-015. A deadlock appears once a week. What data would you collect?
- [ ] MT-SCEN-016. A retry storm causes thread pool exhaustion. How would you redesign retries?
- [ ] MT-SCEN-017. A bounded executor rejects tasks during traffic spikes. How should the application respond?
- [ ] MT-SCEN-018. A Java service must shut down gracefully in Kubernetes. What should happen to executors and in-flight tasks?
- [ ] MT-SCEN-019. A rare memory visibility bug disappears when logging is added. Why can that happen?

## Counts

- Total questions: 149
- Priority 1 questions: 124
- Priority 2 questions: 25
