# JSON, Jackson and Serialization

## 1. JSON and serialization fundamentals

- [ ] JSON-FUND-001. What is the difference between serialization and deserialization?
- [ ] JSON-FUND-002. What is JSON, and why is it common in backend APIs?
- [ ] JSON-FUND-003. What JSON value types exist?
- [ ] JSON-FUND-004. How does the JSON data model differ from Java's type system?
- [ ] JSON-FUND-005. What is the difference between syntactically valid JSON and a semantically valid payload?
- [ ] JSON-FUND-006. What is JSON Schema, and when would you use it?
- [ ] JSON-FUND-007. What are the trade-offs of schema-less JSON in microservice communication?
- [ ] JSON-FUND-008. How does JSON differ from XML, Java native serialization, Protobuf and Avro?
- [ ] 🧠 JSON-FUND-009. What is JSON canonicalization, and why can field order or formatting matter for signatures and caching?
- [ ] JSON-FUND-010. How do character encoding and escaping affect JSON payloads?

## 2. Java native serialization and format choices

- [ ] JSON-SER-001. What is Java native serialization with `Serializable`?
- [ ] JSON-SER-002. How is Java native serialization different from JSON serialization with Jackson?
- [ ] JSON-SER-003. Why is Java native serialization usually avoided for public APIs and long-lived storage?
- [ ] JSON-SER-004. What is `serialVersionUID`, and what problem does it solve?
- [ ] JSON-SER-005. How do `transient`, `static` and `final` fields affect Java serialization?
- [ ] JSON-SER-006. How can Java serialization expose sensitive fields or break encapsulation?
- [ ] 🧠 JSON-SER-007. How can `writeObject`, `readObject`, `writeReplace` and `readResolve` customize Java serialization?
- [ ] 🧠 JSON-SER-008. How can deserialization bypass constructors, break invariants or break a Singleton?

## 3. Jackson architecture and processing models

- [ ] JSON-JACK-001. What problems does Jackson solve in Java backend applications?
- [ ] JSON-JACK-002. What is `ObjectMapper` / `JsonMapper`, and why should it usually be shared and configured centrally?
- [ ] JSON-JACK-003. What is Jackson data binding?
- [ ] JSON-JACK-004. What is the Jackson Tree Model with `JsonNode`, `ObjectNode` and `ArrayNode`?
- [ ] JSON-JACK-005. What is the Jackson Streaming API?
- [ ] JSON-JACK-006. When would you deserialize JSON into a DTO, a `Map`, or a `JsonNode`?
- [ ] JSON-JACK-007. What are `ObjectReader` and `ObjectWriter`, and when are they useful?
- [ ] JSON-JACK-008. What are Jackson modules, and why are they commonly registered with the mapper?
- [ ] JSON-JACK-009. How does Jackson discover properties from fields, getters, setters, constructors and records?
- [ ] JSON-JACK-010. Why is mutating a shared Jackson mapper at request time dangerous?
- [ ] 🧠 JSON-JACK-011. How does Jackson cache serializers and deserializers, and why can that affect performance?
- [ ] 🧠 JSON-JACK-012. How are serializers and deserializers selected when annotations, modules and declared types all apply?

## 4. Spring Boot and HTTP integration

- [ ] JSON-SPRING-001. How does Spring MVC use Jackson for `@RequestBody` and `@ResponseBody`?
- [ ] JSON-SPRING-002. What is a Jackson HTTP message converter responsible for?
- [ ] JSON-SPRING-003. How does content negotiation affect JSON request and response handling?
- [ ] JSON-SPRING-004. How should Jackson be configured centrally in a Spring Boot application?
- [ ] JSON-SPRING-005. What risks appear when defining a custom mapper bean in Spring Boot?
- [ ] JSON-SPRING-006. How can different HTTP clients, Feign clients or message producers accidentally use different JSON configuration?
- [ ] JSON-SPRING-007. How should JSON parse errors differ from validation errors in HTTP responses?
- [ ] 🧠 JSON-SPRING-008. How can reactive JSON encoding or decoding affect memory use and backpressure?

## 5. DTO design, API contracts and compatibility

- [ ] JSON-DTO-001. How should DTOs be designed for REST APIs?
- [ ] JSON-DTO-002. Why should request DTOs often differ from response DTOs?
- [ ] JSON-DTO-003. Why should APIs usually avoid exposing domain objects or persistence entities directly?
- [ ] JSON-DTO-004. How should APIs handle unknown JSON fields?
- [ ] JSON-DTO-005. What compatibility risks exist when adding new JSON fields?
- [ ] JSON-DTO-006. What compatibility risks exist when renaming or removing JSON fields?
- [ ] JSON-DTO-007. What is the difference between an omitted field and a field explicitly set to `null`?
- [ ] JSON-DTO-008. How should optional fields be represented in request and response payloads?
- [ ] JSON-DTO-009. Why are partial updates tricky when JSON fields can be absent or `null`?
- [ ] JSON-DTO-010. How do request DTOs help prevent over-posting or mass assignment vulnerabilities?
- [ ] JSON-DTO-011. How should enums be represented in public JSON APIs?
- [ ] JSON-DTO-012. How should API teams deprecate or version JSON fields?
- [ ] 🧠 JSON-DTO-013. How can projections, field selection or flexible JSON shapes complicate authorization and compatibility?

## 6. Jackson annotations, naming and inclusion

- [ ] JSON-ANN-001. What do `@JsonProperty` and `@JsonAlias` do?
- [ ] JSON-ANN-002. What do `@JsonIgnore` and `@JsonIgnoreProperties` do?
- [ ] JSON-ANN-003. What does `@JsonInclude` control?
- [ ] JSON-ANN-004. What does `@JsonFormat` control?
- [ ] JSON-ANN-005. What are Jackson naming strategies, and when would you use `@JsonNaming`?
- [ ] JSON-ANN-006. How can Jackson control null handling with setters or configuration?
- [ ] JSON-ANN-007. When would you use `@JsonAnyGetter` or `@JsonAnySetter`?
- [ ] JSON-ANN-008. When would you use `@JsonUnwrapped`, and what compatibility risks does it create?
- [ ] JSON-ANN-009. What is `@JsonView`, and why should it be used carefully?
- [ ] JSON-ANN-010. How do custom serializers and deserializers work?
- [ ] JSON-ANN-011. When would you use Jackson mix-ins instead of annotations on the target class?
- [ ] 🧠 JSON-ANN-012. How can annotation-heavy DTOs create maintenance problems or leak transport concerns into the domain model?

## 7. Creators, immutability, records and validation

- [ ] JSON-CREATE-001. How does Jackson deserialize a mutable POJO with a default constructor and setters?
- [ ] JSON-CREATE-002. When would you use `@JsonCreator`?
- [ ] JSON-CREATE-003. What is the difference between constructor-based and static-factory deserialization?
- [ ] JSON-CREATE-004. How do records work with Jackson serialization and deserialization?
- [ ] JSON-CREATE-005. How can immutable DTOs with final fields be deserialized safely?
- [ ] JSON-CREATE-006. How can Jackson work with builder-based DTOs, including Lombok builders?
- [ ] JSON-CREATE-007. How should default values be handled during deserialization?
- [ ] JSON-CREATE-008. How should required fields be validated after deserialization?
- [ ] JSON-CREATE-009. Why is reusing domain objects as request DTOs dangerous?
- [ ] 🧠 JSON-CREATE-010. How can deserialization break business invariants when validation is placed in the wrong layer?

## 8. Dates, numbers, enums and generic types

- [ ] JSON-TYPE-001. How should `LocalDate`, `Instant` and `OffsetDateTime` be serialized?
- [ ] JSON-TYPE-002. Why should API date/time formats be explicit?
- [ ] JSON-TYPE-003. How should time zones and offsets be handled in JSON APIs?
- [ ] JSON-TYPE-004. Why do Java Time types need appropriate Jackson module/configuration support?
- [ ] JSON-TYPE-005. Why is `BigDecimal` usually preferred for money in JSON payloads?
- [ ] JSON-TYPE-006. What precision problems can appear with JSON floating-point numbers?
- [ ] JSON-TYPE-007. Why can large numeric IDs be dangerous for JavaScript clients?
- [ ] JSON-TYPE-008. Should enums be serialized as names, codes or objects?
- [ ] JSON-TYPE-009. How should APIs handle unknown enum values from clients or external systems?
- [ ] JSON-TYPE-010. How should `Optional`, `null`, empty collections and default values be handled in JSON?
- [ ] JSON-TYPE-011. How do generic types lose type information during deserialization?
- [ ] JSON-TYPE-012. How do `TypeReference` and `JavaType` help with generic deserialization?
- [ ] 🧠 JSON-TYPE-013. How can default locale, default charset or default time zone leak into JSON behavior?

## 9. Polymorphism and deserialization security

- [ ] JSON-SEC-001. When would an API need polymorphic JSON?
- [ ] JSON-SEC-002. How do `@JsonTypeInfo` and `@JsonSubTypes` support polymorphic deserialization?
- [ ] JSON-SEC-003. Why is exposing Java class names in JSON type metadata dangerous?
- [ ] JSON-SEC-004. How does Jackson default typing become a security risk?
- [ ] JSON-SEC-005. What is an allow-list approach for polymorphic subtype validation?
- [ ] JSON-SEC-006. Why should untrusted JSON usually not be deserialized into arbitrary `Object` graphs?
- [ ] JSON-SEC-007. What is a deserialization gadget at a high level?
- [ ] JSON-SEC-008. How can large, deeply nested or malformed JSON become a resource-exhaustion risk?
- [ ] 🧠 JSON-SEC-009. How can duplicate keys, trailing tokens or implicit coercions create security or correctness bugs?
- [ ] 🧠 JSON-SEC-010. How can polymorphic JSON APIs hurt backward compatibility and client portability?

## 10. JPA, object graphs and serialization boundaries

- [ ] JSON-JPA-001. Why is serializing JPA entities directly dangerous?
- [ ] JSON-JPA-002. How can JSON serialization accidentally trigger lazy loading?
- [ ] JSON-JPA-003. How can JSON serialization create N+1 query problems?
- [ ] JSON-JPA-004. How do cyclic references happen in JSON serialization?
- [ ] JSON-JPA-005. How do `@JsonManagedReference`, `@JsonBackReference` and `@JsonIdentityInfo` address cyclic references?
- [ ] JSON-JPA-006. How can Hibernate proxies affect JSON field names, types and error messages?
- [ ] JSON-JPA-007. Why can Open Session in View hide serialization and fetching problems?
- [ ] 🧠 JSON-JPA-008. How would you debug database queries triggered during JSON serialization?

## 11. Large payloads, performance and memory

- [ ] JSON-PERF-001. How should large JSON request payloads be handled?
- [ ] JSON-PERF-002. When should you use streaming JSON parsing instead of full data binding?
- [ ] JSON-PERF-003. What memory costs come from reading a whole payload into `JsonNode` or `String`?
- [ ] JSON-PERF-004. Why can `writeValueAsString()` be worse than streaming directly to an output stream?
- [ ] JSON-PERF-005. How does reusing a configured mapper affect performance?
- [ ] JSON-PERF-006. When can reusable `ObjectReader` and `ObjectWriter` instances help?
- [ ] JSON-PERF-007. How can JSON serialization/deserialization create allocation spikes and GC pressure?
- [ ] JSON-PERF-008. How do payload shape, pagination and field selection affect JSON performance?
- [ ] JSON-PERF-009. How would you measure whether JSON processing is actually the bottleneck?
- [ ] 🧠 JSON-PERF-010. What role can Jackson performance modules or generated accessors play, and why should they be benchmarked?

## 12. Testing, debugging and production scenarios

- [ ] JSON-TEST-001. How should JSON serialization and deserialization behavior be tested?
- [ ] JSON-TEST-002. How can Spring Boot tests verify the real application JSON mapper configuration?
- [ ] JSON-TEST-003. What are the strengths and weaknesses of snapshot or golden-file JSON tests?
- [ ] JSON-TEST-004. How do contract tests help catch JSON compatibility breaks between services?
- [ ] JSON-TEST-005. What invalid payload cases should be tested for JSON endpoints?
- [ ] JSON-TEST-006. How would you debug `UnrecognizedPropertyException`?
- [ ] JSON-TEST-007. How would you debug `MismatchedInputException` or `InvalidDefinitionException`?
- [ ] JSON-TEST-008. How would you debug a wrong date/time or time zone in a JSON response?
- [ ] JSON-TEST-009. What should a service do if an external API adds, removes or changes a JSON field?
- [ ] JSON-TEST-010. How should parse errors and raw payloads be logged without leaking sensitive data?
- [ ] JSON-TEST-011. What should be checked before upgrading Jackson, Spring Boot or a JSON-related dependency?
- [ ] 🧠 JSON-TEST-012. How would you investigate slow JSON serialization in production?

## Counts

- Total questions: 126
- Priority 1 questions: 111
- Priority 2 questions: 15
