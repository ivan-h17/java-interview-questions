# Hibernate / JPA

## 1. ORM, JPA and Hibernate fundamentals

- [ ] JPA-FUND-001. What problem does ORM solve, and what problems does it not solve?
- [ ] JPA-FUND-002. What are the main benefits and risks of using ORM in a Java backend application?
- [ ] JPA-FUND-003. What is the difference between JPA / Jakarta Persistence, Hibernate and provider-specific behavior?
- [ ] JPA-FUND-004. What are `EntityManagerFactory`, `EntityManager`, `SessionFactory` and `Session` responsible for?
- [ ] JPA-FUND-005. Why should backend developers still understand SQL when using JPA/Hibernate?
- [ ] JPA-FUND-006. What is the difference between an entity, a DTO and a value object?
- [ ] 🧠 JPA-FUND-007. What changed when JPA moved from `javax.persistence` to `jakarta.persistence`, and when is `persistence.xml` still relevant?

## 2. Mapping, identity and entity design

- [ ] JPA-MAP-001. What requirements must a JPA entity class satisfy?
- [ ] JPA-MAP-002. How do field access, property access and `@Access` differ?
- [ ] JPA-MAP-003. Why should entity mapping metadata not be treated as a replacement for database schema design?
- [ ] JPA-MAP-004. What is the difference between JPA nullability metadata and real database constraints?
- [ ] JPA-MAP-005. When would you use an embeddable value object instead of a separate entity?
- [ ] 🧠 JPA-MAP-006. Can Java records be used with JPA entities, IDs or embeddables?
- [ ] JPA-MAP-007. How are common Java basic types, dates, timestamps and enums mapped?
- [ ] JPA-MAP-008. Why is `EnumType.ORDINAL` risky?
- [ ] JPA-MAP-009. When would you use an attribute converter, JSON mapping or a database-specific Hibernate type?
- [ ] JPA-ID-001. What is entity identity, and how is it different from Java object identity?
- [ ] JPA-ID-002. What identifier generation strategies does JPA support, and how do you choose between them?
- [ ] JPA-ID-003. How do `IDENTITY` and `SEQUENCE` differ for batching and insert performance?
- [ ] 🧠 JPA-ID-004. How do sequence allocation size and pooled optimizers improve insert throughput?
- [ ] JPA-ID-005. What are the trade-offs of UUIDs versus sequential numeric identifiers?
- [ ] JPA-ID-006. What are natural keys, surrogate keys, business keys and assigned identifiers?
- [ ] 🧠 JPA-ID-007. When are composite identifiers useful, and when should they be avoided?
- [ ] JPA-EQ-001. How should `equals()` and `hashCode()` be implemented for JPA entities?
- [ ] JPA-EQ-002. Why are generated IDs and mutable business keys problematic for entity equality?
- [ ] 🧠 JPA-EQ-003. How do Hibernate proxies affect `equals()`, `hashCode()` and `getClass()` checks?
- [ ] JPA-EQ-004. Why are Lombok `@Data`, `@EqualsAndHashCode` and `@ToString` risky on entities?
- [ ] JPA-EQ-005. How do you design entities to protect invariants without fighting JPA?

## 3. Persistence context, lifecycle and core operations

- [ ] JPA-PC-001. What is a persistence context, and why is it called the first-level cache?
- [ ] JPA-PC-002. What are the transient, managed, detached and removed entity states?
- [ ] JPA-PC-003. What do `persist()`, `find()`, `getReference()`, `remove()` and `refresh()` do?
- [ ] JPA-PC-004. What is the difference between `persist()` and `merge()`?
- [ ] 🧠 JPA-PC-005. How do Hibernate `Session.save()` and `Session.update()` differ from portable JPA operations?
- [ ] JPA-PC-006. What do `detach()`, `clear()`, `contains()` and `close()` do?
- [ ] JPA-PC-007. What happens to managed entities when a transaction commits or rolls back?
- [ ] 🧠 JPA-PC-008. What is the difference between transaction-scoped and extended persistence contexts?
- [ ] JPA-PC-009. How does Spring usually bind an `EntityManager` to a transaction?
- [ ] JPA-PC-010. Why should a persistence context usually not be kept open for too long?
- [ ] JPA-PC-011. How do you process large result sets without filling the persistence context forever?
- [ ] JPA-PC-012. Why can detached entities be dangerous at API boundaries?
- [ ] JPA-PC-013. What happens when a lazy proxy is accessed outside an open persistence context?
- [ ] JPA-PC-014. Are `EntityManager` and Hibernate `Session` thread-safe?
- [ ] 🧠 JPA-PC-015. How can persistence exceptions leave a Hibernate `Session` in an unusable state?

## 4. Dirty checking, flush and transaction boundaries

- [ ] JPA-FLUSH-001. What is dirty checking in Hibernate?
- [ ] 🧠 JPA-FLUSH-002. How can bytecode enhancement change dirty tracking behavior?
- [ ] JPA-FLUSH-003. How does flush differ from commit?
- [ ] JPA-FLUSH-004. Why can Hibernate flush before transaction commit or before a query?
- [ ] 🧠 JPA-FLUSH-005. What are common flush modes, and when is manual flush risky?
- [ ] JPA-FLUSH-006. What are transactional write-behind and SQL statement ordering?
- [ ] JPA-FLUSH-007. When is explicit `flush()` useful, and when does it hurt performance?
- [ ] 🧠 JPA-FLUSH-008. What are the pros and cons of Hibernate `@DynamicUpdate`?
- [ ] JPA-FLUSH-009. How can modifying a managed entity in a read path still produce an update?
- [ ] JPA-FLUSH-010. What is the difference between a read-only transaction and an immutable entity?
- [ ] JPA-FLUSH-011. Why do bulk JPQL updates bypass the persistence context, and how do you handle stale managed entities afterward?
- [ ] JPA-FLUSH-012. How would you debug unexpected `INSERT`, `UPDATE` or `DELETE` statements?
- [ ] JPA-TX-001. Where should JPA transaction boundaries usually be placed?
- [ ] JPA-TX-002. How do database isolation, persistence context state and JPA lock modes differ?
- [ ] 🧠 JPA-TX-003. Why can `REQUIRES_NEW` inside an existing transaction exhaust the connection pool?

## 5. Fetching, associations and graph design

- [ ] JPA-FETCH-001. What is the N+1 query problem?
- [ ] JPA-FETCH-002. How would you detect N+1 queries in code, tests or production?
- [ ] JPA-FETCH-003. How can fetch joins, entity graphs, batch fetching and projections solve different N+1 cases?
- [ ] JPA-FETCH-004. What is the difference between lazy and eager loading?
- [ ] JPA-FETCH-005. What is `LazyInitializationException`, and how do you avoid it correctly?
- [ ] JPA-FETCH-006. What is Open Session in View, and why is it controversial?
- [ ] JPA-FETCH-007. How does `JOIN FETCH` work, and what are its risks?
- [ ] JPA-FETCH-008. Why can collection fetch joins break pagination or produce duplicates?
- [ ] JPA-FETCH-009. How does batch fetching with `@BatchSize` or `hibernate.default_batch_fetch_size` work?
- [ ] 🧠 JPA-FETCH-010. What is subselect fetching, and when can it help?
- [ ] 🧠 JPA-FETCH-011. What are entity graphs, `fetchgraph` and `loadgraph`?
- [ ] JPA-FETCH-012. How would you design fetch plans for list pages versus detail pages?
- [ ] JPA-FETCH-013. When should you use DTO projections instead of entities?
- [ ] JPA-FETCH-014. How do you avoid Cartesian product explosions in fetch joins?
- [ ] 🧠 JPA-FETCH-015. What is `MultipleBagFetchException`, and how do you avoid it?
- [ ] 🧠 JPA-FETCH-016. What is the difference between a bag, list and set in Hibernate collections?
- [ ] 🧠 JPA-FETCH-017. How do lazy proxies affect `equals()`, `toString()`, logging and JSON serialization?
- [ ] JPA-FETCH-018. How would you troubleshoot a slow endpoint caused by Hibernate fetching?
- [ ] JPA-REL-001. What relationship types does JPA support, and which side owns a relationship?
- [ ] JPA-REL-002. How should `@ManyToOne` and `@OneToMany` usually be modeled?
- [ ] JPA-REL-003. What are `mappedBy`, the owning side and bidirectional helper methods for?
- [ ] JPA-REL-004. When should a direct `@ManyToMany` be replaced by an explicit join entity?
- [ ] JPA-REL-005. What association mapping defaults and collection choices commonly cause bugs?

## 6. Cascades, locking and consistency

- [ ] JPA-CASCADE-001. When are cascading operations useful, and when are they dangerous?
- [ ] JPA-CASCADE-002. Why is `CascadeType.REMOVE` dangerous on `@ManyToMany`?
- [ ] JPA-CASCADE-003. How is `orphanRemoval = true` different from `CascadeType.REMOVE`?
- [ ] JPA-CASCADE-004. How do orphan removal, aggregate ownership and database `ON DELETE CASCADE` relate?
- [ ] JPA-CASCADE-005. Why can cascading `merge()` over a detached graph be expensive or unsafe?
- [ ] JPA-LOCK-001. What is optimistic locking, and how does `@Version` work?
- [ ] JPA-LOCK-002. What is a lost update, and how can JPA help prevent it?
- [ ] JPA-LOCK-003. Can optimistic locking protect against all consistency problems?
- [ ] JPA-LOCK-004. How should an application handle `OptimisticLockException`?
- [ ] JPA-LOCK-005. How do detached DTO updates interact with optimistic locking?
- [ ] JPA-LOCK-006. What is pessimistic locking, and what do the main pessimistic lock modes mean?
- [ ] JPA-LOCK-007. When should pessimistic locking be used, and what risks does it introduce?
- [ ] 🧠 JPA-LOCK-008. How do lock timeouts and JPA lock modes map to database behavior?
- [ ] JPA-LOCK-009. What is the difference between database isolation level and JPA lock mode?
- [ ] 🧠 JPA-LOCK-010. How do bulk updates interact with optimistic locking?
- [ ] JPA-LOCK-011. How would you design concurrent update handling for an aggregate?
- [ ] 🧠 JPA-LOCK-012. How can constraints or atomic database updates prevent concurrency anomalies that JPA alone cannot?

## 7. Queries, projections, pagination and bulk work

- [ ] JPA-QUERY-001. What is JPQL, and how is it different from SQL?
- [ ] 🧠 JPA-QUERY-002. What is HQL, and what portability trade-offs does it introduce?
- [ ] JPA-QUERY-003. When should native SQL be preferred over JPQL?
- [ ] JPA-QUERY-004. How does parameter binding help prevent SQL injection?
- [ ] JPA-QUERY-005. What is the difference between `JOIN` and `JOIN FETCH` in JPQL?
- [ ] JPA-QUERY-006. What are constructor expressions and DTO projections?
- [ ] 🧠 JPA-QUERY-007. What are the pros and cons of Criteria API?
- [ ] 🧠 JPA-QUERY-008. How would you choose between JPQL, Criteria, QueryDSL, jOOQ and native SQL?
- [ ] JPA-QUERY-009. How do you express dynamic optional filters safely?
- [ ] 🧠 JPA-QUERY-010. How do you call database functions from JPQL?
- [ ] JPA-QUERY-011. How can JPQL hide inefficient SQL, and how do you debug it?
- [ ] JPA-QUERY-012. Why should filtering and sorting usually happen in the database rather than in Java?
- [ ] JPA-DTO-001. What projection types exist in JPA and Spring Data JPA, and what are their limitations?
- [ ] JPA-DTO-002. How would you design a query model for a list endpoint versus a detail endpoint?
- [ ] JPA-PAGE-001. How does offset pagination work, and why can it become slow?
- [ ] JPA-PAGE-002. What is keyset pagination, and when is it a better fit?
- [ ] JPA-PAGE-003. Why is stable ordering important for pagination correctness?
- [ ] JPA-PAGE-004. How can `Page` count queries become a performance bottleneck, and when would you use `Slice`?
- [ ] JPA-BULK-001. How do JPQL bulk updates, deletes and Hibernate batching behave with the persistence context?
- [ ] JPA-BULK-002. How do JDBC batching settings and ID generation strategy interact?

## 8. Spring Data JPA integration

- [ ] JPA-SPRING-001. How do derived query methods work, and when do they become a liability?
- [ ] JPA-SPRING-002. When should derived queries be replaced with `@Query`, Specifications, native SQL or another query tool?
- [ ] JPA-SPRING-003. What is `@Modifying`, and why do modifying queries need transactions?
- [ ] JPA-SPRING-004. What are `@EntityGraph`, projections and Specifications in Spring Data JPA?
- [ ] JPA-SPRING-005. What transactional behavior do Spring Data repository methods have by default?
- [ ] JPA-SPRING-006. Where should transaction boundaries usually live: repository or service layer?
- [ ] JPA-SPRING-007. Why can `@Transactional(readOnly = true)` matter for JPA/Hibernate?
- [ ] JPA-SPRING-008. What is Open EntityManager in View / Open Session in View in Spring Boot?
- [ ] JPA-SPRING-009. How would you review repository code for N+1 queries, count-query bottlenecks and transaction leaks?

## 9. Caching and advanced Hibernate features

- [ ] 🧠 JPA-CACHE-001. What is the second-level cache in Hibernate, and when should it be used or avoided?
- [ ] 🧠 JPA-CACHE-002. What are cache regions, cache providers and cache concurrency strategies?
- [ ] 🧠 JPA-CACHE-003. Why is second-level cache risky when other applications update the same database?
- [ ] 🧠 JPA-CACHE-004. What is the difference between query cache and entity cache, and how does invalidation work conceptually?
- [ ] JPA-CACHE-005. How do JPA/Hibernate caches compare to Redis or application-level response caches?
- [ ] JPA-CACHE-006. What cache metrics would you monitor, and can cache solve N+1 queries?
- [ ] 🧠 JPA-ADV-001. What is Hibernate `StatelessSession`, and when might it help?
- [ ] 🧠 JPA-ADV-002. What inheritance strategies does JPA support, and what are their trade-offs?
- [ ] JPA-ADV-003. How is `@MappedSuperclass` different from entity inheritance, and when should you avoid JPA inheritance?
- [ ] 🧠 JPA-ADV-004. What are the risks of provider-specific annotations?
- [ ] 🧠 JPA-ADV-005. How would you implement soft delete with Hibernate, and what are the traps?
- [ ] 🧠 JPA-ADV-006. How can Hibernate filters or SQL restrictions support tenant, security or visibility predicates?
- [ ] 🧠 JPA-ADV-007. When would you use Envers or another auditing approach?
- [ ] 🧠 JPA-ADV-008. What are entity listeners and lifecycle callbacks, and when are they risky?

## 10. API boundaries, serialization and DTO mapping

- [ ] JPA-API-001. Why should entities usually not be returned directly from REST controllers?
- [ ] JPA-API-002. What can go wrong when Jackson serializes lazy or bidirectional associations?
- [ ] JPA-API-003. Why are Jackson annotations on entities often not the best design?
- [ ] JPA-API-004. How do DTOs protect persistence boundaries?
- [ ] JPA-API-005. How would you map entities to DTOs, and what are the trade-offs of MapStruct versus manual mapping?
- [ ] JPA-API-006. How do you handle partial updates without blindly merging detached entities?
- [ ] JPA-API-007. Why can `merge()` with request DTO data be dangerous?
- [ ] JPA-API-008. How do you avoid mass assignment vulnerabilities with entities?
- [ ] JPA-API-009. How should API validation interact with database constraints?
- [ ] JPA-API-010. How do you avoid exposing internal IDs, audit fields, version fields or lazy-loaded internals?
- [ ] JPA-API-011. What is the difference between command DTOs and query DTOs?
- [ ] JPA-API-012. How does transaction boundary relate to DTO creation and lazy-loading prevention?

## 11. Testing, migrations and production safety

- [ ] JPA-TEST-001. How do you test JPA repository code?
- [ ] JPA-TEST-002. What is the difference between `@DataJpaTest` and a full integration test?
- [ ] JPA-TEST-003. Why can H2 tests differ from PostgreSQL production behavior, and when should you use Testcontainers?
- [ ] JPA-TEST-004. How do you test mappings and database constraints?
- [ ] JPA-TEST-005. How do you test N+1 query prevention and SQL statement counts?
- [ ] JPA-TEST-006. How do you test optimistic and pessimistic locking?
- [ ] JPA-TEST-007. Why can rollback-after-test hide real transaction behavior?
- [ ] JPA-TEST-008. When should tests explicitly call `flush()` and `clear()`?
- [ ] JPA-TEST-009. How do you test custom JPQL, native queries and projections?
- [ ] JPA-TEST-010. How do you test database migrations together with JPA mappings?
- [ ] JPA-MIG-001. What is Hibernate DDL auto-generation, and why is `ddl-auto=update` dangerous in production?
- [ ] JPA-MIG-002. Why should Flyway or Liquibase own production schema changes?
- [ ] JPA-MIG-003. How do you safely add a non-null column used by an entity?
- [ ] JPA-MIG-004. How do you safely rename a column, change a relationship mapping or add an index?
- [ ] JPA-MIG-005. How can entity changes break old application versions during rolling deployment?
- [ ] JPA-MIG-006. How do you handle enum changes and large data backfills safely?
- [ ] 🧠 JPA-MIG-007. How can Hibernate naming strategies affect migrations and startup validation?

## 12. Performance, observability and senior scenarios

- [ ] JPA-PERF-001. What are the most common Hibernate performance problems?
- [ ] JPA-PERF-002. How do you enable SQL logging safely without leaking sensitive data?
- [ ] 🧠 JPA-PERF-003. What Hibernate, database and connection pool metrics would you collect, and what is the Hibernate statistics API useful for?
- [ ] JPA-PERF-004. How do you connect Hibernate-generated SQL with PostgreSQL `EXPLAIN ANALYZE`?
- [ ] JPA-PERF-005. Why can a query be fast in development but slow in production?
- [ ] JPA-PERF-006. What are over-fetching and under-fetching?
- [ ] JPA-PERF-007. How can large transactions and long persistence contexts hurt performance?
- [ ] JPA-PERF-008. How do connection pools and application thread counts limit JPA scalability?
- [ ] JPA-PERF-009. How do indexes, column types and functions in `WHERE` clauses interact with ORM-generated queries?
- [ ] 🧠 JPA-PERF-010. How do traces help diagnose ORM latency, and how would you prove that a Hibernate optimization helped?
- [ ] JPA-SCENARIO-001. A list endpoint executes 501 SQL queries for 500 rows. How do you diagnose and fix it?
- [ ] JPA-SCENARIO-002. A `@Transactional` service returns DTOs but sometimes throws `LazyInitializationException`. What went wrong?
- [ ] JPA-SCENARIO-003. A production endpoint became slow after adding a new `@OneToMany` field to a DTO. What do you check?
- [ ] JPA-SCENARIO-004. A pageable query with `JOIN FETCH` returns duplicates or wrong pages. How do you redesign it?
- [ ] JPA-SCENARIO-005. An entity update unexpectedly updates many columns. How do you investigate?
- [ ] JPA-SCENARIO-006. A batch import becomes slower over time and memory usage grows. What is likely wrong?
- [ ] JPA-SCENARIO-007. A migration changes an enum mapping and data becomes corrupted. What probably happened?
- [ ] JPA-SCENARIO-008. Two users update the same entity and one update is silently lost. How do you fix it?
- [ ] JPA-SCENARIO-009. A transaction with `REQUIRES_NEW` causes connection pool exhaustion. Why?
- [ ] JPA-SCENARIO-010. A method marked `@Transactional(readOnly = true)` still causes writes. What do you check?
- [ ] JPA-SCENARIO-011. A production deadlock involves Hibernate-generated SQL. How do you debug and fix it?
- [ ] 🧠 JPA-SCENARIO-012. A second-level cache returns stale data after another service updates the same table. What are options?
- [ ] JPA-SCENARIO-013. A JSON serializer causes thousands of lazy loads. How do you prevent it?
- [ ] JPA-SCENARIO-014. A many-to-many mapping starts deleting shared records. What likely caused it?
- [ ] JPA-SCENARIO-015. A service uses `merge()` on a request body entity and overwrites fields with nulls. How do you redesign it?
- [ ] JPA-SCENARIO-016. An entity in a `HashSet` disappears after being persisted. What could be wrong?
- [ ] JPA-SCENARIO-017. A query uses an entity graph and suddenly returns much more data. What do you check?
- [ ] JPA-SCENARIO-018. A service needs to export millions of rows. Would you use entities, projections, streaming or native SQL?
- [ ] 🧠 JPA-SCENARIO-019. A service needs strict write consistency across two aggregates. Would you use one JPA transaction, optimistic locking, pessimistic locking or outbox?
- [ ] JPA-SCENARIO-020. A team wants to remove all database constraints because JPA validates entities. How do you argue against it?

## Counts

- Total questions: 200
- Priority 1 questions: 160
- Priority 2 questions: 40
