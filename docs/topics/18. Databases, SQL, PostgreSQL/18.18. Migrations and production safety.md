# Migrations and production safety

<a id="db-mig-001"></a>

<details>
<summary><strong>DB-MIG-001. What is a database migration?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A database migration is a versioned change to database schema or data that is applied in a controlled, repeatable way.</p>
</td>
<td valign="top" width="50%">
<p>Миграция базы данных — версионированное изменение схемы или данных, применяемое контролируемым и повторяемым способом.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Migrations include actions like creating tables, adding columns, creating indexes, changing constraints, or backfilling data.</p>
<p>They are stored in version control and applied consistently across environments.</p>
</td>
<td valign="top" width="50%">
<p>Миграции включают создание таблиц, добавление колонок, создание индексов, изменение ограничений или дозаполнение (backfill) данных.</p>
<p>Они хранятся в version control и применяются consistently между окружениями.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A migration system tracks which migrations were applied, prevents accidental reordering, and makes deployments reproducible.</p>
<p>Schema changes are part of application delivery and need review like code.</p>
</td>
<td valign="top" width="50%">
<p>Migration system tracks применённые миграции, prevents случайное изменение порядка и делает деплои воспроизводимыми.</p>
<p>Изменения схемы — часть поставки приложения и должны ревьюиться как код.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, migrations must be safe for live traffic: avoid long locks, table rewrites, unbounded backfills and breaking old app versions.</p>
<p>Large migrations need rollout plans, monitoring, and rollback/recovery strategy.</p>
</td>
<td valign="top" width="50%">
<p>В production миграции должны быть безопасными для живого трафика: избегать долгих блокировок, переписывания таблиц, неограниченных backfill-ов и поломки старых версий приложения.</p>
<p>Большие миграции требуют план rollout-а, monitoring и стратегию отката и восстановления.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Migrations are not only DDL; they can include data changes.</li>
<li>A migration that works locally can lock production.</li>
<li>Manual hotfixes cause schema drift.</li>
<li>Rollback is not always the reverse SQL.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Миграции — не только DDL; могут включать изменения данных.</li>
<li>Migration that works locally can lock production.</li>
<li>Ручные hotfix-ы вызывают расхождение схем (схемы drift).</li>
<li>Откат не всегда обратный SQL.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: What is a database migration?<br>A: A versioned, repeatable database schema/data change.</p>
</td>
<td valign="top" width="50%">
<p>Q: Что такое миграция базы данных?<br>A: Версионированное и повторяемое изменение схемы/данных базы.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-002"></a>

<details>
<summary><strong>DB-MIG-002. What are Flyway and Liquibase used for?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Flyway and Liquibase are tools for managing versioned database migrations across environments.</p>
</td>
<td valign="top" width="50%">
<p>Flyway и Liquibase — tools для управления версионированными миграциями базы между окружениями.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>They keep a history table of applied migrations and run pending changes in order.</p>
<p>Flyway is commonly SQL-script oriented and convention-based. Liquibase is more feature-rich and supports multiple changelog formats.</p>
</td>
<td valign="top" width="50%">
<p>Они хранят таблицу истории применённые миграции и запускают ожидающие изменения по порядку.</p>
<p>Flyway обычно ориентирован на SQL-скрипты and основан на соглашениях. Liquibase более более функциональный и поддерживает несколько форматов changelog-а.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Both help teams avoid manual schema drift and make deployments reproducible.</p>
<p>They do not automatically make a migration zero-downtime; the migration content still matters.</p>
</td>
<td valign="top" width="50%">
<p>Оба помогают избежать ручного расхождение схем (схемы drift) и make deployments reproducible.</p>
<p>Они не делают migration zero-downtime автоматически; содержание миграции всё равно важно.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In Java/Spring systems, these tools often run during application startup or CI/CD pipeline. For risky migrations, running as a separate controlled deployment step may be safer.</p>
<p>Migration failures must be observable and stop deployment.</p>
</td>
<td valign="top" width="50%">
<p>В Java/Spring-системах эти tools часто run during старте приложения или CI/CD pipeline. Для risky миграции безопаснее отдельный контролируемый шаг деплоя.</p>
<p>Ошибки миграций должны быть наблюдаемыми и останавливать деплой.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Do not run heavy migrations blindly on app startup.</li>
<li>Version scripts carefully.</li>
<li>Never edit already-applied migrations without a controlled repair process.</li>
<li>Tooling does not replace production-safe DDL knowledge.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Не слепо запускать тяжёлые миграции on app startup.</li>
<li>Version scripts carefully.</li>
<li>Не редактировать уже применённые миграции без контролируемого repair-процесса.</li>
<li>Tooling не заменяет знание безопасного production DDL.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: What do Flyway/Liquibase manage?<br>A: Versioned database migrations and applied-migration history.</p>
</td>
<td valign="top" width="50%">
<p>Q: Чем управляют Flyway/Liquibase?<br>A: Versioned database миграции и history применённые миграции.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-003"></a>

<details>
<summary><strong>DB-MIG-003. What is a zero-downtime migration?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A zero-downtime migration changes schema or data without breaking old or new application versions while traffic continues.</p>
</td>
<td valign="top" width="50%">
<p>Миграция без простоя (zero-downtime migration) меняет схемы или данных, не ломая старые и новые версии приложения при продолжающемся traffic.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>The core idea is backward and forward compatibility. During deployment, old and new code may run at the same time, so the database must support both.</p>
<p>Typical strategy: expand schema, deploy code that supports both, backfill, switch reads/writes, then contract old schema later.</p>
</td>
<td valign="top" width="50%">
<p>Ключевая идея — backward и forward compatibility. Во время деплоя старый и новый код могут работать одновременно, поэтому database должна поддерживать оба варианта.</p>
<p>Типичная стратегия: расширить схему, задеплоить код с поддержкой обоих вариантов, дозаполнение (backfill), переключить чтение/запись, затем позже удалить старую схему.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Zero-downtime migrations avoid destructive changes in the first step. Do not rename/drop/change meaning of a column while old code still uses it.</p>
<p>Data backfills should be chunked and observable.</p>
</td>
<td valign="top" width="50%">
<p>Zero-downtime миграции avoid destructive changes at first step. Не rename/drop/change meaning column, пока old code uses it.</p>
<p>Data backfills should be chunked and наблюдаемыми.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, plan deployments as multiple releases. Use feature flags, dual-write/dual-read carefully, validation queries, and rollback points.</p>
<p>Define what zero-downtime means for your SLA: no app errors, no write blocking, no read inconsistency.</p>
</td>
<td valign="top" width="50%">
<p>В production plan deployments as multiple releases. Use feature flags, dual-write/dual-read carefully, validation queries и rollback points.</p>
<p>Define what zero-downtime means для SLA: no app errors, no write blocking, no read inconsistency.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Zero-downtime is a process, not one SQL trick.</li>
<li>Rollback after partial data migration may be hard.</li>
<li>Dual writes can create inconsistencies.</li>
<li>Long locks can still cause downtime.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Zero-downtime — process, не one SQL trick.</li>
<li>Откат после partial данных migration can be hard.</li>
<li>Dual writes могут create inconsistencies.</li>
<li>Long locks всё равно cause downtime.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: What is zero-downtime migration?<br>A: A compatible multi-step change that keeps old and new app versions working during traffic.</p>
</td>
<td valign="top" width="50%">
<p>Q: Что такое миграция без простоя (zero-downtime migration)?<br>A: Compatible multi-step change, при котором old/new app versions keep working during traffic.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-004"></a>

<details>
<summary><strong>DB-MIG-004. What is expand-and-contract migration?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Expand-and-contract is a multi-step migration pattern: add new schema first, migrate usage, then remove old schema later.</p>
</td>
<td valign="top" width="50%">
<p>Expand-and-contract — multi-step migration pattern: сначала add new схемы, migrate usage, потом remove old схемы later.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Expand phase adds backward-compatible objects: new columns, tables, indexes or constraints. Migration phase updates code and backfills data. Contract phase removes old objects after they are unused.</p>
<p>It is the standard approach for zero-downtime schema evolution.</p>
</td>
<td valign="top" width="50%">
<p>Expand phase добавляет backward-compatible objects: new columns, tables, indexes или constraints. Migration phase updates code и backfills данных. Contract phase removes old objects после unused.</p>
<p>Это standard approach for zero-downtime схемы evolution.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Example: add <code>full_name</code> while old <code>first_name</code>/<code>last_name</code> still exist; deploy code that writes both; backfill; read new column; remove old columns later.</p>
<p>Each step should be deployable and reversible enough.</p>
</td>
<td valign="top" width="50%">
<p>Пример: add <code>full_name</code>, пока old <code>first_name</code>/<code>last_name</code> still exist; deploy code writes both; дозаполнение (backfill); read new column; remove old columns later.</p>
<p>Каждый step should be deployable and reversible enough.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, separate expand and contract by at least one release. Verify metrics and logs before dropping old columns.</p>
<p>Keep compatibility across rolling deployments.</p>
</td>
<td valign="top" width="50%">
<p>В production separate expand and contract минимум by one release. Verify metrics и logs перед dropping old columns.</p>
<p>Keep compatibility across rolling deployments.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Contract too early breaks old code.</li>
<li>Dual-write logic must be tested.</li>
<li>Backfills can take longer than planned.</li>
<li>Old data and new data must be reconciled.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Contract too early ломает old code.</li>
<li>Dual-write logic must be tested.</li>
<li>Backfills могут take longer than planned.</li>
<li>Old данных and new данных must be reconciled.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: What are the phases of expand-and-contract?<br>A: Expand schema, migrate/backfill usage, then contract old schema.</p>
</td>
<td valign="top" width="50%">
<p>Q: Какие phases expand-and-contract?<br>A: Expand схемы, migrate/дозаполнение (backfill) usage, then contract old схемы.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-005"></a>

<details>
<summary><strong>DB-MIG-005. How would you add a nullable column safely?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Adding a nullable column without a heavy default is usually a safe, backward-compatible expand step.</p>
</td>
<td valign="top" width="50%">
<p>Добавление nullable column без heavy default обычно safe, backward-compatible expand step.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A simple <code>ALTER TABLE ... ADD COLUMN new_col type</code> is generally compatible with old code because existing rows get NULL and old code ignores the column.</p>
<p>Then new code can start writing it.</p>
</td>
<td valign="top" width="50%">
<p>Простой <code>ALTER TABLE ... ADD COLUMN new_col type</code> обычно compatible with old code, потому что existing rows get NULL и old code ignores column.</p>
<p>Затем new code starts writing it.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Be careful with default values and NOT NULL. Some changes can rewrite a large table or take stronger locks depending on PostgreSQL version and expression.</p>
<p>If the column must eventually be mandatory, add it nullable first, backfill, then add constraint later.</p>
</td>
<td valign="top" width="50%">
<p>Be careful with default values и NOT NULL. Некоторые changes могут rewrite large table или take stronger locks depending on PostgreSQL version and expression.</p>
<p>Если column eventually mandatory, add nullable first, дозаполнение (backfill), then add constraint later.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, check table size, lock behavior, replication, and migration timeout. Run during a safe window if the table is hot.</p>
<p>Deploy code that can tolerate NULL until backfill and constraint are complete.</p>
</td>
<td valign="top" width="50%">
<p>В production check table size, поведение блокировок, replication и migration timeout. Run during safe window if table hot.</p>
<p>Deploy code that can tolerate NULL until дозаполнение (backfill) and constraint complete.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Nullable does not mean business-optional forever.</li>
<li>Backfill and constraints need a later step.</li>
<li>Adding default can change lock/rewrite behavior.</li>
<li>Old app versions must ignore the new column.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Nullable не значит business-optional forever.</li>
<li>Backfill and constraints need later step.</li>
<li>Adding default can change lock/rewrite behavior.</li>
<li>Old app versions должны ignore new column.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Safe first step for new required column?<br>A: Add it nullable first, then backfill and constrain later.</p>
</td>
<td valign="top" width="50%">
<p>Q: Safe first step для новой required column?<br>A: Добавить nullable first, затем дозаполнение (backfill) и constrain later.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-006"></a>

<details>
<summary><strong>DB-MIG-006. How would you add a NOT NULL column to a huge table safely?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Add it nullable, backfill in batches, validate data, then add NOT NULL or a validated constraint in a separate step.</p>
</td>
<td valign="top" width="50%">
<p>Добавить nullable, дозаполнение (backfill) in batches, validate данных, затем add NOT NULL или validated constraint separate step.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Adding a NOT NULL column with a value for every existing row can be risky on a huge table. The safe pattern is multi-step.</p>
<p>First add nullable column. Then deploy code writing it. Then backfill old rows in chunks. Then enforce non-null.</p>
</td>
<td valign="top" width="50%">
<p>Adding NOT NULL column with value for every existing row can be risky on huge table. Safe pattern is multi-step.</p>
<p>Сначала add nullable column. Затем deploy code writing it. Then дозаполнение (backfill) old rows in chunks. Then enforce non-null.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>For enforcement, a common PostgreSQL pattern is: add <code>CHECK (new_col IS NOT NULL) NOT VALID</code>, validate it separately with <code>VALIDATE CONSTRAINT</code>, then set <code>NOT NULL</code> in a short controlled step.</p>
<p>Test the exact PostgreSQL version and lock behavior on staging before running it on a hot table.</p>
</td>
<td valign="top" width="50%">
<p>Для enforcement часто используют PostgreSQL pattern: добавить <code>CHECK (new_col IS NOT NULL) NOT VALID</code>, отдельно выполнить <code>VALIDATE CONSTRAINT</code>, затем коротким контролируемым шагом поставить <code>NOT NULL</code>.</p>
<p>Перед запуском на hot table нужно проверить точную версию PostgreSQL и поведение блокировок на staging.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Backfill should be rate-limited, resumable and monitored. Use small transactions to avoid long locks, WAL spikes and replication lag.</p>
<p>Use <code>lock_timeout</code> and <code>statement_timeout</code> for DDL steps so the migration fails fast instead of joining a production lock queue. Application code should handle NULL until the final enforcement step.</p>
</td>
<td valign="top" width="50%">
<p>Backfill должен быть rate-limited, resumable и monitored. Маленькие transactions помогают избежать long locks, WAL spikes и задержка репликации (replication lag).</p>
<p>Для DDL steps стоит задавать <code>lock_timeout</code> и <code>statement_timeout</code>, чтобы migration быстро падала, а не вставала в production lock queue. Application должна уметь работать с NULL до финального enforcement step.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>One huge UPDATE is dangerous.</li>
<li>NOT NULL too early breaks old writes.</li>
<li>Backfill can create bloat and WAL.</li>
<li>Validation can still consume resources.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Один huge UPDATE dangerous.</li>
<li>NOT NULL too early ломает old writes.</li>
<li>Backfill can create bloat and WAL.</li>
<li>Validation still consumes resources.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Safe pattern for NOT NULL on huge table?<br>A: Nullable column → code writes it → batched backfill → validate/enforce NOT NULL.</p>
</td>
<td valign="top" width="50%">
<p>Q: Safe pattern для NOT NULL на huge table?<br>A: Nullable column → code writes it → batched дозаполнение (backfill) → validate/enforce NOT NULL.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-007"></a>

<details>
<summary><strong>DB-MIG-007. How would you add a new index on a large production table?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Use CREATE INDEX CONCURRENTLY when possible, run it separately, monitor progress, and validate it with real query plans.</p>
</td>
<td valign="top" width="50%">
<p>Использовать CREATE INDEX CONCURRENTLY где возможно, run separately, monitor progress и validate with реальный запрос plans.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A regular CREATE INDEX can block writes to the table. On large hot tables, that can cause downtime.</p>
<p><code>CREATE INDEX CONCURRENTLY</code> avoids blocking normal writes for most of the build, but takes longer and has its own restrictions.</p>
</td>
<td valign="top" width="50%">
<p>Regular CREATE INDEX может block writes to table. На large горячие таблицы это can cause downtime.</p>
<p><code>CREATE INDEX CONCURRENTLY</code> avoids blocking normal writes for most build, но takes longer и имеет restrictions.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Before adding the index, confirm it is needed: inspect slow query, expected selectivity, existing indexes, write overhead, and possible redundancy.</p>
<p>After creation, run <code>EXPLAIN (ANALYZE, BUFFERS)</code> on representative queries.</p>
</td>
<td valign="top" width="50%">
<p>Перед adding index confirm needed: inspect медленный запрос, expected selectivity, existing indexes, write overhead и possible redundancy.</p>
<p>After creation run <code>EXPLAIN (ANALYZE, BUFFERS)</code> on representative queries.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, avoid wrapping CREATE INDEX CONCURRENTLY in a transaction block. Schedule it, monitor locks/progress, disk usage, replication lag and failure cleanup.</p>
<p>If it fails, it may leave an invalid index that needs cleanup.</p>
</td>
<td valign="top" width="50%">
<p>В production avoid wrapping CREATE INDEX CONCURRENTLY in transaction block. Schedule it, monitor locks/progress, disk usage, задержка репликации (replication lag) and failure cleanup.</p>
<p>If it fails, it may leave invalid index requiring cleanup.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Concurrent index build is safer but not free.</li>
<li>It still consumes CPU/IO and disk.</li>
<li>It can take much longer than regular CREATE INDEX.</li>
<li>Too many indexes hurt writes.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Concurrent index build safer, but not free.</li>
<li>It still consumes CPU/IO and disk.</li>
<li>It can take much longer than regular CREATE INDEX.</li>
<li>Too many indexes hurt writes.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: How add index on hot large table?<br>A: Use CREATE INDEX CONCURRENTLY, monitor it, and validate with EXPLAIN.</p>
</td>
<td valign="top" width="50%">
<p>Q: Как add index на hot large table?<br>A: Use CREATE INDEX CONCURRENTLY, monitor it и validate with EXPLAIN.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-008"></a>

<details>
<summary><strong>DB-MIG-008. Why can CREATE INDEX block writes?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Regular CREATE INDEX takes locks that prevent concurrent writes to the indexed table while the index is built.</p>
</td>
<td valign="top" width="50%">
<p>Regular CREATE INDEX берет locks, которые prevent concurrent writes to indexed table while index built.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>PostgreSQL must build a consistent index over existing table data. A regular build is faster but more intrusive.</p>
<p>For hot tables, blocked writes can cascade into application timeouts and pool exhaustion.</p>
</td>
<td valign="top" width="50%">
<p>PostgreSQL должен build consistent index over existing table данных. Regular build быстрее, но more intrusive.</p>
<p>Для горячие таблицы заблокированным writes могут cascade into application timeouts и исчерпание пула.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>This is why PostgreSQL provides <code>CREATE INDEX CONCURRENTLY</code>. It uses a more complex multi-phase process to allow writes, but with more time and overhead.</p>
<p>Reads are usually less of a concern than writes for a regular index build.</p>
</td>
<td valign="top" width="50%">
<p>Поэтому PostgreSQL предоставляет <code>CREATE INDEX CONCURRENTLY</code>. Он uses more complex multi-phase process to allow writes, but more time and overhead.</p>
<p>Reads usually less concern than writes for regular index build.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, a blocking index build can look like a full application outage. Always know the lock behavior of DDL before running it.</p>
<p>Use lock timeouts and maintenance windows if needed.</p>
</td>
<td valign="top" width="50%">
<p>В production blocking index build может look like full application outage. Always know поведение блокировок of DDL before running.</p>
<p>Use lock timeouts и maintenance windows if needed.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>CREATE INDEX on large hot table is risky.</li>
<li>Lock waits can exhaust app connection pools.</li>
<li>DDL should be reviewed separately from small code changes.</li>
<li>Rollback/cleanup plan is needed.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>CREATE INDEX на large hot table risky.</li>
<li>Lock waits can exhaust app пулы соединений (connection pools).</li>
<li>DDL should be reviewed separately from small code changes.</li>
<li>Откат/cleanup plan needed.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Why regular CREATE INDEX is risky?<br>A: It can block writes while building the index.</p>
</td>
<td valign="top" width="50%">
<p>Q: Почему regular CREATE INDEX risky?<br>A: Он может block writes while building index.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-009"></a>

<details>
<summary><strong>DB-MIG-009. Why should CREATE INDEX CONCURRENTLY be used carefully?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>It avoids most write blocking but is slower, more resource-intensive, cannot run inside a transaction block, and can leave invalid indexes on failure.</p>
</td>
<td valign="top" width="50%">
<p>Он avoids most write blocking, но slower, more resource-intensive, cannot run inside transaction block и can leave invalid indexes on failure.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Concurrent index creation scans the table more than once and coordinates with concurrent transactions. This makes it safer for availability but heavier operationally.</p>
<p>If canceled or failed, cleanup may be required.</p>
</td>
<td valign="top" width="50%">
<p>Concurrent index creation scans table more than once и coordinates with concurrent transactions. Это safer for availability, но heavier operationally.</p>
<p>If canceled/failed, cleanup may be required.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>It still uses CPU, IO, WAL and disk. On replicas, it can increase lag. On primary, it can compete with production workload.</p>
<p>Also, not all migrations tools run it correctly unless configured, because it cannot be inside a transaction block.</p>
</td>
<td valign="top" width="50%">
<p>It still uses CPU, IO, WAL and disk. On replicas, it can increase lag. On primary, compete with production workload.</p>
<p>Также не все migration tools run it correctly unless configured, because cannot be inside transaction block.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Run it as a separate migration or operational task. Monitor progress, locks, IO, replication lag, and invalid index state.</p>
<p>Plan cleanup for failed builds.</p>
</td>
<td valign="top" width="50%">
<p>Run it как separate migration или operational task. Monitor progress, locks, IO, задержка репликации (replication lag) и invalid index state.</p>
<p>Plan cleanup for failed builds.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Concurrent does not mean zero impact.</li>
<li>It may fail and leave invalid index.</li>
<li>Migration transaction wrappers can break it.</li>
<li>Building many indexes concurrently can overload DB.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Concurrent не значит zero impact.</li>
<li>It may fail and leave invalid index.</li>
<li>Migration transaction wrappers can break it.</li>
<li>Building many indexes concurrently can overload DB.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Why be careful with CREATE INDEX CONCURRENTLY?<br>A: It is availability-friendly but slower/heavier and has failure/transaction restrictions.</p>
</td>
<td valign="top" width="50%">
<p>Q: Почему careful with CREATE INDEX CONCURRENTLY?<br>A: Он availability-friendly, но slower/heavier и имеет failure/transaction restrictions.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-010"></a>

<details>
<summary><strong>DB-MIG-010. How would you backfill data safely?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Backfill in small, resumable batches with rate limiting, monitoring, and idempotent logic.</p>
</td>
<td valign="top" width="50%">
<p>Backfill делать small resumable batches с rate limiting, monitoring и идемпотентным logic.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Backfill means populating new or corrected data for existing rows. A huge single UPDATE can create locks, WAL spikes, bloat, replica lag and long rollbacks.</p>
<p>Use a stable key range and commit after each batch.</p>
</td>
<td valign="top" width="50%">
<p>Backfill означает populate new/corrected данных для existing rows. Huge single UPDATE can create locks, WAL spikes, bloat, replica lag и long rollbacks.</p>
<p>Use stable key range и commit after each batch.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Good backfills are restartable: if the job stops, it continues from last processed id or reprocesses safely.</p>
<p>Add progress metrics, dry-run mode, and validation queries. For worker-based jobs, <code>FOR UPDATE SKIP LOCKED</code> can let multiple workers claim rows without blocking each other.</p>
<pre><code>UPDATE orders o
SET new_col = ...
WHERE o.id IN (
  SELECT id
  FROM orders
  WHERE new_col IS NULL
  ORDER BY id
  LIMIT 1000
  FOR UPDATE SKIP LOCKED
);</code></pre>
</td>
<td valign="top" width="50%">
<p>Хороший дозаполнение (backfill) можно безопасно продолжить: если job остановилась, она продолжает с last processed id или повторно обрабатывает rows идемпотентно.</p>
<p>Нужны progress metrics, dry-run mode и validation queries. Для worker-based jobs <code>FOR UPDATE SKIP LOCKED</code> помогает нескольким workers забирать rows без взаимной блокировки.</p>
<pre><code>UPDATE orders o
SET new_col = ...
WHERE o.id IN (
  SELECT id
  FROM orders
  WHERE new_col IS NULL
  ORDER BY id
  LIMIT 1000
  FOR UPDATE SKIP LOCKED
);</code></pre>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, throttle based on DB load, replication lag and lock waits. Run during low traffic if necessary.</p>
<p>Backfill code should handle concurrent application writes.</p>
</td>
<td valign="top" width="50%">
<p>В production throttle based on DB load, задержка репликации (replication lag) и ожидание блокировок. Run during low traffic if needed.</p>
<p>Backfill code should handle concurrent application writes.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Huge updates create bloat.</li>
<li>Backfill and live writes can race.</li>
<li>Retries require idempotency.</li>
<li>Validation is as important as writing.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Huge updates create bloat.</li>
<li>Backfill и live writes can race.</li>
<li>Retries require idempotency.</li>
<li>Validation as important as writing.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Safe backfill principle?<br>A: Small idempotent batches with monitoring and validation.</p>
</td>
<td valign="top" width="50%">
<p>Q: Safe дозаполнение (backfill) principle?<br>A: Small идемпотентным batches with monitoring and validation.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-011"></a>

<details>
<summary><strong>DB-MIG-011. How would you rename a column without breaking old application versions?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Use expand-and-contract: add new column, dual-write or sync, migrate reads, then drop old column later.</p>
</td>
<td valign="top" width="50%">
<p>Используй expand-and-contract: add new column, dual-write или sync, migrate reads, then drop old column later.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A direct <code>ALTER TABLE RENAME COLUMN</code> breaks old code that still references the old name during rolling deploy.</p>
<p>Instead, keep both names available during the transition.</p>
</td>
<td valign="top" width="50%">
<p>Direct <code>ALTER TABLE RENAME COLUMN</code> ломает old code, которое still references old name during rolling deploy.</p>
<p>Instead keep both names available during transition.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Possible steps: add new column; write both old and new; backfill new from old; read from new; stop using old; drop old in a later release.</p>
<p>Views or compatibility layers may help for some cases.</p>
</td>
<td valign="top" width="50%">
<p>Possible steps: add new column; write both old and new; дозаполнение (backfill) new from old; read from new; stop using old; drop old in later release.</p>
<p>Views or compatibility layers may help in some cases.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, inspect all consumers: application versions, analytics, reports, ETL, ad hoc scripts, stored procedures.</p>
<p>Column rename is often more dangerous than it looks.</p>
</td>
<td valign="top" width="50%">
<p>В production inspect all consumers: application versions, analytics, reports, ETL, ad hoc scripts, stored procedures.</p>
<p>Column rename often more dangerous than looks.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Rolling deployments mean old and new code coexist.</li>
<li>Direct rename is a breaking change.</li>
<li>Dual-write requires reconciliation.</li>
<li>Do not forget external consumers.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Rolling deployments mean old/new code coexist.</li>
<li>Direct rename breaking change.</li>
<li>Dual-write requires reconciliation.</li>
<li>Do not forget external consumers.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Safe column rename pattern?<br>A: Add new column, migrate usage, drop old column later.</p>
</td>
<td valign="top" width="50%">
<p>Q: Safe column rename pattern?<br>A: Add new column, migrate usage, drop old column later.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-012"></a>

<details>
<summary><strong>DB-MIG-012. How would you change a column type safely?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Add a new column with the new type, dual-write/backfill, switch reads, then remove the old column later.</p>
</td>
<td valign="top" width="50%">
<p>Add new column with new type, dual-write/дозаполнение (backfill), switch reads, then remove old column later.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Direct <code>ALTER COLUMN TYPE</code> can rewrite a table, lock writes, fail on bad data, or break old application versions.</p>
<p>The safer pattern is expand-and-contract.</p>
</td>
<td valign="top" width="50%">
<p>Direct <code>ALTER COLUMN TYPE</code> can rewrite table, lock writes, fail on bad данных или break old app versions.</p>
<p>Safer pattern — expand-and-contract.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>For small tables or metadata-only compatible changes, direct type change might be acceptable. For huge hot tables, avoid large blocking rewrites.</p>
<p>Backfill conversion should be validated with edge cases.</p>
</td>
<td valign="top" width="50%">
<p>Для small tables или metadata-only compatible changes direct type change can be acceptable. Для huge горячие таблицы avoid large blocking rewrites.</p>
<p>Backfill conversion should be validated with edge cases.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Production plan: add new column, update app to write both, backfill in batches, compare old/new values, switch reads, stop old writes, drop old later.</p>
<p>Keep rollback points between steps.</p>
</td>
<td valign="top" width="50%">
<p>Production plan: add new column, update app writes both, дозаполнение (backfill) in batches, compare old/new values, switch reads, stop old writes, drop old later.</p>
<p>Keep rollback points between steps.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Type conversion can lose data.</li>
<li>Indexes and constraints may need changes too.</li>
<li>ORM mappings must change in sync.</li>
<li>Old consumers may still expect old type.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Type conversion can lose данных.</li>
<li>Indexes and constraints may need changes too.</li>
<li>ORM mappings должны change in sync.</li>
<li>Old consumers may still expect old type.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Safe large column type change?<br>A: New column, dual-write, backfill/validate, switch, drop old later.</p>
</td>
<td valign="top" width="50%">
<p>Q: Safe large column type change?<br>A: New column, dual-write, дозаполнение (backfill)/validate, switch, drop old later.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-013"></a>

<details>
<summary><strong>DB-MIG-013. How would you drop a column safely?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Stop all reads/writes first, deploy compatible code, verify no consumers use it, then drop it in a later release.</p>
</td>
<td valign="top" width="50%">
<p>Сначала stop all reads/writes, deploy compatible code, verify no consumers use it, then drop in later release.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Dropping a column is destructive and breaks old code immediately. It should be the final contract step, not the first migration.</p>
<p>Before dropping, search code, reports, ETL jobs, dashboards, stored procedures, and ad hoc integrations.</p>
</td>
<td valign="top" width="50%">
<p>Dropping column destructive and breaks old code immediately. It should be final contract step, not first migration.</p>
<p>Before dropping, search code, reports, ETL jobs, dashboards, stored procedures и ad hoc integrations.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>A common safe pattern is: stop using column in code, deploy, monitor, optionally rename to mark deprecated or revoke access, then drop after confidence window.</p>
<p>Backups do not replace a rollback plan.</p>
</td>
<td valign="top" width="50%">
<p>Common safe pattern: stop using column in code, deploy, monitor, optionally rename to mark deprecated or revoke access, then drop after confidence window.</p>
<p>Backups do not replace план отката.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, watch query logs or dependency tracking to ensure no usage. Coordinate with BI/data teams.</p>
<p>Schedule drop during safe window if the table is huge or hot.</p>
</td>
<td valign="top" width="50%">
<p>В production watch query logs/dependency tracking to ensure no usage. Coordinate with BI/данных teams.</p>
<p>Schedule drop during safe window if table huge/hot.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Drop is not backward-compatible.</li>
<li>Rollback may require restoring data from backup.</li>
<li>External consumers are easy to miss.</li>
<li>ORM generated SQL may still include the column.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Drop not backward-compatible.</li>
<li>Откат may require restoring данных from резервная копия.</li>
<li>External consumers easy to miss.</li>
<li>ORM generated SQL may still include column.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: When should you drop old column?<br>A: Only after all consumers have stopped using it and compatibility window passed.</p>
</td>
<td valign="top" width="50%">
<p>Q: Когда drop old column?<br>A: Только after all consumers stopped using it and окно совместимости passed.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-014"></a>

<details>
<summary><strong>🧠 DB-MIG-014. How would you roll back a failed database migration?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Rollback depends on migration type: some can be reverted by SQL, but destructive/data migrations often need forward fixes or restore/reconciliation.</p>
</td>
<td valign="top" width="50%">
<p>Откат зависит от migration type: some can be reverted by SQL, but destructive/данных миграции often need forward fixes or restore/reconciliation.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>For simple additive migrations, rollback may mean dropping a newly added column or index. For data changes, rollback may require previous values, audit logs, backups, or compensating migration.</p>
<p>Do not assume down migration is always safe.</p>
</td>
<td valign="top" width="50%">
<p>Для simple additive миграции rollback может mean drop newly added column/index. Для данных changes rollback может require previous values, журналов аудита, резервные копии или compensating migration.</p>
<p>Do not assume down migration always safe.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>The safest rollback is often avoiding destructive first steps. Expand-and-contract gives you rollback points before contract/destructive stage.</p>
<p>For failed partial migrations, you may need to repair migration metadata carefully.</p>
</td>
<td valign="top" width="50%">
<p>Safest rollback often avoiding destructive first steps. Expand-and-contract gives rollback points before contract/destructive stage.</p>
<p>For failed partial миграции, may need repair migration metadata carefully.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, document rollback before running migration. Know whether you will roll back code, roll forward database, or restore data.</p>
<p>Practice restore procedures, not just schema rollback.</p>
</td>
<td valign="top" width="50%">
<p>В production document rollback before running migration. Know whether roll back code, roll forward database или restore данных.</p>
<p>Practice restore procedures, not just схемы rollback.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Down migrations can lose new data.</li>
<li>Failed migrations can leave locks or invalid objects.</li>
<li>Code rollback may not match DB rollback.</li>
<li>Backups must be tested.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Down миграции can lose new данных.</li>
<li>Failed миграции can leave locks/invalid objects.</li>
<li>Code rollback may not match DB rollback.</li>
<li>Backups must be tested.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Why rollback can be hard?<br>A: Data/destructive changes may not be reversible without backups or compensating logic.</p>
</td>
<td valign="top" width="50%">
<p>Q: Почему rollback can be hard?<br>A: Data/destructive changes may not be reversible without резервные копии or compensating logic.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-015"></a>

<details>
<summary><strong>DB-MIG-015. Why are long DDL locks dangerous?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Long DDL locks can block application reads/writes and cascade into timeouts, pool exhaustion and outages.</p>
</td>
<td valign="top" width="50%">
<p>Long DDL locks могут block application reads/writes и cascade into timeouts, исчерпание пула и outages.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>DDL often needs table locks to change metadata safely. Even a short metadata lock can be dangerous if it waits behind a long transaction and blocks subsequent queries.</p>
<p>This is a common production migration failure mode.</p>
</td>
<td valign="top" width="50%">
<p>DDL часто needs table locks to change metadata safely. Even short metadata lock dangerous if waits behind long transaction and blocks subsequent queries.</p>
<p>Это common production migration failure mode.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Lock queues matter: a DDL waiting for an exclusive lock can block later compatible queries behind it, causing a traffic pile-up.</p>
<p>Use lock_timeout so migrations fail instead of waiting indefinitely.</p>
</td>
<td valign="top" width="50%">
<p>Lock queues matter: DDL waiting for exclusive lock can block later compatible queries behind it, causing traffic pile-up.</p>
<p>Use lock_timeout so миграции fail instead of waiting indefinitely.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, check active transactions before DDL, use short lock timeouts, schedule risky changes, and monitor blocked sessions.</p>
<p>Use online/concurrent variants when available. A useful diagnostic path is <code>pg_stat_activity</code> + <code>pg_locks</code> + <code>pg_blocking_pids(pid)</code>, with query age and transaction age visible.</p>
</td>
<td valign="top" width="50%">
<p>В production перед DDL проверяют active transactions, ставят короткие lock timeouts, планируют рискованные changes и мониторят заблокированным sessions.</p>
<p>Когда возможно, используют online/concurrent variants. Практичный diagnostic path: <code>pg_stat_activity</code> + <code>pg_locks</code> + <code>pg_blocking_pids(pid)</code>, обязательно с query age и transaction age.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>A migration can block even before it acquires the lock.</li>
<li>Idle-in-transaction sessions can block DDL.</li>
<li>DDL lock waits can exhaust app threads.</li>
<li>Killing blockers needs careful judgement.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Migration can block even before lock acquired.</li>
<li>Idle-in-transaction sessions can block DDL.</li>
<li>DDL ожидание блокировок can exhaust app threads.</li>
<li>Killing blockers requires judgement.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Why use lock_timeout for migrations?<br>A: To fail fast instead of waiting and blocking production traffic.</p>
</td>
<td valign="top" width="50%">
<p>Q: Зачем lock_timeout for миграции?<br>A: Чтобы fail fast instead of waiting and blocking production traffic.</p>
</td>
</tr>
</tbody>
</table>

</details>

<a id="db-mig-016"></a>

<details>
<summary><strong>DB-MIG-016. How do you deploy application code and database migrations safely together?</strong></summary>

<table>
<tbody>
<tr>
<th colspan="2" align="left">Short answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Use compatibility-first sequencing: apply backward-compatible DB changes, deploy code, backfill/switch, then remove old schema later.</p>
</td>
<td valign="top" width="50%">
<p>Используй compatibility-first sequencing: apply backward-compatible DB changes, deploy code, дозаполнение (backfill)/switch, then remove old схемы later.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Interview answer</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>The database is shared state. During rolling deployments, old and new app versions can run simultaneously, so migrations must support both.</p>
<p>Do not deploy code that requires a destructive DB change before all old instances are gone.</p>
</td>
<td valign="top" width="50%">
<p>Database — shared state. During rolling deployments old and new app versions can работать одновременно, so миграции must поддерживать оба варианта.</p>
<p>Do not deploy code requiring destructive DB change before all old instances gone.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Deep dive</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Safe sequence: expand schema; deploy code that can use old/new; backfill; switch reads/writes with feature flag; verify; contract old schema.</p>
<p>Automate migrations but separate risky ones from normal app startup.</p>
</td>
<td valign="top" width="50%">
<p>Safe sequence: расширить схему; deploy code that can use old/new; дозаполнение (backfill); переключить чтение/запись with feature flag; verify; contract old схемы.</p>
<p>Automate миграции but separate risky ones from normal app startup.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Production angle</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>In production, include observability, rollback points, migration ownership, and communication with DB/ops teams.</p>
<p>CI/CD should make it hard to run destructive migrations accidentally.</p>
</td>
<td valign="top" width="50%">
<p>В production include observability, rollback points, migration ownership и communication with DB/ops teams.</p>
<p>CI/CD should make it hard to run destructive миграции accidentally.</p>
</td>
</tr>
<tr>
<th colspan="2" align="left">Traps / caveats</th>
</tr>
<tr>
<td valign="top" width="50%">
<ul>
<li>Rolling deploys require backward compatibility.</li>
<li>Running heavy migrations on every app startup is risky.</li>
<li>Feature flags do not replace data validation.</li>
<li>Old services and background jobs also matter.</li>
</ul>
</td>
<td valign="top" width="50%">
<ul>
<li>Rolling deploys require backward compatibility.</li>
<li>Running heavy миграции on every app startup risky.</li>
<li>Feature flags do not replace данных validation.</li>
<li>Old services and background jobs also matter.</li>
</ul>
</td>
</tr>
<tr>
<th colspan="2" align="left">Optional Anki</th>
</tr>
<tr>
<td valign="top" width="50%">
<p>Q: Safe DB/app deployment pattern?<br>A: Expand → deploy compatible code → backfill/switch → contract later.</p>
</td>
<td valign="top" width="50%">
<p>Q: Safe DB/app deployment pattern?<br>A: Expand → deploy compatible code → дозаполнение (backfill)/switch → contract later.</p>
</td>
</tr>
</tbody>
</table>

</details>
